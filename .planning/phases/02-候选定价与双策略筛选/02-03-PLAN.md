---
phase: 02-候选定价与双策略筛选
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - bot_v2.py
  - config.json
  - tests/test_phase2_strategies.py
autonomous: true
requirements: [STRAT-02, STRAT-03]
must_haves:
  truths:
    - "操作者可为低价 YES 与高价 NO 分别配置价格、概率、时间窗和仓位阈值。"
    - "scan loop 会对同一 bucket 同时跑 YES_SNIPER 与 NO_CARRY evaluator，并得到独立候选结论。"
    - "候选评估会把 accept / reject / size_down / reprice 结构化写入 market JSON，而不是只打印。"
  artifacts:
    - path: config.json
      provides: "yes_strategy/no_strategy config blocks"
      contains: "yes_strategy"
    - path: bot_v2.py
      provides: "dual strategy evaluators and candidate assessment persistence"
      contains: "candidate_assessments"
    - path: tests/test_phase2_strategies.py
      provides: "YES/NO strategy regression coverage"
      contains: "strategy_leg"
  key_links:
    - from: config.json
      to: bot_v2.py
      via: "strategy config loader"
      pattern: "yes_strategy|no_strategy"
    - from: bot_v2.py
      to: market JSON
      via: "candidate_assessments persistence"
      pattern: "candidate_assessments"
---

<objective>
把双策略从一套全局阈值升级为 YES/NO 分腿配置，并把候选评估结果结构化持久化为 `candidate_assessments`。

Purpose: 满足低价 YES 与高价 NO 两条赔率结构完全不同的策略腿需求，并为后续订单层保留稳定事实源。
Output: `yes_strategy` / `no_strategy` 配置块、dual evaluator helpers、候选评估持久化。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-候选定价与双策略筛选/02-RESEARCH.md
@bot_v2.py
@config.json

<interfaces>
Current global config shape:
```json
{
  "balance": 10000.0,
  "max_bet": 20.0,
  "min_ev": 0.1,
  "max_price": 0.45,
  "min_volume": 500,
  "min_hours": 2.0,
  "max_hours": 72.0,
  "kelly_fraction": 0.25,
  "scan_interval": 3600,
  "calibration_min": 30,
  "vc_key": "YOUR_KEY_HERE",
  "max_slippage": 0.03
}
```

Upstream inputs already available after Plan 02:
```python
mkt["bucket_probabilities"]
mkt["quote_snapshot"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 增加 YES/NO 分腿配置与 evaluator 合同</name>
  <files>config.json, tests/test_phase2_strategies.py, bot_v2.py</files>
  <behavior>
    - Test 1: YES evaluator 读取 `yes_strategy` 阈值，不受 `no_strategy` 配置影响。
    - Test 2: NO evaluator 读取 `no_strategy` 阈值，不复用 YES 的 `max_price` / `min_probability`。
    - Test 3: 同一 bucket 对 YES/NO 可得出不同结论，例如 YES rejected 但 NO accepted。
  </behavior>
  <action>把 `config.json` 从单组全局策略阈值改为共享基础配置 + `yes_strategy` / `no_strategy` 两个独立块，同时在 `bot_v2.py` 中新增配置读取与纯 evaluator helper。YES evaluator 面向低价被动买入，NO evaluator 面向高价 NO carry；两侧至少独立控制 price、probability、hours、size cap 相关阈值。不要在本任务里引入资金路由或组合暴露控制，那是 Phase 3 范围。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_strategies.py -q -k "yes or no"</automated>
  </verify>
  <done>YES/NO evaluator 都通过独立阈值回归，且配置文件已不再依赖单一 `max_price` / `min_ev` 覆盖所有策略腿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 dual strategy candidate assessments 接入 scan loop</name>
  <files>bot_v2.py, tests/test_phase2_strategies.py</files>
  <behavior>
    - Test 1: scan loop 对每个 bucket 同时产出 YES 与 NO candidate assessment。
    - Test 2: assessment 记录 `strategy_leg`、`status`、`reasons`、`fair_price`、`quote_context`、`size_multiplier` 等结构化字段。
    - Test 3: 未通过 execution guardrail 的 assessment 仍会保留 reject/reprice/size_down 解释，而不是直接消失。
  </behavior>
  <action>在 `scan_and_update()` 中消费 `bucket_probabilities` + `quote_snapshot`，为每个 bucket 生成 `candidate_assessments`。每条 assessment 至少包含 `strategy_leg`、`token_side`、`range`、`aggregate_probability`、`fair_yes`/`fair_no`、`quote_context`、`status`（accepted/rejected/size_down/reprice）、`reasons[]`、`size_multiplier`。保持 Phase 2 边界：只输出候选与推荐被动价/缩量因子，不创建 order intent，不更新仓位或风控总账。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_strategies.py -q</automated>
  </verify>
  <done>每个 ready market 都能持久化 `candidate_assessments`，且 YES/NO 两条策略腿独立输出、互不覆盖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → strategy evaluator | 用户配置直接改变 YES/NO 候选阈值 |
| probability/quote facts → candidate_assessments | 策略输出将被后续订单层与报告层复用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | `config.json` strategy blocks | mitigate | 显式解析 `yes_strategy` / `no_strategy` 必需字段；缺字段时让 evaluator 返回配置错误 reason，而不是静默回退到旧全局阈值。 |
| T-02-07 | R | `candidate_assessments` persistence | mitigate | assessment 必须持久化 `strategy_leg`、`status`、`reasons`、`quote_context`，保证后续订单/报告可追溯为何接受、缩量或降价。 |
| T-02-08 | E | future strategy routing | accept | 本计划只做单 market 候选评估，不处理组合级资金暴露；Phase 3 再做跨 market 暴露裁剪。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase2_strategies.py -q`
- `uv run pytest tests/test_phase2_probability.py tests/test_phase2_quotes.py tests/test_phase2_strategies.py -q`
</verification>

<success_criteria>
- 配置文件存在独立 `yes_strategy` / `no_strategy` 块。
- `candidate_assessments` 会同时输出 YES/NO 结论，并记录结构化理由。
- 旧的单阈值入场路径不再是 Phase 2 候选真相源。
</success_criteria>

<output>
After completion, create `.planning/phases/02-候选定价与双策略筛选/02-03-SUMMARY.md`
</output>
