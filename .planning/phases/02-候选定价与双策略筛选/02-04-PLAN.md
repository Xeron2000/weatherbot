---
phase: 02-候选定价与双策略筛选
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - bot_v2.py
  - tests/test_phase2_reporting.py
  - README.md
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "操作者可以在 status/report 中看见每个候选为何 accepted、rejected、size_down 或 reprice。"
    - "候选解释同时展示 strategy_leg、bucket、fair price 与 quote context，而不是只有 free-form print 文本。"
    - "Phase 2 的本地验证命令和候选 JSON 字段在 README 中有最小说明。"
  artifacts:
    - path: bot_v2.py
      provides: "candidate assessment reporting"
      contains: "print_scan_summary"
    - path: tests/test_phase2_reporting.py
      provides: "OBS-01 reporting regressions"
      contains: "candidate_assessments"
    - path: README.md
      provides: "Phase 2 verification guidance"
      contains: "candidate_assessments"
  key_links:
    - from: bot_v2.py
      to: tests/test_phase2_reporting.py
      via: "operator-facing reporting contract"
      pattern: "accepted|rejected|size_down|reprice"
    - from: market JSON
      to: print_status/print_report
      via: "candidate_assessments"
      pattern: "strategy_leg"
---

<objective>
把 `candidate_assessments` 变成操作者真正看得见的候选解释面，并补齐 Phase 2 使用说明。

Purpose: OBS-01 要求不是“代码里算过理由”，而是操作者能直接看到候选为何被接受、拒绝、缩量或降价。
Output: reporting 回归测试、status/report 候选解释输出、README Phase 2 验证说明。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-市场语义与扫描基线/01-03-SUMMARY.md
@.planning/phases/01-市场语义与扫描基线/01-04-SUMMARY.md
@bot_v2.py
@README.md

<interfaces>
Current reporting seam:
```python
def print_scan_summary(markets):
    accepted = [m for m in markets if m.get("last_scan_status") == "ready"]
    skipped = [m for m in markets if m.get("last_scan_status") == "skipped"]
```

Expected upstream data after Plan 03:
```python
mkt["candidate_assessments"] = [
    {
        "strategy_leg": "yes_strategy" | "no_strategy",
        "status": "accepted" | "rejected" | "size_down" | "reprice",
        "reasons": [...],
        "quote_context": {...},
    }
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 candidate assessments 建立 operator-facing reporting 合同</name>
  <files>tests/test_phase2_reporting.py, bot_v2.py</files>
  <behavior>
    - Test 1: `print_status()` 和 `print_report()` 会展示 YES/NO 候选摘要，而不是只显示 accepted/skipped market 数。
    - Test 2: 输出中可见 `strategy_leg`、bucket、status、reason codes、fair price 与 quote context 关键字段。
    - Test 3: 即使没有持仓或 resolved trades，也能先看到候选解释摘要。
  </behavior>
  <action>新增 `tests/test_phase2_reporting.py`，沿用 Phase 1 reporting smoke-test 风格，直接断言 operator-facing 字符串。随后扩展 `print_scan_summary()` / `print_status()` / `print_report()`，把 `candidate_assessments` 以按 market、strategy_leg 分组的摘要展示出来。保持“scan summary 与 trade stats 分离”的 Phase 1 模式，不要把候选数混入持仓/战绩统计。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_reporting.py -q</automated>
  </verify>
  <done>status/report 在无交易时也能显示结构化候选解释，且 reporting 测试会在缺失关键 explanation 字段时失败。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 README 的 Phase 2 候选字段与验证命令</name>
  <files>README.md</files>
  <action>在 README 中补一小节说明 `bucket_probabilities`、`quote_snapshot`、`candidate_assessments` 的用途，以及本 phase 推荐的本地验证命令。只写与 Phase 2 新事实源直接相关的说明，不扩展到 Phase 3+ 的订单/风控路线图。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_reporting.py -q</automated>
  </verify>
  <done>README 能让操作者知道 Phase 2 候选解释数据存在哪里、怎么本地跑回归验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted candidate facts → terminal output | 操作者将依据 CLI 输出理解候选接受/拒绝原因 |
| README guidance → manual verification | 文档会影响操作者如何验证候选链路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | R | `print_status()` / `print_report()` candidate output | mitigate | 输出直接读取 `candidate_assessments` 持久化字段，不在展示层重新推断 fair price 或 reason；用 smoke tests 防止 operator-visible 字段丢失。 |
| T-02-10 | I | README Phase 2 guidance | accept | README 只记录本地验证和 JSON 字段，不包含任何 auth/secret/真实交易步骤，因此信息泄露风险低。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase2_reporting.py -q`
- `uv run pytest tests/test_phase2_probability.py tests/test_phase2_quotes.py tests/test_phase2_strategies.py tests/test_phase2_reporting.py -q`
</verification>

<success_criteria>
- status/report 能显示结构化候选解释。
- 输出包含 accept/reject/size_down/reprice 四类状态中的实际候选状态。
- README 包含 Phase 2 回归命令与新持久化字段说明。
</success_criteria>

<output>
After completion, create `.planning/phases/02-候选定价与双策略筛选/02-04-SUMMARY.md`
</output>
