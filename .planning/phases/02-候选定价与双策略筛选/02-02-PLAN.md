---
phase: 02-候选定价与双策略筛选
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - bot_v2.py
  - tests/conftest.py
  - tests/fixtures/phase2_clob_book_yes.json
  - tests/fixtures/phase2_clob_book_no.json
  - tests/test_phase2_quotes.py
autonomous: true
requirements: [MKT-04, RISK-03]
must_haves:
  truths:
    - "机器人基于 token-level bid/ask、tick size、book 状态判断 bucket 是否值得进入候选。"
    - "缺少实时 quote、tick size、open orderbook 或 market state 不可执行时，候选会被停单并写明 reason code。"
    - "YES 与 NO 两侧 quote 真相来自各自 token_id，而不是 `1 - yes_price` 或 Gamma market convenience price。"
  artifacts:
    - path: bot_v2.py
      provides: "CLOB quote snapshot helpers and execution guardrails"
      contains: "quote_snapshot"
    - path: tests/test_phase2_quotes.py
      provides: "MKT-04/RISK-03 regression coverage"
      contains: "orderbook_empty"
    - path: tests/fixtures/phase2_clob_book_yes.json
      provides: "YES token orderbook fixture"
  key_links:
    - from: bot_v2.py
      to: tests/test_phase2_quotes.py
      via: "quote snapshot guardrail contract"
      pattern: "tick_size_missing|orderbook_empty|market_closed"
    - from: bot_v2.py
      to: market JSON
      via: "token-side quote persistence"
      pattern: "quote_snapshot"
---

<objective>
把 Phase 2 的执行面从 Gamma convenience price 切到 CLOB token-level quote snapshot，并把不可执行盘口直接转成停单 reason codes。

Purpose: 候选是否值得挂单必须基于真实可执行盘口，而不是 discovery 层近似价格。
Output: quote fixtures、MKT-04/RISK-03 测试、`quote_snapshot` 与 execution guardrails 接线。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-候选定价与双策略筛选/02-RESEARCH.md
@.planning/phases/01-市场语义与扫描基线/01-02-SUMMARY.md
@bot_v2.py

<interfaces>
From `bot_v2.py` Phase 1 market contracts:
```python
{
    "market_id": market.get("id", ""),
    "question": question,
    "range": rng,
    "condition_id": condition_id,
    "token_id_yes": token_id_yes,
    "token_id_no": token_id_no,
    "unit": unit or expected_unit,
}
```

Current anti-pattern to replace:
```python
r = requests.get(f"https://gamma-api.polymarket.com/markets/{best_signal['market_id']}")
real_ask = float(mdata.get("bestAsk", best_signal["entry_price"]))
real_bid = float(mdata.get("bestBid", best_signal["bid_at_entry"]))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 YES/NO token-level quote fixtures 与 guardrail 合同</name>
  <files>tests/conftest.py, tests/fixtures/phase2_clob_book_yes.json, tests/fixtures/phase2_clob_book_no.json, tests/test_phase2_quotes.py, bot_v2.py</files>
  <behavior>
    - Test 1: quote helper 会分别读取 YES / NO token 的 bid、ask、spread、tick_size、min_order_size。
    - Test 2: book 为空、tick size 缺失、market closed、quote 缺字段时会返回明确 reason code。
    - Test 3: NO 侧不能通过 `1 - yes_price` 近似，必须读取独立 token quote。
  </behavior>
  <action>新增 CLOB orderbook fixtures，并在 `tests/test_phase2_quotes.py` 写出 token-level quote contract。随后在 `bot_v2.py` 增加纯 helper：按 token id 拉取/规范化 YES 与 NO 两侧 quote snapshot，输出结构化字段（至少含 `side`、`bid`、`ask`、`spread`、`tick_size`、`min_order_size`、`book_ok`、`reason_codes`）。这里保持 `requests`，不要提前引入 `py-clob-client` 或 WebSocket；quote helper 需为后续 plan 留出 side 参数化接口。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_quotes.py -q</automated>
  </verify>
  <done>YES/NO token quote helpers 通过回归，且 guardrail reason codes 能稳定覆盖缺盘口/closed/tick-size 缺失场景。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 quote snapshot 与 execution stop reasons 接入 scan loop</name>
  <files>bot_v2.py, tests/test_phase2_quotes.py</files>
  <behavior>
    - Test 1: admissible market 会持久化 YES/NO 两侧 `quote_snapshot`，供策略层复用。
    - Test 2: 若 quote guardrail 不通过，则 market 仍可保留语义与概率，但候选层会得到 execution stop reasons。
  </behavior>
  <action>在 `scan_and_update()` 中于 `bucket_probabilities` 之后生成 token-level `quote_snapshot`。持久化结构必须保留 YES/NO 两侧独立 quote，且把 `market_closed`、`orderbook_empty`、`tick_size_missing`、`missing_quote_book` 等原因落到 execution layer。不要在此任务里实现策略阈值与最终候选接受/拒绝逻辑；本任务只负责 execution truth 和停单理由事实源。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_quotes.py -q</automated>
  </verify>
  <done>market JSON 含独立 YES/NO `quote_snapshot`，后续策略 evaluator 不再读取 Gamma `bestAsk` / `bestBid` 作为执行真相。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLOB REST payload → quote helper | 外部 orderbook/tick size JSON 进入本地执行判断 |
| quote snapshot → strategy evaluator | 下游策略会把该快照当作挂单可执行事实源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T | token-level CLOB payload parsing in `bot_v2.py` | mitigate | 对 `bid/ask/tick_size/min_order_size` 做显式字段校验；字段缺失直接产出 `missing_quote_book` / `tick_size_missing` reason code。 |
| T-02-04 | D | quote polling inside `scan_and_update()` | mitigate | 把 quote helper 设计成单次 snapshot + side 参数化，避免在策略循环里重复请求；缺 quote 时停单而不是重试风暴。 |
| T-02-05 | I | market-side price inference | mitigate | 严禁用 Gamma `bestAsk` / `bestBid` 或 `1 - yes_price` 近似 NO 侧 quote；YES/NO 都必须走 token_id-specific helper。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase2_quotes.py -q`
- `uv run pytest tests/test_phase2_probability.py tests/test_phase2_quotes.py -q`
</verification>

<success_criteria>
- 每个 admissible market 都可生成 YES/NO 独立 `quote_snapshot`。
- quote 缺失或不可执行时会输出结构化 stop reason，而不是沉默降级。
- scan loop 不再把 Gamma market-level price 当作 execution truth。
</success_criteria>

<output>
After completion, create `.planning/phases/02-候选定价与双策略筛选/02-02-SUMMARY.md`
</output>
