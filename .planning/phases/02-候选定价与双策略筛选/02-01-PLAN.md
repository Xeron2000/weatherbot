---
phase: 02-候选定价与双策略筛选
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot_v2.py
  - tests/conftest.py
  - tests/fixtures/phase2_gamma_event.json
  - tests/fixtures/phase2_weather_snapshot.json
  - tests/test_phase2_probability.py
autonomous: true
requirements: [STRAT-01]
must_haves:
  truths:
    - "操作者能在每个 admissible 温区看到 per-source 与 aggregate band probability，而不是 matched bucket 一热编码。"
    - "同一 market 的所有 bucket 概率表都基于 Phase 1 的 market_contracts 全量生成。"
    - "scan loop 会把概率表持久化到 market JSON，供后续 YES/NO evaluator 直接消费。"
  artifacts:
    - path: bot_v2.py
      provides: "bucket probability helpers and persistence wiring"
      contains: "bucket_probabilities"
    - path: tests/test_phase2_probability.py
      provides: "STRAT-01 regression coverage"
      contains: "aggregate_probability"
    - path: tests/fixtures/phase2_gamma_event.json
      provides: "stable Phase 2 weather market fixture"
  key_links:
    - from: bot_v2.py
      to: tests/test_phase2_probability.py
      via: "probability helper contract"
      pattern: "aggregate_probability"
    - from: bot_v2.py
      to: data/markets/*.json
      via: "scan_and_update persistence"
      pattern: "bucket_probabilities"
---

<objective>
为 Phase 2 建立 band probability 真相源，并把每个温区的 per-source / aggregate probability 直接落进 market JSON。

Purpose: 先把概率真相从“点预测命中哪个 bucket”升级成“所有 bucket 的概率质量表”，后续 YES/NO 双策略才能共享同一概率基础。
Output: Phase 2 fixtures、概率回归测试、`bucket_probabilities` 持久化接线。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-候选定价与双策略筛选/02-RESEARCH.md
@.planning/phases/01-市场语义与扫描基线/01-02-SUMMARY.md
@bot_v2.py
@tests/conftest.py

<interfaces>
From bot_v2.py:
```python
def bucket_prob(forecast, t_low, t_high, sigma=None):
    ...

def get_sigma(city_slug, source="ecmwf"):
    ...

def new_market(city_slug, date_str, event, hours):
    return {
        "market_contracts": [],
        "scan_guardrails": {"admissible": False, "skip_reasons": []},
        "all_outcomes": [],
    }

def scan_and_update():
    ...
```

Current persisted outcome shape in `scan_and_update()`:
```python
{
    "question": contract["question"],
    "market_id": contract["market_id"],
    "range": contract["range"],
    "condition_id": contract["condition_id"],
    "token_id_yes": contract["token_id_yes"],
    "token_id_no": contract["token_id_no"],
    "bid": ...,
    "ask": ...,
    "price": ...,
    "spread": ...,
    "volume": ...,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 Phase 2 概率 fixtures 与 bucket probability 合同</name>
  <files>tests/conftest.py, tests/fixtures/phase2_gamma_event.json, tests/fixtures/phase2_weather_snapshot.json, tests/test_phase2_probability.py, bot_v2.py</files>
  <behavior>
    - Test 1: 中间 bucket 不再只返回 0/1；同一 forecast 对多个 bucket 会产生连续概率质量。
    - Test 2: per-source_probability 会分别记录 ECMWF / HRRR / METAR-anchor 的 bucket 概率。
    - Test 3: aggregate_probability 对同一 market 的全部 bucket 可比较、可持久化，并与 `fair_yes` / `fair_no` 同步输出。
  </behavior>
  <action>新增 Phase 2 fixture loader，并在 `tests/test_phase2_probability.py` 写出 STRAT-01 回归。随后在 `bot_v2.py` 中新增纯 helper（保持 helper 留在单文件脚本内）来从 Phase 1 的 `market_contracts` + weather snapshot 生成完整 `bucket_probabilities`。这里不要引入新包，也不要把概率逻辑拆到新模块；按 brownfield 约束直接扩展现有 math / scan helper。中间 bucket 必须用连续概率质量而不是当前 0/1 命中逻辑，且所有 bucket 都要产出 `per_source_probability`、`aggregate_probability`、`fair_yes`、`fair_no`。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_probability.py -q</automated>
  </verify>
  <done>`tests/test_phase2_probability.py` 绿灯，且 helper 能为同一 market 全量返回可持久化的 bucket probability records。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 bucket probability 表接入 scan loop 与 market JSON</name>
  <files>bot_v2.py, tests/test_phase2_probability.py</files>
  <behavior>
    - Test 1: admissible market 会持久化 `bucket_probabilities`，并与 `market_contracts` 一一对齐。
    - Test 2: skipped market 不会残留旧概率表；重新扫描后字段状态一致。
  </behavior>
  <action>在 `new_market()` 中加入 `bucket_probabilities` 默认字段，并在 `scan_and_update()` 中于 Phase 1 guardrail 通过后、策略筛选前生成并写入该字段。沿用 Phase 1 的 persist-before-trade 模式：先保存语义/概率事实，再进入后续候选逻辑。不要在此任务里加入 YES/NO evaluator、quote snapshot 或订单意图；本任务只负责 STRAT-01 的概率真相与持久化接线。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_probability.py -q</automated>
  </verify>
  <done>market JSON 对每个 admissible weather market 都包含可复用的 `bucket_probabilities`，后续计划无需重新推断 band probability。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| weather/Gamma payload → probability helper | 外部 forecast 与 market 文本进入本地 band probability 计算 |
| probability table → persisted market JSON | 策略后续直接信任该持久化结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `bucket_probabilities` generation in `bot_v2.py` | mitigate | 只基于通过 Phase 1 guardrail 的 `market_contracts` 生成概率表，缺 source 值时输出显式 `None`/reason 而不是 silently 伪造 0/1。 |
| T-02-02 | D | `scan_and_update()` probability loop | mitigate | 概率表只在 admissible market 上生成并落盘；skipped market 清空旧 `bucket_probabilities`，避免陈旧概率污染后续策略。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase2_probability.py -q`
- 如需 phase-level 抽查，再运行 `uv run pytest tests/test_phase1_market_semantics.py tests/test_phase1_scan_loop.py tests/test_phase2_probability.py -q`
</verification>

<success_criteria>
- 每个 admissible market 都有完整 `bucket_probabilities`。
- 每个 probability record 都含 `range`、`per_source_probability`、`aggregate_probability`、`fair_yes`、`fair_no`。
- 中间 bucket 概率不再依赖 matched-bucket 一热逻辑。
</success_criteria>

<output>
After completion, create `.planning/phases/02-候选定价与双策略筛选/02-01-SUMMARY.md`
</output>
