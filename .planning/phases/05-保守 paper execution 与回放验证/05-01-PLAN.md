---
phase: 05-保守 paper execution 与回放验证
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - config.json
  - bot_v2.py
  - tests/test_phase5_paper_execution.py
autonomous: true
requirements:
  - SIM-01
  - SIM-02
must_haves:
  truths:
    - "paper 模式的执行假设来自显式配置，而不是散落在 `sync_market_order()` 里的硬编码。"
    - "同一笔 passive order 可以被保守地判定为延迟提交、排队中、touch-not-fill、partial fill 或 cancel pending。"
    - "后续 wiring 层可以直接复用统一的 execution event / paper state 合同，而不必重新发明字段。"
  artifacts:
    - path: "config.json"
      provides: "paper_execution 顶层配置块"
      contains: "paper_execution"
    - path: "bot_v2.py"
      provides: "paper execution pure helpers 与默认 schema"
      contains: "load_paper_execution_config"
    - path: "tests/test_phase5_paper_execution.py"
      provides: "paper execution RED→GREEN 合同"
      contains: "test_"
  key_links:
    - from: "config.json"
      to: "bot_v2.py"
      via: "load_paper_execution_config()"
      pattern: "paper_execution"
    - from: "bot_v2.py"
      to: "tests/test_phase5_paper_execution.py"
      via: "pure helper contracts"
      pattern: "simulate_paper_execution_step|record_execution_event"
---

<objective>
把 Phase 5 的保守 paper execution 合同先钉死：显式配置执行假设、定义 append-only execution event schema、提供可测试的纯函数 helper。

Purpose: 先把仿真真相源做成稳定合同，避免后续把延迟、排队、touch-not-fill、cancel delay 再次写死进 scan loop。
Output: `paper_execution` 配置、execution event / paper state schema、纯函数 helper、对应 pytest 回归。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-被动挂单与订单恢复/04-02-SUMMARY.md
@.planning/phases/04-被动挂单与订单恢复/04-03-SUMMARY.md
@.planning/research/SUMMARY.md
@config.json
@bot_v2.py
@tests/test_phase4_orders.py
@tests/test_phase4_scan_loop.py

<interfaces>
From `bot_v2.py`:
```python
def build_passive_order_intent(market, reservation, assessment, quote_snapshot, now_ts):
    return {"order": order_or_none, "reason": reason_or_none}

def apply_order_transition(order, next_status, reason, ts, fill_shares=0.0, fill_price=None, patch=None):
    ...

def sync_market_order(market, risk_state, forecast_snap, market_ready=True):
    return {"filled_cost": 0.0, "opened_position": False}
```

Existing market persistence shape in `bot_v2.py`:
```python
{
  "candidate_assessments": [],
  "reserved_exposure": None,
  "active_order": None,
  "order_history": [],
  "quote_snapshot": []
}
```

Use the existing brownfield rule: helpers stay inside `bot_v2.py`, persisted facts stay in market JSON / state JSON, no package refactor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 paper execution RED 合同与事件 schema</name>
  <files>tests/test_phase5_paper_execution.py</files>
  <behavior>
    - Test 1: submission latency 未到前，accepted passive order 只能进入 queued/submitting paper state，不能直接成交。
    - Test 2: quote 触碰限价但 queue threshold 未满足时，记录 `touch_not_fill` 事件并保持未成交。
    - Test 3: queue 消化后允许 partial fill，再继续推进到 filled；每一步都写 append-only execution events。
    - Test 4: cancel request 进入 pending cancel，只有 cancel latency 结束后才写 terminal cancel event。
  </behavior>
  <action>新增 `tests/test_phase5_paper_execution.py`，用 fixture-style market/order dict 直接覆盖 Phase 5 纯函数合同。测试必须锁定保守、deterministic 的 execution event schema：每条 event 至少包含 `event_type`、`ts`、`order_id`、`status_before`、`status_after`、`reason`、`simulated_fill_shares`、`queue_ahead_shares`、`latency_ms`。不要写依赖 live API 的测试；继续沿用 pytest + monkeypatch 模式。</action>
  <verify>
    <automated>uv run pytest tests/test_phase5_paper_execution.py -q</automated>
  </verify>
  <done>存在失败中的 RED 测试，且每个保守 execution 行为都有明确断言与稳定字段合同。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 paper execution 配置、默认字段与纯函数 helper</name>
  <files>config.json, bot_v2.py, tests/test_phase5_paper_execution.py</files>
  <behavior>
    - Test 1: `load_paper_execution_config()` 返回完整阈值，不允许静默缺关键字段。
    - Test 2: helper 会生成 append-only execution events 与可恢复 paper state。
    - Test 3: helpers 只基于 persisted quote/order facts 计算，不重拉外部数据。
  </behavior>
  <action>在 `config.json` 增加顶层 `paper_execution` 块，至少覆盖 submission latency、queue ahead shares/ratio、touch-not-fill 最少触碰次数、partial fill slice ratio、cancel latency、adverse fill buffer 等保守参数。然后在 `bot_v2.py` 中实现 `load_paper_execution_config()`、market/order 默认字段补齐 helper、execution event recorder、以及单步 `simulate_paper_execution_step()` 之类的纯函数合同；同时把 `new_market()` / `load_market()` 默认 schema 扩展到 `paper_execution_state`、`execution_events`、`execution_metrics`。严格遵守现有决策：继续把 helper 留在 `bot_v2.py`；真相源保存在 JSON；不引入新依赖；v1 只做 paper，不接真实下单边界。</action>
  <verify>
    <automated>uv run pytest tests/test_phase5_paper_execution.py -q</automated>
  </verify>
  <done>`config.json` 和 `bot_v2.py` 已提供可复用的 Phase 5 合同，所有 RED 测试转绿，后续 scan/monitor/replay 都能直接消费同一套 execution schema。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → paper engine | 本地配置值进入仿真逻辑，错误参数会系统性高估成交 |
| quote_snapshot/order facts → execution helper | 未验证的持久化市场事实进入成交仿真 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `load_paper_execution_config()` | mitigate | 启动时显式校验关键字段存在且为正数/合法区间，缺失时返回 deterministic reason，不静默回退乐观默认值。 |
| T-05-02 | I | execution event schema | mitigate | 统一 event 字段，保留 queue/latency/fill 证据，避免 replay 时因字段缺失无法审计。 |
| T-05-03 | R | pure helper outputs | mitigate | pytest 锁定 event append-only 行为与 status transition，避免后续 wiring 层伪造或覆盖历史事件。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase5_paper_execution.py -q`
</verification>

<success_criteria>
- `paper_execution` 配置块存在且覆盖 Phase 5 关键保守参数。
- `bot_v2.py` 提供纯函数级 paper execution helper 和默认 schema。
- execution events 成为 append-only 真相源，可供后续 wiring 和 replay 直接读取。
</success_criteria>

<output>
After completion, create `.planning/phases/05-保守 paper execution 与回放验证/05-01-SUMMARY.md`
</output>
