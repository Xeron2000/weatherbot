---
phase: 05-保守 paper execution 与回放验证
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - bot_v2.py
  - tests/test_phase5_scan_loop.py
autonomous: true
requirements:
  - SIM-01
  - SIM-02
must_haves:
  truths:
    - "运行 `python bot_v2.py` 时，订单只会经过 paper execution engine 推进，不会再把 `ask<=limit` 直接视为成交。"
    - "未完成订单在 full scan 与 monitor 分支里都能继续经历 latency、queue、partial fill、touch-not-fill、cancel delay。"
    - "成交、未成交、撤单延迟和 adverse fill 结果会被持久化，重启后仍可继续回放。"
  artifacts:
    - path: "bot_v2.py"
      provides: "scan/monitor wiring 与 state persistence"
      contains: "sync_market_order"
    - path: "tests/test_phase5_scan_loop.py"
      provides: "Phase 5 集成回归"
      contains: "test_scan_and_update"
  key_links:
    - from: "sync_market_order()"
      to: "market['execution_events']"
      via: "paper execution step persistence"
      pattern: "execution_events"
    - from: "monitor_active_orders()"
      to: "paper_execution_state"
      via: "resume unfinished order simulation"
      pattern: "monitor_active_orders"
---

<objective>
把 Phase 5 的保守 paper execution 真正接入运行时：scan loop 和 monitor 分支都通过统一的仿真引擎推进订单，而不是“触价即成交”。

Purpose: 把 SIM-01 / SIM-02 从 helper 合同落到真实工作流，确保完整候选→挂单→paper fill→持仓链路成立且保守。
Output: `sync_market_order()` / `monitor_active_orders()` / 状态持久化已接入 paper execution engine，新增 Phase 5 集成测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-保守 paper execution 与回放验证/05-01-PLAN.md
@.planning/phases/04-被动挂单与订单恢复/04-02-SUMMARY.md
@.planning/phases/04-被动挂单与订单恢复/04-03-SUMMARY.md
@bot_v2.py
@tests/test_phase4_scan_loop.py
@tests/test_phase4_restore.py

<interfaces>
From `bot_v2.py` today:
```python
def sync_market_order(market, risk_state, forecast_snap, market_ready=True):
    ...
    if ask <= limit_price:
        # currently opens partial/filled immediately
```

```python
def monitor_active_orders():
    refresh_active_order_quotes(mkt)
    update = sync_market_order(mkt, state["risk_state"], forecast_snap, market_ready=True)
```

New contracts from Plan 05-01 must become the only path for:
- submission latency
- queue/touch-not-fill logic
- partial fill slices
- cancel latency / pending cancel
- execution event persistence
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为保守 paper scan/monitor wiring 建立集成回归</name>
  <files>tests/test_phase5_scan_loop.py</files>
  <behavior>
    - Test 1: full scan 创建 working order 后，首次触价只记录 queued/touch-not-fill，不直接开仓。
    - Test 2: 多轮 monitor/scan 推进后，订单经历 partial fill -> filled，并把 position 成本建立在 simulated fill prices 上。
    - Test 3: candidate downgrade 或 market_no_longer_ready 时，订单先进入 cancel_pending，再在 cancel latency 满足后终结并释放 reservation。
    - Test 4: 重启恢复的 unfinished order 会继续沿用现有 `paper_execution_state` / `execution_events`，不会重置排队进度。
  </behavior>
  <action>新增 `tests/test_phase5_scan_loop.py`，用 monkeypatch 固定 quote snapshot / assessment / route facts，覆盖 `scan_and_update()` 与 `monitor_active_orders()` 的完整链路。重点锁定保守行为：per D-项目决定，v1 仍然只做 paper execution，不允许任何“实时 API 下单”分支；per Phase 04 persisted-facts 决定，仿真只能读取 `candidate_assessments`、`route_decisions`、`reserved_exposure`、`quote_snapshot`、`active_order` 和 Phase 5 新增 execution facts，不在 wiring 层重算候选。</action>
  <verify>
    <automated>uv run pytest tests/test_phase5_scan_loop.py -q</automated>
  </verify>
  <done>存在能复现保守 paper execution 生命周期的失败集成测试，并明确覆盖 latency、queue、partial、touch-not-fill、cancel delay、restart resume。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 paper execution engine 接入 scan loop、monitor 与持久化账本</name>
  <files>bot_v2.py, tests/test_phase5_scan_loop.py</files>
  <behavior>
    - Test 1: `sync_market_order()` 改为消费 paper helper 输出，而不是直接 `ask<=limit` 成交。
    - Test 2: `monitor_active_orders()` 可以推进 queued/partial/cancel_pending 订单。
    - Test 3: `execution_events`、`paper_execution_state`、`execution_metrics` 会稳定落盘并在 `load_state()` / restart 后继续有效。
  </behavior>
  <action>在 `bot_v2.py` 中重写 `sync_market_order()` 的 fill/cancel 分支：accepted passive order 先进入 paper engine，基于 quote snapshot、paper policy 和 persisted paper state 决定 queued、touch_not_fill、partial、filled、cancel_pending、canceled、expired。把 simulated fill prices / latency / queue evidence 持久化到 `execution_events` 和聚合 `execution_metrics`；filled position 的 entry price 必须来自 simulated fills，而不是 quote snapshot 的即时 ask。同步更新 `monitor_active_orders()`、`load_state()` 恢复逻辑与相关默认字段，确保重启后继续推进已有 paper state。不要添加 live trading adapter、不要改 Phase 4 的 order truth taxonomy、不要把 execution events 写成覆盖式快照。</action>
  <verify>
    <automated>uv run pytest tests/test_phase5_paper_execution.py tests/test_phase5_scan_loop.py -q</automated>
  </verify>
  <done>bot 运行时已经显式区分“触价”和“成交”，所有 paper lifecycle 证据都持久化且可恢复，SIM-01/SIM-02 的完整链路成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted market facts → runtime engine | 本地 market JSON 被重新加载并驱动资金/订单状态变化 |
| paper engine → balance/position state | 仿真结果会改变余额、持仓和 reservation 释放 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | `sync_market_order()` | mitigate | 删除直接 `ask<=limit` 即成交路径，统一走 paper engine，避免乐观成交绕过保守模型。 |
| T-05-05 | D | `monitor_active_orders()` | mitigate | unfinished order resume 必须读取 persisted paper state，避免 monitor 分支把 queued/cancel_pending 卡死或重复创建订单。 |
| T-05-06 | R | `execution_events` / `execution_metrics` | mitigate | append-only 记录 submit/touch/partial/fill/cancel 事件，并用 pytest 锁定 restart 后事件不丢失。 |
| T-05-07 | T | balance / reservation release | mitigate | 只有 terminal cancel/filled/expired 才允许更新余额或释放 reservation，避免 pending cancel 过早归零风险占用。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase5_paper_execution.py tests/test_phase5_scan_loop.py -q`
</verification>

<success_criteria>
- `sync_market_order()` 与 `monitor_active_orders()` 都通过 paper engine 推进订单。
- `execution_events` / `paper_execution_state` / `execution_metrics` 已成为 persisted facts，并在重启后继续有效。
- candidate→order→paper fill→position 链路在不发送真实订单的前提下可以完整运行。
</success_criteria>

<output>
After completion, create `.planning/phases/05-保守 paper execution 与回放验证/05-02-SUMMARY.md`
</output>
