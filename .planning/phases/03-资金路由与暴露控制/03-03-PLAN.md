---
phase: 03-资金路由与暴露控制
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - bot_v2.py
  - tests/test_phase3_scan_loop.py
autonomous: true
requirements: [RISK-02, RISK-01]
must_haves:
  truths:
    - "已有 reservation 在候选失效、冲突或消失时会立刻释放。"
    - "同 event 高相关 bucket 与同 bucket 反向腿冲突都会拒绝新候选。"
    - "释放会写入 release_reason，不会静默消失。"
  artifacts:
    - path: bot_v2.py
      provides: "reservation reconciliation and conflict release"
      contains: "release_reason"
    - path: tests/test_phase3_scan_loop.py
      provides: "release/conflict regressions"
      contains: "reserved_exposure"
  key_links:
    - from: prior market reservation
      to: refreshed candidate set
      via: "reconcile_market_reservation"
      pattern: "release_reason"
    - from: event exposure ledger
      to: route rejection
      via: "event_cluster_conflict"
      pattern: "event_cluster_conflict"
---

<objective>
补齐 reservation 的释放与冲突回收逻辑，确保 Phase 3 风控账本在连续扫描下保持一致。

Purpose: D-21~D-23 与 RISK-02 的关键不在“会拒绝一次”，而在重复扫描后不会把失效占用留在账上污染后续预算。
Output: reservation reconciliation 逻辑、release/conflict 集成回归。

Decision coverage: 本计划直接落地 D-11、D-12、D-13、D-14 与 D-21、D-22、D-23，确保相关簇冲突、保留已有暴露、候选失效即释放。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-资金路由与暴露控制/03-CONTEXT.md
@.planning/phases/03-资金路由与暴露控制/03-02-PLAN.md
@bot_v2.py
@tests/test_phase3_scan_loop.py

<interfaces>
State after Plan 02 should expose:
```python
state["risk_state"] = {
    "global_reserved_worst_loss": 0.0,
    "legs": {"YES_SNIPER": {...}, "NO_CARRY": {...}},
    "event_exposure": {}
}

mkt["route_decisions"] = [...]
mkt["reserved_exposure"] = {
    "strategy_leg": "YES_SNIPER",
    "reserved_worst_loss": 12.5,
    "release_reason": None,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为释放与冲突回收建立连续扫描回归</name>
  <files>tests/test_phase3_scan_loop.py</files>
  <read_first>tests/test_phase3_scan_loop.py, bot_v2.py, .planning/phases/03-资金路由与暴露控制/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: 第二轮扫描里若原候选从 accepted 降级为 rejected/reprice，旧 `reserved_exposure` 会立即释放并写 `release_reason`。
    - Test 2: 若同 bucket 反向腿出现冲突，保留已有 reservation，新候选在 `route_decisions[]` 里被 rejected。
    - Test 3: 若候选从 market JSON 中消失（例如 bucket 不再存在或 market 不 admissible），state `risk_state` 对应 exposure 会回收。
  </behavior>
  <action>扩展 `tests/test_phase3_scan_loop.py` 为两轮连续扫描场景：第一轮接受候选并占用 reservation，第二轮通过 monkeypatch 修改 `candidate_assessments` / admissibility / quote 状态，验证 release 发生。测试必须显式覆盖 D-11~D-14：同一 city+date event 的 bucket 作为同相关簇、同 bucket YES/NO 不得同时存在、新冲突候选默认拒绝并保留已有 reservation。测试中显式断言 `release_reason` 字符串至少覆盖：`candidate_downgraded`、`candidate_missing`、`market_no_longer_ready`，以及冲突拒绝 reason `same_bucket_conflict` / `event_cluster_conflict`。</action>
  <acceptance_criteria>
    - `tests/test_phase3_scan_loop.py` contains `candidate_downgraded` and `candidate_missing`
    - `uv run pytest tests/test_phase3_scan_loop.py -q -k "release or conflict"` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_scan_loop.py -q -k "release or conflict"</automated>
  </verify>
  <done>释放生命周期已被连续扫描回归锁定，后续实现不能只会加占用不会回收。</done>
</task>

<task type="auto">
  <name>Task 2: 在 scan loop 中实现 reservation reconciliation</name>
  <files>bot_v2.py, tests/test_phase3_scan_loop.py</files>
  <read_first>bot_v2.py, tests/test_phase3_scan_loop.py, .planning/phases/02-候选定价与双策略筛选/02-04-SUMMARY.md</read_first>
  <action>在 `bot_v2.py` 增加 reconciliation helper（名称可用 `reconcile_market_reservation` / `release_reserved_exposure`），并在每轮 market 刷新时先处理旧 reservation，再重新运行 routing。具体要求：1) 如果 market 从 ready 变为 skipped，立刻释放该 market 的 `reserved_exposure` 并写 `release_reason = "market_no_longer_ready"`; 2) 如果先前被保留的 strategy_leg 在本轮 assessment 中不再是 route accepted，释放并写 `candidate_downgraded` 或 `candidate_missing`; 3) 若新候选与已存在 reservation 冲突，保留旧 reservation，一律拒绝新候选，不做替换、不做净额化； 4) 同一 event 下所有 bucket 默认视为同相关簇，按 D-11 的最坏路径聚合，不做净额抵消； 5) `risk_state` 的 leg/global/city/date/event exposure 在 release 后必须同步回写，不能留脏值。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains `candidate_downgraded`, `candidate_missing`, and `market_no_longer_ready`
    - `bot_v2.py` contains a helper named `release_reserved_exposure` or `reconcile_market_reservation`
    - `uv run pytest tests/test_phase3_router.py tests/test_phase3_scan_loop.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_router.py tests/test_phase3_scan_loop.py -q</automated>
  </verify>
  <done>连续扫描下的 reservation 会正确释放/保留，Phase 3 风控账本保持一致，不会因为候选失效而永久占用预算。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| previous scan reservation → current scan routing | 旧 reservation 可能污染新一轮预算判断 |
| conflict detection → release logic | 冲突处理错误会造成重复暴露或错误释放 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | D | stale reservations in `risk_state` | mitigate | 每轮 scan 先 reconcile 再 reroute；测试覆盖 market skipped、candidate missing、candidate downgraded 三类释放入口。 |
| T-03-08 | T | conflict handling between existing and new routes | mitigate | 冲突处理严格遵循“保留已有暴露，拒绝新候选”；禁止替换和净额化。 |
| T-03-09 | R | release traceability | mitigate | market JSON 必须保留 `release_reason`，确保预算变化有据可查。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase3_scan_loop.py -q -k "release or conflict"`
- `uv run pytest tests/test_phase3_router.py tests/test_phase3_scan_loop.py -q`
</verification>

<success_criteria>
- 失效候选与 skipped market 都会释放 reservation。
- 同 bucket / 同 event 冲突默认保留已有暴露。
- 所有释放都有 deterministic `release_reason`。
</success_criteria>

<output>
After completion, create `.planning/phases/03-资金路由与暴露控制/03-03-SUMMARY.md`
</output>
