---
phase: 03-资金路由与暴露控制
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - bot_v2.py
  - tests/test_phase3_reporting.py
  - README.md
autonomous: true
requirements: [STRAT-04, RISK-01, RISK-02]
must_haves:
  truths:
    - "操作者可以在 status/report 中直接看到 YES/NO 预算占用与全局 worst-loss 使用率。"
    - "操作者可以看到 route reject / release reasons，而不是只知道候选没进单。"
    - "README 有 Phase 3 回归命令与新风险字段说明。"
  artifacts:
    - path: bot_v2.py
      provides: "operator-facing risk reporting"
      contains: "risk_state"
    - path: tests/test_phase3_reporting.py
      provides: "Phase 3 reporting regressions"
      contains: "release_reason"
    - path: README.md
      provides: "Phase 3 verification guidance"
      contains: "risk_state"
  key_links:
    - from: state.json
      to: status/report output
      via: "risk usage summary"
      pattern: "YES_SNIPER|NO_CARRY|global_reserved_worst_loss"
    - from: market JSON route_decisions
      to: terminal output
      via: "reject/release explanation summary"
      pattern: "release_reason|same_bucket_conflict"
---

<objective>
把 Phase 3 的风控真相源变成操作者可见输出，并补齐 README 的本地验证说明。

Purpose: Phase 3 的完成标准不是“账本里多了几个字段”，而是操作者能据此理解预算占用、冲突拒绝和释放原因。
Output: Phase 3 reporting 回归、status/report 风险摘要、README 更新。

Decision coverage: 本计划把 D-07、D-18、D-21~D-23 的 operator-visible 结果直接暴露出来，完成 success criteria 中“能看到每条腿当前占用风险额度”。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-资金路由与暴露控制/03-CONTEXT.md
@.planning/phases/02-候选定价与双策略筛选/02-04-SUMMARY.md
@bot_v2.py
@README.md

<interfaces>
Expected persisted truths after Plan 03:
```python
state["risk_state"]["legs"]["YES_SNIPER"]
state["risk_state"]["legs"]["NO_CARRY"]
mkt["route_decisions"]
mkt["reserved_exposure"]["release_reason"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 Phase 3 风险摘要建立 operator-facing reporting 合同</name>
  <files>tests/test_phase3_reporting.py, bot_v2.py</files>
  <read_first>bot_v2.py, tests/test_phase2_reporting.py, .planning/phases/03-资金路由与暴露控制/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `print_status()` 显示 YES/NO leg 的 budget、reserved、available 与全局 worst-loss 使用率。
    - Test 2: `print_report()` 显示 city/date/event exposure 汇总，以及本轮 route rejected / released 的 reason summaries。
    - Test 3: 即使还没有真实 position，也能先看到 Phase 3 risk summary。
  </behavior>
  <action>新增 `tests/test_phase3_reporting.py`，沿用 Phase 2 reporting smoke-test 风格，直接断言 operator-facing 字符串。随后扩展 `print_scan_summary()`、`print_status()`、`print_report()`：1) 新增 risk summary 段，输出 `YES_SNIPER` / `NO_CARRY` 的 budget/reserved/available；2) 输出 `global_reserved_worst_loss` 与使用率；3) 输出 city/date/event exposure top lines；4) 输出 route 决策摘要，显示 accepted/rejected/released 计数及常见 reason code（至少 `same_bucket_conflict`、`event_cluster_conflict`、`candidate_downgraded`、`candidate_missing`）。不要在展示层重算暴露，只读取持久化的 `risk_state` 与 `route_decisions`。</action>
  <acceptance_criteria>
    - `tests/test_phase3_reporting.py` contains `YES_SNIPER`, `NO_CARRY`, and `candidate_downgraded`
    - `bot_v2.py` contains `global_reserved_worst_loss` inside reporting functions
    - `uv run pytest tests/test_phase3_reporting.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_reporting.py -q</automated>
  </verify>
  <done>status/report 在没有真实下单的前提下也能展示 Phase 3 预算、暴露和拒绝/释放解释。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 README 的 Phase 3 风控字段与验证命令</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/03-资金路由与暴露控制/03-RESEARCH.md, .planning/phases/03-资金路由与暴露控制/03-VALIDATION.md</read_first>
  <action>在 README 新增 “Phase 3 Verification” 小节，写入完整回归命令 `uv run pytest tests/test_phase2_probability.py tests/test_phase2_quotes.py tests/test_phase2_strategies.py tests/test_phase2_reporting.py tests/test_phase3_router.py tests/test_phase3_scan_loop.py tests/test_phase3_reporting.py -q`。同时在 Data Storage 说明中补充 `risk_state`、`route_decisions`、`reserved_exposure` 三个新事实源，明确它们分别对应预算总账、单 market 路由审计、释放原因。只写与 Phase 3 直接相关的内容，不扩展到订单生命周期或真钱执行。</action>
  <acceptance_criteria>
    - `README.md` contains `## Phase 3 Verification`
    - `README.md` contains `risk_state`, `route_decisions`, and `reserved_exposure`
    - `uv run pytest tests/test_phase3_reporting.py -q` still exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_reporting.py -q</automated>
  </verify>
  <done>README 能让操作者知道 Phase 3 风控账本存在哪里、怎么跑回归、status/report 应看到什么。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted risk facts → operator terminal output | 操作者将根据 CLI 输出判断资金是否还能继续分配 |
| README guidance → manual verification | 文档会影响操作者是否能正确运行 Phase 3 回归 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | R | `print_status()` / `print_report()` risk output | mitigate | 输出直接读取 `risk_state` 与 `route_decisions` 持久化字段，reporting tests 断言关键 budget/reason strings 存在。 |
| T-03-11 | I | README Phase 3 guidance | accept | README 只记录本地验证命令和字段说明，不涉及 secrets 或真实交易凭据，泄露风险低。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase3_reporting.py -q`
- `uv run pytest tests/test_phase2_probability.py tests/test_phase2_quotes.py tests/test_phase2_strategies.py tests/test_phase2_reporting.py tests/test_phase3_router.py tests/test_phase3_scan_loop.py tests/test_phase3_reporting.py -q`
</verification>

<success_criteria>
- CLI 已能直接显示 per-leg budget usage 与 global risk usage。
- rejected / released route reasons 对操作者可见。
- README 包含 Phase 3 回归与数据字段说明。
</success_criteria>

<output>
After completion, create `.planning/phases/03-资金路由与暴露控制/03-04-SUMMARY.md`
</output>
