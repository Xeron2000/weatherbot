---
phase: 03-资金路由与暴露控制
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.json
  - bot_v2.py
  - tests/test_phase3_router.py
autonomous: true
requirements: [STRAT-04, RISK-01, RISK-02]
must_haves:
  truths:
    - "操作者可为 YES_SNIPER 与 NO_CARRY 配置独立预算与硬 cap。"
    - "router 会按最坏损失而不是名义成本做预算占用和 cap 判断。"
    - "同 bucket YES/NO 冲突、同 event 最坏路径冲突、edge/liquidity 排序都有可测试的纯函数合同。"
  artifacts:
    - path: config.json
      provides: "risk_router conservative template"
      contains: "risk_router"
    - path: bot_v2.py
      provides: "pure routing/exposure helpers"
      contains: "route_candidate_assessment"
    - path: tests/test_phase3_router.py
      provides: "router contract coverage"
      contains: "test_route_candidate_assessment"
  key_links:
    - from: config.json
      to: bot_v2.py
      via: "RISK_ROUTER config loader"
      pattern: "risk_router|RISK_ROUTER"
    - from: bot_v2.py
      to: tests/test_phase3_router.py
      via: "router helper contracts"
      pattern: "same_bucket_conflict|worst_loss"
---

<objective>
先定义 Phase 3 的风控合同：risk router 配置、最坏损失口径、冲突键与腿内排序 helper。

Purpose: 后续 scan loop 接线必须建立在稳定的纯函数合同上，避免把风险规则埋进大段过程逻辑里难以验证。
Output: `risk_router` 配置块、router helper、Phase 3 单元回归测试。

Decision coverage: 本计划直接落地 D-01、D-02、D-03、D-04、D-05、D-06、D-07、D-08、D-09、D-10（独立资金池、百分比预算、每轮刷新、禁止跨腿借预算、worst-loss 口径、多层 cap、保守模板）以及 D-16、D-17、D-18、D-19 的腿内排序/拒绝理由合同。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-资金路由与暴露控制/03-CONTEXT.md
@.planning/phases/03-资金路由与暴露控制/03-RESEARCH.md
@config.json
@bot_v2.py

<interfaces>
Current strategy config already available:
```json
{
  "yes_strategy": {"max_price": 0.25, "min_probability": 0.14, "min_edge": 0.03, "max_size": 20.0},
  "no_strategy": {"min_price": 0.65, "min_probability": 0.70, "min_edge": 0.04, "max_size": 20.0}
}
```

Current candidate truth source:
```python
{
    "strategy_leg": "YES_SNIPER" | "NO_CARRY",
    "token_side": "yes" | "no",
    "range": (65.0, 69.0),
    "status": "accepted" | "rejected" | "size_down" | "reprice",
    "edge": 0.05,
    "quote_context": {"bid": 0.09, "ask": 0.11, "spread": 0.02},
    "reasons": []
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 risk router 配置与纯函数测试合同</name>
  <files>tests/test_phase3_router.py, config.json</files>
  <read_first>config.json, bot_v2.py, tests/test_phase2_strategies.py, .planning/phases/03-资金路由与暴露控制/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: YES/NO 预算按总 bankroll 百分比独立计算，YES 默认 30%、NO 默认 70%。
    - Test 2: 单市场、单城市、单日期、单 event、单腿、全局 cap 任一命中都会返回 rejected，并保留精确 reason code。
    - Test 3: 同 bucket 已有 YES 暴露时，新 NO 候选会被 `same_bucket_conflict` 拒绝；同 event 最坏路径冲突会被 `event_cluster_conflict` 拒绝。
    - Test 4: 同腿候选按 `edge` 降序，edge 相同时按 quote liquidity 降序。
  </behavior>
  <action>新增 `tests/test_phase3_router.py`，建立以下 helper 合同：`load_risk_router_config(config_dict)`、`candidate_worst_loss(assessment, bankroll)`、`build_exposure_keys(market)`、`route_candidate_assessment(assessment, market, risk_state, router_cfg)`、`sort_leg_candidates(assessments)`。同时在 `config.json` 新增顶层 `risk_router` 配置块，至少包含这些具体键：`yes_budget_pct: 0.30`、`no_budget_pct: 0.70`、`yes_leg_cap_pct: 0.30`、`no_leg_cap_pct: 0.70`、`global_usage_cap_pct: 0.85`、`per_market_cap_pct: 0.08`、`per_city_cap_pct: 0.18`、`per_date_cap_pct: 0.18`、`per_event_cap_pct: 0.18`。测试必须显式覆盖 D-01~D-10：独立资金池、百分比预算、每轮刷新、禁止跨腿借预算、保守模板、worst-loss 记账、多层 cap 任一命中即拒绝。测试里显式断言 reason code 字符串：`leg_budget_exceeded`、`leg_cap_exceeded`、`global_cap_exceeded`、`market_cap_exceeded`、`city_cap_exceeded`、`date_cap_exceeded`、`event_cap_exceeded`、`same_bucket_conflict`、`event_cluster_conflict`。</action>
  <acceptance_criteria>
    - `config.json` contains `"risk_router"` and `"yes_budget_pct": 0.3`
    - `tests/test_phase3_router.py` contains `same_bucket_conflict` and `event_cluster_conflict`
    - `uv run pytest tests/test_phase3_router.py -q` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_router.py -q</automated>
  </verify>
  <done>`risk_router` 配置与 router reason-code 合同都被测试锁定，执行者不需要在 scan loop 阶段再发明口径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 bot_v2.py 实现最坏损失路由 helper</name>
  <files>bot_v2.py, tests/test_phase3_router.py, config.json</files>
  <read_first>bot_v2.py, config.json, tests/test_phase3_router.py, .planning/phases/02-候选定价与双策略筛选/02-03-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `candidate_worst_loss()` 对 YES/NO 都返回按最坏损失记账的美元值，不回退到 entry cost 或 size_multiplier。
    - Test 2: `route_candidate_assessment()` 只接受 `candidate_assessments.status in {accepted,size_down,reprice,rejected}` 的输入，并把最终 route status 归一为 `accepted` 或 `rejected`。
    - Test 3: `sort_leg_candidates()` 先按 edge，再按 quote liquidity（`bid_size`/`ask_size` 或 size 总和）排序。
  </behavior>
  <action>在 `bot_v2.py` 的 candidate helper 区域后新增 Phase 3 纯函数：`RISK_ROUTER = load_risk_router_config(_cfg)`、`candidate_worst_loss(...)`、`normalize_route_reason_codes(...)`、`build_exposure_keys(city_slug, date_str, assessment)`、`route_candidate_assessment(...)`、`sort_leg_candidates(...)`。实现时使用固定的 legs：`YES_SNIPER` 与 `NO_CARRY`；所有预算和 cap 都基于 `bankroll = state["starting_balance"]` 或显式传入 bankroll 计算，不用当前 `balance` 浮动值替代。`candidate_worst_loss` 直接按 `min(strategy max_size, strategy min_size/size rules after size_multiplier)` 产出 worst-loss 美元占用。该实现必须覆盖 D-16~D-19：同腿先按 edge 排序、edge 接近时按流动性排序、reject reason 持久化 contract、且不做跨腿重排。不要在本任务里修改 `scan_and_update()` 或持久化结构。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains `RISK_ROUTER = load_risk_router_config(_cfg)`
    - `bot_v2.py` contains `def route_candidate_assessment(` and `def candidate_worst_loss(`
    - `bot_v2.py` contains the literal reason codes `same_bucket_conflict` and `global_cap_exceeded`
    - `uv run pytest tests/test_phase3_router.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_router.py -q</automated>
  </verify>
  <done>`bot_v2.py` 具备独立、可测试的资金路由 helper，后续 wiring 只需消费这些合同。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → router helpers | 配置直接决定预算、hard cap 与拒绝边界 |
| candidate assessments → route decisions | Phase 2 truth 将被升级为 Phase 3 风控真相 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `config.json` risk_router | mitigate | 显式解析 `risk_router` 必需字段与默认 conservative values；测试断言缺字段或异常值会返回 deterministic reason code，而不是静默借预算。 |
| T-03-02 | E | `route_candidate_assessment()` | mitigate | 同 bucket YES/NO 冲突和同 event 最坏路径冲突都在纯函数层被拒绝，避免后续 scan wiring 绕过风控。 |
| T-03-03 | R | router reason codes | mitigate | reason code 使用固定字符串并由 pytest 锁定，保证后续报告与订单层可追溯。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase3_router.py -q`
</verification>

<success_criteria>
- 风控配置块与预算/硬 cap helper 已落地。
- YES/NO、同 bucket、同 event 冲突口径都有自动化回归。
- 还未接线 scan loop，但后续 plan 可直接复用这些纯函数合同。
</success_criteria>

<output>
After completion, create `.planning/phases/03-资金路由与暴露控制/03-01-SUMMARY.md`
</output>
