---
phase: 03-资金路由与暴露控制
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - bot_v2.py
  - tests/test_phase3_scan_loop.py
autonomous: true
requirements: [STRAT-04, RISK-01]
must_haves:
  truths:
    - "scan loop 会在 candidate_assessments 之后、旧开仓逻辑之前完成 per-leg routing。"
    - "state.json 会显示 YES/NO 预算与全局已占用 worst-loss。"
    - "market JSON 会记录 route_decisions 与当前 reserved_exposure。"
  artifacts:
    - path: bot_v2.py
      provides: "scan loop routing + state ledger persistence"
      contains: "route_decisions"
    - path: tests/test_phase3_scan_loop.py
      provides: "scan loop routing regressions"
      contains: "risk_state"
  key_links:
    - from: candidate_assessments
      to: route_decisions
      via: "scan_and_update"
      pattern: "route_decisions"
    - from: state.json
      to: print_status/print_report
      via: "risk_state ledger"
      pattern: "global_reserved_worst_loss"
---

<objective>
把 router helper 接入 scan loop，并把 per-leg / global worst-loss ledger 持久化到 state 与 market JSON。

Purpose: 满足 STRAT-04 与 RISK-01 的核心结果——机器人不仅会评估候选，还会真正为每条腿分配预算并阻止超额占用。
Output: `risk_state`、`route_decisions[]`、`reserved_exposure`、scan loop 集成回归测试。

Decision coverage: 本计划直接落地 D-03、D-06~D-10、D-15、D-18、D-20、D-22，把 router seam 插入 scan loop 并持久化资金占用。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-资金路由与暴露控制/03-CONTEXT.md
@.planning/phases/03-资金路由与暴露控制/03-RESEARCH.md
@.planning/phases/03-资金路由与暴露控制/03-01-PLAN.md
@bot_v2.py

<interfaces>
Plan 01 provides pure helpers:
```python
RISK_ROUTER
candidate_worst_loss(assessment, bankroll)
route_candidate_assessment(assessment, market, risk_state, router_cfg)
sort_leg_candidates(assessments)
```

Current state defaults:
```python
{
    "balance": BALANCE,
    "starting_balance": BALANCE,
    "total_trades": 0,
    "wins": 0,
    "losses": 0,
    "peak_balance": BALANCE,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 scan loop 风险路由持久化建立集成合同</name>
  <files>tests/test_phase3_scan_loop.py, bot_v2.py</files>
  <read_first>tests/test_phase2_strategies.py, bot_v2.py, config.json, .planning/phases/03-资金路由与暴露控制/03-01-PLAN.md</read_first>
  <behavior>
    - Test 1: `scan_and_update()` 会对同一 market 的 YES/NO candidate_assessments 做分腿排序与路由，不再直接走旧 `best_signal` 开仓路径。
    - Test 2: `data/state.json` 写入 `risk_state.bankroll`、`risk_state.global_reserved_worst_loss`、`risk_state.legs.YES_SNIPER.reserved`、`risk_state.legs.NO_CARRY.reserved`。
    - Test 3: `data/markets/*.json` 写入 `route_decisions[]`，且只有 router 通过的候选会写入 `reserved_exposure`。
  </behavior>
  <action>新增 `tests/test_phase3_scan_loop.py`，沿用 Phase 2 的 `tmp_path + monkeypatch` 模式驱动 `scan_and_update()`。测试夹具必须构造一个 city/date market，同时产出 YES/NO assessment，让 router 只接受一条并把另一条以明确 reason code 拒绝。测试断言 D-15 的“先分腿、后全局拦截”已经生效：同腿排序先决，随后才命中全局 cap。测试还要断言 D-20 / D-22：market JSON 中 `reserved_exposure.reserved_worst_loss` 与 state `global_reserved_worst_loss` 都在 candidate 通过 router 时立刻记账，并且按 worst-loss 全额记账。数组项至少有 `strategy_leg`、`status`、`reserved_worst_loss`、`reasons`、`budget_bucket`；state JSON 包含 `risk_state.bankroll`、`global_reserved_worst_loss`、`legs.YES_SNIPER.budget/reserved`、`legs.NO_CARRY.budget/reserved`。</action>
  <acceptance_criteria>
    - `tests/test_phase3_scan_loop.py` contains `route_decisions` and `global_reserved_worst_loss`
    - `uv run pytest tests/test_phase3_scan_loop.py -q` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_scan_loop.py -q</automated>
  </verify>
  <done>scan loop 风险路由落盘合同已固定，后续实现不能再偷偷只改内存或 print。</done>
</task>

<task type="auto">
  <name>Task 2: 将 risk ledger 与 route decisions 接入 scan_and_update</name>
  <files>bot_v2.py, tests/test_phase3_scan_loop.py</files>
  <read_first>bot_v2.py, tests/test_phase3_scan_loop.py, .planning/phases/02-候选定价与双策略筛选/02-03-SUMMARY.md, .planning/phases/02-候选定价与双策略筛选/02-04-SUMMARY.md</read_first>
  <action>修改 `new_market()`、`load_state()`、`scan_and_update()`：1) `new_market()` 默认新增 `route_decisions: []` 与 `reserved_exposure: null`; 2) `load_state()` 默认新增 `risk_state`，其结构至少包含 `bankroll`、`global_reserved_worst_loss`、`legs`、`city_exposure`、`date_exposure`、`event_exposure`; 3) `scan_and_update()` 在 `candidate_assessments` 生成后、旧 `best_signal` 逻辑之前，按 leg 分组调用 `sort_leg_candidates()` 与 `route_candidate_assessment()`；4) 对每个 accepted route，立刻把 worst-loss 记入 `risk_state` 与 market `reserved_exposure`；5) 按 D-18 为所有 rejected route 保留 `reasons[]`，不能静默丢弃；6) 旧的直接 `balance -= best_signal["cost"]` 开仓路径在本 phase 内保留为后续 seam，但不能绕过 router——没有通过 route 的候选不得再成为 `best_signal`。不得在本计划里生成 order intent、working order 或真实成交对象，保持 Phase 4 边界。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains `"route_decisions": []` in `new_market()`
    - `bot_v2.py` contains `risk_state` in `load_state()` default payload
    - `bot_v2.py` contains `global_reserved_worst_loss` and `reserved_exposure`
    - `uv run pytest tests/test_phase3_router.py tests/test_phase3_scan_loop.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase3_router.py tests/test_phase3_scan_loop.py -q</automated>
  </verify>
  <done>每轮 scan 后，state 与 market JSON 都有真实风险账本；未通过 router 的候选不会再被旧逻辑直接带入开仓。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure router helpers → scan_and_update | scan loop 可能绕过纯函数合同直接占用资金 |
| persisted state → future order/report phases | state/market ledger 会成为 Phase 4/6 的真相源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `scan_and_update()` routing seam | mitigate | 在 `candidate_assessments` 之后立即生成 `route_decisions`，并用测试断言没有 route 通过就不会写 `reserved_exposure`。 |
| T-03-05 | R | `state.json` / market JSON risk ledger | mitigate | 同时落地 state-level aggregate ledger 和 market-level trace ledger，确保后续报告不需要重算。 |
| T-03-06 | D | global/leg caps | mitigate | 所有 cap 命中直接 rejected，不自动替换旧占用，也不跨腿重排；测试覆盖 `global_cap_exceeded` 与 `leg_cap_exceeded`。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase3_scan_loop.py -q`
- `uv run pytest tests/test_phase3_router.py tests/test_phase3_scan_loop.py -q`
</verification>

<success_criteria>
- `risk_state` 与 `route_decisions` 已成为持久化事实。
- YES/NO 两条腿预算占用可从 state.json 直接读取。
- 路由发生在 candidate → future order seam，而不是成交后才记账。
</success_criteria>

<output>
After completion, create `.planning/phases/03-资金路由与暴露控制/03-02-SUMMARY.md`
</output>
