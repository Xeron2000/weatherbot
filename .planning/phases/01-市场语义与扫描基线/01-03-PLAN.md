---
phase: 01-市场语义与扫描基线
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - bot_v2.py
  - README.md
  - tests/test_phase1_reporting.py
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03
must_haves:
  truths:
    - "操作者可以看到扫描 universe 中哪些市场被接受、哪些被跳过"
    - "被纳入扫描结果的市场能显示 station、温区、规则和 contract 标识"
    - "被跳过的市场会显示明确原因"
  artifacts:
    - path: bot_v2.py
      provides: operator-facing status/report formatting for semantic scan results
    - path: README.md
      provides: updated explanation of market snapshot schema and skip semantics
    - path: tests/test_phase1_reporting.py
      provides: smoke checks for accepted/skipped reporting output
  key_links:
    - from: print_status/print_report in bot_v2.py
      to: `resolution_metadata` and `scan_guardrails`
      via: persisted market records
      pattern: "last_scan_status|skip_reasons|station"
---

<objective>
把 Phase 1 的扫描语义结果暴露给操作者，确保 accepted/skipped market 都可见、可解释、可审计。

Purpose: 如果 operator 看不到 station / rules / contract IDs / skip reasons，Phase 1 即使内部 schema 正确，也无法满足“稳定产出候选市场快照且能解释为何跳过”的目标。
Output: status/report 输出增强、README 存储说明更新、reporting smoke tests。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-市场语义与扫描基线/01-RESEARCH.md
@.planning/phases/01-市场语义与扫描基线/01-02-PLAN.md
@README.md
@bot_v2.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在状态/报告输出中展示 accepted vs skipped market 语义</name>
  <read_first>
    - bot_v2.py
    - README.md
    - tests/test_phase1_scan_loop.py
  </read_first>
  <files>bot_v2.py, tests/test_phase1_reporting.py</files>
  <behavior>
    - Test 1: report/status 能显示 skipped market 的 `skip_reasons`
    - Test 2: admitted market 能显示 `station`, `unit`, bucket range, `market_id` / contract 标识摘要
    - Test 3: 输出不把 skipped market 当 open position 或 resolved trade 统计
  </behavior>
  <action>更新 `print_status()` 与 `print_report()`，新增一段 Phase 1 扫描摘要：按 market record 的 `last_scan_status` 分 accepted/skipped，accepted 行至少显示 `city`, `date`, `station`, `unit`, 首个 contract 的 bucket + `market_id`；skipped 行至少显示 `city`, `date`, `skip_reasons`。保持现有 balance/open positions/resolved 汇总不变，不要把 skipped market 误算为持仓。新增 `tests/test_phase1_reporting.py`，对 accepted/skipped 两类 market fixtures 做字符串级 smoke 断言。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains `skip_reasons`
    - `bot_v2.py` contains reporting text that references `station`
    - `tests/test_phase1_reporting.py` exists and asserts both accepted and skipped output cases
    - `uv run pytest tests/test_phase1_reporting.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase1_reporting.py -q</automated>
  </verify>
  <done>操作者可直接从 CLI 输出判断 market 是被接纳还是被跳过，以及原因是什么。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 README 的扫描快照说明与 Phase 1 验证命令</name>
  <read_first>
    - README.md
    - bot_v2.py
    - tests/test_phase1_reporting.py
  </read_first>
  <files>README.md, tests/test_phase1_reporting.py</files>
  <action>更新 README 的 `Data Storage` / `How It Works` 相关段落，明确说明 `data/markets/*.json` 现在保存的不仅是 forecast 与价格，还包括 resolution metadata、condition/token 标识、以及 `skip_reasons`；补充 Phase 1 本地验证命令，使用 `uv run pytest tests/test_phase1_market_semantics.py tests/test_phase1_guardrails.py tests/test_phase1_scan_loop.py tests/test_phase1_reporting.py -q`。不要宣传 live trading、不要添加 Phase 2 之后的承诺。</action>
  <acceptance_criteria>
    - `README.md` contains `skip_reasons`
    - `README.md` contains `resolution metadata`
    - `README.md` contains `uv run pytest`
    - `uv run pytest tests/test_phase1_market_semantics.py tests/test_phase1_guardrails.py tests/test_phase1_scan_loop.py tests/test_phase1_reporting.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase1_market_semantics.py tests/test_phase1_guardrails.py tests/test_phase1_scan_loop.py tests/test_phase1_reporting.py -q</automated>
  </verify>
  <done>README 与测试命令同步到新的市场语义扫描基线，后续执行/审计时不需要猜 snapshot 里多了什么字段。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted market JSON → operator output | 如果输出遗漏 skip reason 或误报 contract 信息，操作者会对 universe 产生错误信心 |
| README/docs → future execution | 文档如果没跟上 schema 变化，会导致错误使用与错误审计 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-21 | I | `print_status()` / `print_report()` | mitigate | 输出 accepted/skipped 分组并直接读取 `skip_reasons`、`station`、contract 标识，禁止重新推断 |
| T-1-22 | R | README validation docs | mitigate | README 写明实际 snapshot 字段与 pytest 命令，减少执行后否认或误解 |
| T-1-23 | D | reporting smoke tests | accept | 输出格式变化可能让测试需要调整，但风险局部且可快速回归 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase1_reporting.py -q`
- `uv run pytest -q`
</verification>

<success_criteria>
- CLI/report 可见 accepted/skipped market 及其原因
- README 反映新的 snapshot schema 与验证命令
- Phase 1 全部自动测试入口可一次性运行
</success_criteria>

<output>
After completion, create `.planning/phases/01-市场语义与扫描基线/01-03-SUMMARY.md`
</output>
