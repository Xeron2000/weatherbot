---
phase: 01-市场语义与扫描基线
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - bot_v2.py
  - tests/conftest.py
  - tests/fixtures/phase1_gamma_event.json
  - tests/fixtures/phase1_weather_snapshot.json
  - tests/test_phase1_market_semantics.py
autonomous: true
requirements:
  - MKT-02
  - MKT-03
must_haves:
  truths:
    - "扫描代码有可重复验证的测试入口，而不是只能靠手跑脚本"
    - "市场语义字段与跳过原因有统一的 helper 合同"
  artifacts:
    - path: pyproject.toml
      provides: pytest + uv test entrypoint for Phase 1
    - path: bot_v2.py
      provides: market semantics helpers and guardrail evaluators
    - path: tests/test_phase1_market_semantics.py
      provides: automated checks for metadata extraction and skip reasons
  key_links:
    - from: tests/test_phase1_market_semantics.py
      to: bot_v2.py
      via: helper-level imports/execution
      pattern: "parse_|extract_|guardrail|stale"
---

<objective>
建立 Phase 1 的验证底座与市场语义 helper 合同，让后续扫描接线改动有自动回归保护。

Purpose: 先把“如何验证市场语义正确”和“坏 market 为什么被拒绝”收口成可测试接口，避免直接在 `scan_and_update()` 里继续堆匿名 dict 和隐式 `continue`。
Output: 最小 pytest/uv 测试底座、Phase 1 fixtures、市场语义与 guardrail helper。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-市场语义与扫描基线/01-RESEARCH.md
@.planning/phases/01-市场语义与扫描基线/01-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
From `bot_v2.py:295-339`:

```python
def get_polymarket_event(city_slug, month, day, year):
def get_market_price(market_id):
def parse_temp_range(question):
def hours_to_resolution(end_date_str):
def in_bucket(forecast, t_low, t_high):
```

From `bot_v2.py:370-389`:

```python
def new_market(city_slug, date_str, event, hours):
    return {
        "city": city_slug,
        "city_name": loc["name"],
        "date": date_str,
        "unit": loc["unit"],
        "station": loc["station"],
        "event_end_date": event.get("endDate", ""),
        "hours_at_discovery": round(hours, 1),
        "status": "open",
        "position": None,
        "forecast_snapshots": [],
        "market_snapshots": [],
        "all_outcomes": [],
    }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 Phase 1 测试底座与固定 fixtures</name>
  <read_first>
    - README.md
    - bot_v2.py
    - .planning/phases/01-市场语义与扫描基线/01-VALIDATION.md
  </read_first>
  <files>pyproject.toml, tests/conftest.py, tests/fixtures/phase1_gamma_event.json, tests/fixtures/phase1_weather_snapshot.json, tests/test_phase1_market_semantics.py</files>
  <action>创建最小 `pyproject.toml`，使用 uv 管理并仅加入执行本 phase 所需的开发依赖 `pytest`；不要引入 package 重构、不要迁移现有运行入口。新增 `tests/` 目录、`conftest.py` 和两个 Phase 1 fixture：一个代表可解析的 Gamma weather event，包含多个温区 market、rules/resolution text、condition/token 标识；一个代表天气快照，覆盖 fresh / stale / missing 三种状态。新增 `tests/test_phase1_market_semantics.py`，先把以下行为写成断言：温区文本可被精确解析；station/unit/rule metadata 可从 event + LOCATIONS 映射得到；缺少 token/condition、单位不一致、天气过期会返回明确的 reason code，而不是 `None` 或静默通过。</action>
  <acceptance_criteria>
    - `pyproject.toml` contains `pytest`
    - `tests/fixtures/phase1_gamma_event.json` exists and contains `markets`
    - `tests/test_phase1_market_semantics.py` contains assertions for `missing_contract_identifiers`, `unit_mismatch`, and `weather_data_stale`
    - `uv run pytest tests/test_phase1_market_semantics.py -q` executes and reports failing or passing tests instead of import/setup errors
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase1_market_semantics.py -q</automated>
  </verify>
  <done>仓库首次具备可运行的 Phase 1 自动验证入口，且 fixture 覆盖正确 market、错误 market、陈旧天气三类输入。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 bot_v2.py 中补齐市场语义与 guardrail helper</name>
  <read_first>
    - bot_v2.py
    - tests/test_phase1_market_semantics.py
    - tests/fixtures/phase1_gamma_event.json
    - tests/fixtures/phase1_weather_snapshot.json
  </read_first>
  <files>bot_v2.py, tests/test_phase1_market_semantics.py</files>
  <behavior>
    - Test 1: 可解析的 weather event 产出 station/unit/resolution_text/condition-token 标识结构
    - Test 2: event 缺 token 或 condition 时返回 `missing_contract_identifiers`
    - Test 3: 市场文本单位与 `LOCATIONS` 单位不一致时返回 `unit_mismatch`
    - Test 4: 天气快照时间超过 freshness 阈值时返回 `weather_data_stale`
  </behavior>
  <action>在 `bot_v2.py` 的 `# POLYMARKET` / `# MARKET DATA STORAGE` 之间新增纯 helper，不改 run loop 外观：`extract_resolution_metadata(event, loc)` 负责从 event/rules/title 抽取 `station`, `unit`, `resolution_text`, `resolution_source`, `rounding_rule`；`build_market_contracts(event, expected_unit)` 负责把每个 outcome 变成 `{market_id, question, range, condition_id, token_id_yes, token_id_no}` 结构；`evaluate_market_guardrails(loc, resolution_metadata, market_contracts, weather_snapshot, hours)` 返回 `{admissible, skip_reasons}`。如果 event payload 拿不到 `condition_id`/token 标识，就显式返回 `missing_contract_identifiers`；如果 unit 不一致，返回 `unit_mismatch`；如果天气快照时间超过固定 freshness 阈值，返回 `weather_data_stale`。保持 helper 为纯函数/可测试接口，不在本 task 里直接改 `scan_and_update()` 主循环。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains `def extract_resolution_metadata(`
    - `bot_v2.py` contains `def build_market_contracts(`
    - `bot_v2.py` contains `def evaluate_market_guardrails(`
    - `bot_v2.py` contains literal reason codes `missing_contract_identifiers`, `unit_mismatch`, and `weather_data_stale`
    - `uv run pytest tests/test_phase1_market_semantics.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase1_market_semantics.py -q</automated>
  </verify>
  <done>后续 plans 可以直接复用统一的 metadata/contract/guardrail helper，而不用在扫描主循环里临时拼接匿名 dict。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Weather/Gamma API → parser | 外部 payload 不可信，可能缺字段、单位不一致、规则文本异常 |
| Fixture/test input → helper logic | 测试必须覆盖坏输入，防止 helper 默认放行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | T | `build_market_contracts` | mitigate | 对 `condition_id` / token 标识缺失做显式 reason code，禁止 silent fallback |
| T-1-02 | I | `extract_resolution_metadata` | mitigate | 从规则文本和本地 `LOCATIONS` 双向校验 station/unit，不一致直接拒绝 |
| T-1-03 | D | pytest bootstrap | accept | 初次引入测试基础设施会增加少量 setup 成本，但风险低且仅影响本地执行 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase1_market_semantics.py -q`
- `uv run pytest -q`
</verification>

<success_criteria>
- 市场语义 helper 拥有自动化测试覆盖
- 坏输入被结构化拒绝，而不是靠隐式 `continue`
- Phase 1 后续 plans 可以建立在明确 helper 合同之上
</success_criteria>

<output>
After completion, create `.planning/phases/01-市场语义与扫描基线/01-01-SUMMARY.md`
</output>
