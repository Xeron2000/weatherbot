---
phase: 01-市场语义与扫描基线
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - bot_v2.py
  - tests/test_phase1_reporting.py
autonomous: true
requirements:
  - MKT-02
gap_closure: true
must_haves:
  truths:
    - "被纳入扫描结果的市场会在 CLI 摘要中显示结算规则文本摘要"
    - "被纳入扫描结果的市场会在 CLI 摘要中显示 condition_id 与 YES/NO token identifiers"
    - "状态/报告测试会在缺少规则文本或 token identifiers 输出时失败"
  artifacts:
    - path: bot_v2.py
      provides: accepted scan summary renders resolution text and contract identifier summary from persisted market semantics
    - path: tests/test_phase1_reporting.py
      provides: regression assertions for resolution_text and condition/token identifiers in status/report output
  key_links:
    - from: print_scan_summary() in bot_v2.py
      to: resolution_metadata.resolution_text
      via: accepted market render line
      pattern: "resolution_text"
    - from: print_scan_summary() in bot_v2.py
      to: market_contracts[0].condition_id/token_id_yes/token_id_no
      via: accepted market render line
      pattern: "condition_id|token_id_yes|token_id_no"
---

<objective>
闭合 Phase 1 验证缺口：让 accepted market 的 CLI 摘要真正把结算规则与 contract identifiers 暴露给操作者，而不是只停留在 market JSON 持久化层。

Purpose: `MKT-02` 已经把语义字段保存到 market snapshot，但 operator-facing 输出还看不到 `resolution_text` 和 `condition/token` 标识，导致 roadmap 成功标准 #2 仍未达成。
Output: accepted scan summary 渲染增强、reporting 回归测试补强、Phase 1 复验入口可直接证明缺口关闭。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-市场语义与扫描基线/01-VERIFICATION.md
@.planning/phases/01-市场语义与扫描基线/01-03-SUMMARY.md
@bot_v2.py
@tests/test_phase1_reporting.py

<interfaces>
From `bot_v2.py`:

```python
def print_scan_summary(markets):
    accepted = [m for m in markets if m.get("last_scan_status") == "ready"]
    skipped = [m for m in markets if m.get("last_scan_status") == "skipped"]
```

```python
metadata = m.get("resolution_metadata", {})
contract = (m.get("market_contracts") or [{}])[0]
market_id = contract.get("market_id") or "unknown"
```

```python
"resolution_metadata": {
    "station": loc["station"],
    "unit": loc["unit"],
    "resolution_text": "",
    "resolution_source": "",
    "rounding_rule": "",
},
"market_contracts": [
    {
        "market_id": "...",
        "condition_id": "...",
        "token_id_yes": "...",
        "token_id_no": "...",
    }
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 accepted scan summary 补齐规则文本与 contract identifiers 输出</name>
  <read_first>
    - bot_v2.py
    - tests/test_phase1_reporting.py
    - .planning/phases/01-市场语义与扫描基线/01-VERIFICATION.md
    - .planning/phases/01-市场语义与扫描基线/01-03-SUMMARY.md
  </read_first>
  <files>bot_v2.py, tests/test_phase1_reporting.py</files>
  <behavior>
    - Test 1: `print_status()` 的 accepted market 行包含 `resolution_metadata.resolution_text` 摘要，满足 VERIFICATION gap #1 的第一条 missing。
    - Test 2: `print_status()` 的 accepted market 行包含 `condition_id`、`token_id_yes`、`token_id_no` 摘要，满足 VERIFICATION gap #1 的第二条 missing。
    - Test 3: `print_report()` 继续保留 accepted/skipped scan summary 与交易统计分离，同时新的规则文本 / token identifiers 断言进入 `tests/test_phase1_reporting.py`。
  </behavior>
  <action>按 `01-VERIFICATION.md` 的 gap.missing 精确修复，不扩展到 Phase 2。先在 `tests/test_phase1_reporting.py` 增加 accepted output 断言：输出里必须包含 `resolution_text` 的稳定摘要，以及 `condition_id=...`、`yes=...`、`no=...` 三段 identifier 文本；现有 skipped 输出与 trade-count 隔离断言保留。随后更新 `bot_v2.py` 的 `print_scan_summary()` accepted 分支：继续显示 `city/date/station/bucket/market_id/unit`，并在同一 accepted market 摘要里额外渲染 `resolution_metadata["resolution_text"]` 的可读摘要，以及首个 contract 的 `condition_id`、`token_id_yes`、`token_id_no`。保持 truth source 仍然直接来自持久化的 `resolution_metadata` 与 `market_contracts`（沿用 01-03 的决定），不要重新推断语义，也不要改动 skipped 分支逻辑。若规则文本过长，可在不丢失规则语义的前提下做 deterministic 截断，但输出中仍必须包含足以识别结算规则的文字片段。</action>
  <acceptance_criteria>
    - `tests/test_phase1_reporting.py` contains assertions for `condition_id=`, `yes=`, and `no=`
    - `tests/test_phase1_reporting.py` asserts accepted output includes a `resolution_text`-derived substring
    - `bot_v2.py` accepted scan summary render path references `resolution_text`, `condition_id`, `token_id_yes`, and `token_id_no`
    - `uv run pytest tests/test_phase1_reporting.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase1_reporting.py -q</automated>
  </verify>
  <done>operator-facing accepted market 摘要不再只显示 station/bucket/market_id，而是能直接看到结算规则文本和 condition/token identifiers。</done>
</task>

<task type="auto">
  <name>Task 2: 运行 Phase 1 回归，确保 gap 修复没有破坏 accepted/skipped 报告边界</name>
  <read_first>
    - bot_v2.py
    - tests/test_phase1_reporting.py
    - tests/test_phase1_market_semantics.py
    - tests/test_phase1_scan_loop.py
  </read_first>
  <files>tests/test_phase1_reporting.py</files>
  <action>在 Task 1 完成后，用现有 Phase 1 套件复跑 reporting 与 scan loop 回归，确认新增 accepted summary 字段不会让 skipped market 误入交易统计，也不会破坏 Phase 1 已有语义/guardrail 行为。若回归暴露断言缺口，只在 `tests/test_phase1_reporting.py` 中补足与本 gap 直接相关的 smoke 断言，不重写其他 phase 测试结构。</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_phase1_reporting.py tests/test_phase1_scan_loop.py -q` exits 0
    - `uv run pytest tests/test_phase1_market_semantics.py tests/test_phase1_guardrails.py tests/test_phase1_scan_loop.py tests/test_phase1_reporting.py -q` exits 0
    - reporting tests still assert skipped reasons and open/resolved counts isolation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase1_market_semantics.py tests/test_phase1_guardrails.py tests/test_phase1_scan_loop.py tests/test_phase1_reporting.py -q</automated>
  </verify>
  <done>Phase 1 的 gap 修复被完整回归守住，后续复验可直接用同一套自动测试证明 Truth #2 已闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted market JSON → CLI accepted summary | 若 `resolution_text` / token identifiers 在输出层被丢弃，操作者会误以为市场语义仍不完整 |
| Reporting regression tests → future refactors | 若测试不守护规则文本与 contract identifiers，可再次出现“数据已存但不可见”的回归 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24 | I | `print_scan_summary()` accepted render path | mitigate | 直接从 `resolution_metadata` 和 `market_contracts[0]` 渲染 `resolution_text`、`condition_id`、`token_id_yes`、`token_id_no`，避免 operator 看不到关键市场语义 |
| T-01-25 | R | `tests/test_phase1_reporting.py` | mitigate | 为规则文本与 identifier 输出加字符串级断言，确保未来改动不能在测试全绿时再次丢字段 |
| T-01-26 | D | CLI accepted summary formatting | accept | accepted 行新增字段会变长，但这是本 gap 所需可见性；风险局部且可由 smoke tests 快速回归 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase1_reporting.py -q`
- `uv run pytest tests/test_phase1_market_semantics.py tests/test_phase1_guardrails.py tests/test_phase1_scan_loop.py tests/test_phase1_reporting.py -q`
</verification>

<success_criteria>
- accepted market 的 CLI 摘要明确显示结算规则文本摘要
- accepted market 的 CLI 摘要明确显示 condition_id 与 YES/NO token identifiers
- Phase 1 reporting 回归在缺少上述字段时会失败
</success_criteria>

<output>
After completion, create `.planning/phases/01-市场语义与扫描基线/01-04-SUMMARY.md`
</output>
