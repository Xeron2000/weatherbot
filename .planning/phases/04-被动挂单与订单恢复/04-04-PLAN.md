---
phase: 04-被动挂单与订单恢复
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - bot_v2.py
  - tests/test_phase4_reporting.py
  - README.md
autonomous: true
requirements: [OBS-02, ORDR-02]
must_haves:
  truths:
    - "操作者可以在 CLI 里看到当前有哪些订单处于 planned/working/partial，以及最近有哪些 canceled/expired/filled。"
    - "每笔订单的 limit price、tif、expires_at、status reason 与 fill progress 都可追踪。"
    - "README 提供 Phase 4 的回归命令和新增持久化字段说明。"
  artifacts:
    - path: bot_v2.py
      provides: "operator-facing order lifecycle reporting"
      contains: "print_order_summary"
    - path: tests/test_phase4_reporting.py
      provides: "status/report order visibility regression"
      contains: "test_print_status"
    - path: README.md
      provides: "Phase 4 verification docs"
      contains: "Phase 4 Verification"
  key_links:
    - from: bot_v2.py
      to: bot_v2.py
      via: "order_state + market order facts -> status/report"
      pattern: "print_status|print_report|order_state|active_order"
    - from: README.md
      to: tests/test_phase4_reporting.py
      via: "documented regression command"
      pattern: "test_phase4_reporting.py"
---

<objective>
把 Phase 4 订单生命周期暴露给操作者：在 status/report 里展示 active order、status counts、reason summary，并补 README 验证说明。

Purpose: Phase 4 不是“后台悄悄挂单”；如果 operator 看不到 working/partial/canceled/expired/fill 轨迹，就无法判断策略执行是否可靠。
Output: CLI order summary、Phase 4 reporting 回归测试、README 验证与字段文档。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bot_v2.py
@README.md
@tests/test_phase3_reporting.py
@tests/test_phase4_restore.py

<interfaces>
Expected restored order summary from Plan 03:
```python
{
    "active_orders": [...],
    "status_counts": {
        "planned": 1,
        "working": 2,
        "partial": 1,
        "filled": 3,
        "canceled": 2,
        "expired": 1
    },
    "last_restored_at": "..."
}
```

Per-market order truth source:
```python
{
    "active_order": {...} | None,
    "order_history": [{"status": "filled", "status_reason": "passive_fill_complete", ...}],
    "reserved_exposure": {...},
    "position": {...} | None
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 CLI order summary 建立 operator-facing 合同</name>
  <files>tests/test_phase4_reporting.py</files>
  <read_first>tests/test_phase3_reporting.py, bot_v2.py, tests/test_phase4_restore.py</read_first>
  <behavior>
    - Test 1: `print_status()` 显示 order status counts（planned/working/partial/filled/canceled/expired）、active order 数量，以及每笔 active order 的 city/date/leg/limit/tif/expires/status reason。
    - Test 2: `print_report()` 显示最近 terminal order reason summary，并区分 fill reasons、cancel reasons、expire reasons。
    - Test 3: order summary 与已有 risk/candidate summary 分段展示，不把订单统计混进 trades 或 candidate count。
  </behavior>
  <action>新增 `tests/test_phase4_reporting.py`，沿用 Phase 3 reporting fixture 风格，mock `load_state()` 和 `load_all_markets()`。测试必须显式断言以下 operator-facing 文案：`Order lifecycle`、`planned`、`working`、`partial`、`filled`、`canceled`、`expired`、`time_in_force`、`limit=`、`remaining=`、`status_reason=`。同时锁定 recent reason summary 至少包含 `quote_repriced`、`candidate_downgraded`、`expired`、`passive_fill_complete` 其中若干项。不要在测试里触碰 live API。</action>
  <acceptance_criteria>
    - `tests/test_phase4_reporting.py` contains `Order lifecycle`
    - `uv run pytest tests/test_phase4_reporting.py -q` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase4_reporting.py -q</automated>
  </verify>
  <done>status/report 需要展示的订单事实已被测试锁死，执行者不会把 Phase 4 输出做成模糊计数。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 order reporting 并补 README 验证说明</name>
  <files>bot_v2.py, tests/test_phase4_reporting.py, README.md</files>
  <read_first>bot_v2.py, README.md, tests/test_phase4_reporting.py, .planning/phases/03-资金路由与暴露控制/03-04-SUMMARY.md</read_first>
  <action>在 `bot_v2.py` 中新增 `print_order_summary(state, markets)` 或等价 helper，并让 `print_status()` / `print_report()` 在风险摘要之后、trade 明细之前输出订单生命周期段。该段至少要展示：`order_state.status_counts`、当前 active orders 明细（city/date/strategy_leg/token_side/range/limit_price/time_in_force/expires_at/filled_shares/remaining_shares/status_reason）、以及 recent terminal reason summary。展示层必须直接读取 `active_order`、`order_history`、`order_state`，不要重算订单决策。同步更新 `README.md`：新增 `## Phase 4 Verification`，给出完整回归命令 `uv run pytest tests/test_phase4_orders.py tests/test_phase4_scan_loop.py tests/test_phase4_restore.py tests/test_phase4_reporting.py -q`，并文档化 `active_order`、`order_history`、`order_state` 三个 Phase 4 真相源。不要顺手改动其他 phase 的文档结构。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains order summary output in both `print_status()` and `print_report()`
    - `README.md` contains `Phase 4 Verification`
    - `uv run pytest tests/test_phase4_orders.py tests/test_phase4_scan_loop.py tests/test_phase4_restore.py tests/test_phase4_reporting.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase4_orders.py tests/test_phase4_scan_loop.py tests/test_phase4_restore.py tests/test_phase4_reporting.py -q</automated>
  </verify>
  <done>operator 可以直接在 CLI/README 里理解订单生命周期，而不需要翻 market JSON 手工审计。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restored order facts → CLI output | operator 完全依赖持久化订单事实判断系统健康 |
| README verification docs → manual regression | 文档必须准确指向 Phase 4 真相源与测试入口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | R | status/report order summary | mitigate | 展示层只读取 `active_order`、`order_history`、`order_state`，pytest 锁定关键字段与 reason summary，避免 operator 看到重算后的伪状态。 |
| T-04-11 | I | terminal order reasons | mitigate | report 仅输出 order lifecycle 所需字段（status/reason/limit/tif/fill progress），不引入额外敏感配置或 token secrets。 |
| T-04-12 | D | verification docs | mitigate | README 明确列出 Phase 4 回归命令与字段真相源，减少 operator 因错跑测试或误读字段导致的诊断失效。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase4_orders.py tests/test_phase4_scan_loop.py tests/test_phase4_restore.py tests/test_phase4_reporting.py -q`
</verification>

<success_criteria>
- status/report 能直接展示 Phase 4 订单生命周期与 reason summary。
- README 提供完整的 Phase 4 回归命令和字段说明。
- 操作者无需打开 JSON 文件，也能判断 working/partial/canceled/expired/filled 的当前状态与历史原因。
</success_criteria>

<output>
After completion, create `.planning/phases/04-被动挂单与订单恢复/04-04-SUMMARY.md`
</output>
