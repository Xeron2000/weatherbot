---
phase: 04-被动挂单与订单恢复
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - bot_v2.py
  - tests/test_phase4_reporting.py
autonomous: true
requirements: [OBS-02]
gap_closure: true
must_haves:
  truths:
    - "操作者可以在 active order 明细里直接看到每笔 unfinished 订单的显式 status 与 status_reason。"
    - "操作者可以在 report 中逐笔查看 recent terminal orders，而不只是 grouped reason counts。"
    - "逐笔 terminal order 输出仍然直接读取持久化 order facts，不在展示层重算 lifecycle。"
  artifacts:
    - path: bot_v2.py
      provides: "per-order active and terminal order lifecycle reporting"
      contains: "Recent terminal orders"
    - path: tests/test_phase4_reporting.py
      provides: "OBS-02 per-order reporting regression"
      contains: "status=partial"
  key_links:
    - from: "markets[*].active_order"
      to: "print_status()/print_report()"
      via: "active_order_details rows"
      pattern: "status=|status_reason=|time_in_force=|expires_at="
    - from: "markets[*].order_history"
      to: "print_status()/print_report()"
      via: "recent terminal orders rows"
      pattern: "order_id=|status=|reason=|updated_at="
---

<objective>
补齐 Phase 4 在 OBS-02 上的最后缺口：让 CLI/report 逐笔展示 unfinished 与 terminal 订单的 lifecycle 轨迹，而不是只给聚合计数。

Purpose: 验证报告已经确认订单工作流是真的跑通了，但 operator 仍无法直接看到“哪一笔为什么被刷新/撤销/成交”。这个 gap plan 只修补可观测性缺口，不改订单执行语义。
Output: 更严格的 Phase 4 reporting 回归测试，以及 `print_order_summary()` 的逐笔 order visibility 输出。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-被动挂单与订单恢复/04-VERIFICATION.md
@.planning/phases/04-被动挂单与订单恢复/04-04-SUMMARY.md
@bot_v2.py
@tests/test_phase4_reporting.py

<interfaces>
Active order facts already persisted and must be rendered directly:
```python
{
    "order_id": "...",
    "strategy_leg": "YES_SNIPER",
    "token_side": "yes",
    "limit_price": 0.11,
    "time_in_force": "GTD",
    "expires_at": "2026-04-17T18:00:00+00:00",
    "status": "partial",
    "status_reason": "quote_repriced",
    "filled_shares": 120.0,
    "remaining_shares": 80.0,
    "updated_at": "2026-04-17T12:00:00+00:00"
}
```

Terminal orders already live in `order_history` and must be shown per entry:
```python
{
    "order_id": "terminal:filled:passive_fill_complete:0.1000",
    "status": "filled",
    "status_reason": "passive_fill_complete",
    "limit_price": 0.10,
    "filled_shares": 200.0,
    "remaining_shares": 0.0,
    "updated_at": "2026-04-17T12:00:00+00:00"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 锁定逐笔 unfinished / terminal order 可见性的回归合同</name>
  <files>tests/test_phase4_reporting.py</files>
  <read_first>tests/test_phase4_reporting.py, bot_v2.py, .planning/phases/04-被动挂单与订单恢复/04-VERIFICATION.md, .planning/phases/04-被动挂单与订单恢复/04-04-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `print_status()` 输出 active order 行时显式包含 `status=partial`（或对应 unfinished 状态），同时保留 `status_reason=quote_repriced`、`limit=0.1100`、`time_in_force=GTD`、`expires_at=...`、`filled=120.0000 remaining=80.0000`。
    - Test 2: `print_report()` 输出 `Recent terminal orders` 小节，并逐笔展示至少 3 类 terminal orders：`filled` / `canceled` / `expired`，每行至少包含 `order_id=`、`status=`、`reason=`、`updated_at=`，以及 `limit=` 或 fill progress。
    - Test 3: grouped terminal reason summary 仍可保留，但测试不能只靠 grouped reasons 通过；必须断言逐笔 terminal rows 存在。
  </behavior>
  <action>扩展 `tests/test_phase4_reporting.py`，直接用现有 `make_active_order()` / `make_terminal_order()` fixture 数据，新增对 active row 显式 `status=` 的断言；把 report 回归从 `Recent terminal reasons` 提升为逐笔 `Recent terminal orders` 合同，明确断言 `terminal:filled:passive_fill_complete:0.1000`、`terminal:canceled:candidate_downgraded:0.1100`、`terminal:expired:expired:0.1200`、`status=filled|canceled|expired`、`reason=`、`updated_at=` 这些具体字符串。不要删除已有 section-ordering 断言；只把 OBS-02 缺失的 per-order visibility 锁死。</action>
  <acceptance_criteria>
    - `tests/test_phase4_reporting.py` contains `status=partial`
    - `tests/test_phase4_reporting.py` contains `Recent terminal orders`
    - `tests/test_phase4_reporting.py` contains `terminal:filled:passive_fill_complete:0.1000`
    - `uv run pytest tests/test_phase4_reporting.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase4_reporting.py -q</automated>
  </verify>
  <done>OBS-02 的 operator-facing 输出要求先被测试精确锁定，执行者无法再用 grouped reason rollup 冒充逐笔可见性。</done>
</task>

<task type="auto">
  <name>Task 2: 在 print_order_summary() 中输出逐笔 active/terminal order lifecycle</name>
  <files>bot_v2.py, tests/test_phase4_reporting.py</files>
  <read_first>bot_v2.py, tests/test_phase4_reporting.py, .planning/phases/04-被动挂单与订单恢复/04-VERIFICATION.md, .planning/phases/04-被动挂单与订单恢复/04-02-SUMMARY.md, .planning/phases/04-被动挂单与订单恢复/04-03-SUMMARY.md</read_first>
  <action>在 `bot_v2.py` 为 `print_order_summary(state, markets)` 增加逐笔 terminal order 展示 helper（例如 `collect_recent_terminal_orders()`），直接遍历 `markets[*].order_history`，按 `updated_at` 倒序取最近 10 笔 terminal orders。active order 行必须从 `active_order` 直接打印显式 `status={order['status']}`，并保留 `status_reason`、`limit`、`time_in_force`、`expires_at`、`filled`、`remaining`。terminal order 行必须至少打印：`order_id=... | status=... | reason=... | updated_at=... | limit=... | filled=... remaining=...`。可以继续保留 grouped `Recent terminal reasons` 汇总，但它必须位于逐笔 `Recent terminal orders` 之后，且不能替代逐笔明细。不要在展示层推断 lifecycle 或 reason；只读取持久化 `active_order` / `order_history` 字段。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains `Recent terminal orders`
    - `bot_v2.py` contains `status=` within active order detail output
    - `bot_v2.py` contains `order_id=` within terminal order output
    - `uv run pytest tests/test_phase4_orders.py tests/test_phase4_scan_loop.py tests/test_phase4_restore.py tests/test_phase4_reporting.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase4_orders.py tests/test_phase4_scan_loop.py tests/test_phase4_restore.py tests/test_phase4_reporting.py -q</automated>
  </verify>
  <done>status/report 现在能逐笔回答“哪一笔单当前是什么状态、为什么变成这个状态、最近哪些单以何原因终结”，从而真正满足 OBS-02。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted `active_order` / `order_history` → CLI output | 操作者依赖这些输出审计订单原因，不能被展示层重算成伪事实 |
| reporting regression tests → acceptance of Phase 4 closeout | 如果测试继续只看 grouped reasons，OBS-02 会再次被误判为完成 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | R | `print_order_summary()` active order lines | mitigate | active rows直接读取 persisted `active_order.status` / `status_reason`，并用 pytest 断言 `status=` 与 `status_reason=` 同时存在，避免 operator 无法追责状态来源。 |
| T-04-14 | R | terminal order reporting | mitigate | 新增逐笔 `Recent terminal orders` 输出，逐行打印 `order_id/status/reason/updated_at/limit/fill progress`，并用回归断言具体 terminal order ids，避免 grouped reason rollup 掩盖具体订单。 |
| T-04-15 | I | order reporting surface | accept | 输出仅限 order_id、status、reason、价格与 fill progress，不暴露 secrets、API keys 或真实资金凭据；信息泄露风险低且对 operator 诊断必要。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase4_reporting.py -q`
- `uv run pytest tests/test_phase4_orders.py tests/test_phase4_scan_loop.py tests/test_phase4_restore.py tests/test_phase4_reporting.py -q`
</verification>

<success_criteria>
- active order 明细显式展示 unfinished 订单的 `status` 与 `status_reason`。
- report/status 都能逐笔列出 recent terminal orders，并附带 reason、updated_at 与价格/填充进度字段。
- `tests/test_phase4_reporting.py` 不再允许仅靠 grouped reason summary 通过。
</success_criteria>

<output>
After completion, create `.planning/phases/04-被动挂单与订单恢复/04-05-SUMMARY.md`
</output>
