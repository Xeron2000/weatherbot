---
phase: 04-被动挂单与订单恢复
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.json
  - bot_v2.py
  - tests/test_phase4_orders.py
autonomous: true
requirements: [ORDR-01, ORDR-02]
must_haves:
  truths:
    - "操作者可以看到每个被接受候选生成的被动挂单意图，包含 token side、limit price、time in force 与 expires_at。"
    - "每个订单都以明确状态机表示，而不是散落在 scan loop 的临时变量里。"
    - "订单状态变化会写入连续 history，后续 scan/report/restore 都可直接复用。"
  artifacts:
    - path: config.json
      provides: "Phase 4 order policy defaults"
      contains: "order_policy"
    - path: bot_v2.py
      provides: "pure order intent and transition helpers"
      contains: "build_passive_order_intent"
    - path: tests/test_phase4_orders.py
      provides: "order intent/state-machine contracts"
      contains: "test_build_passive_order_intent"
  key_links:
    - from: config.json
      to: bot_v2.py
      via: "ORDER_POLICY config loader"
      pattern: "order_policy|ORDER_POLICY"
    - from: bot_v2.py
      to: tests/test_phase4_orders.py
      via: "order intent and transition helper contracts"
      pattern: "planned|working|partial|filled|canceled|expired"
---

<objective>
先定义 Phase 4 的订单合同：被动挂单配置、限价意图构建、状态机与 history 记录 helper。

Purpose: 后续 scan loop 只能消费稳定的订单真相源，不能把 planned/working/filled/canceled 语义埋进过程逻辑里临时拼接。
Output: `order_policy` 配置块、被动订单 helper、Phase 4 基础回归测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@config.json
@bot_v2.py
@tests/test_phase3_router.py

<interfaces>
Current routing truth source already persisted on each market:
```python
{
    "candidate_assessments": [
        {
            "strategy_leg": "YES_SNIPER" | "NO_CARRY",
            "token_side": "yes" | "no",
            "range": (65.0, 69.0),
            "status": "accepted" | "size_down" | "reprice" | "rejected",
            "edge": 0.05,
            "quote_context": {"bid": 0.09, "ask": 0.11, "tick_size": 0.01},
            "reasons": []
        }
    ],
    "route_decisions": [
        {
            "strategy_leg": "YES_SNIPER",
            "status": "accepted",
            "reserved_worst_loss": 20.0,
            "exposure_keys": {...}
        }
    ],
    "reserved_exposure": {"strategy_leg": "YES_SNIPER", "release_reason": None}
}
```

Current quote truth for a contract:
```python
{
    "market_id": "mkt-65-69",
    "yes": {"bid": 0.09, "ask": 0.11, "tick_size": 0.01, "min_order_size": 1.0},
    "no": {"bid": 0.81, "ask": 0.83, "tick_size": 0.01, "min_order_size": 1.0},
    "execution_ok": True,
    "execution_stop_reasons": []
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立被动订单 helper 的 RED 合同</name>
  <files>tests/test_phase4_orders.py, config.json</files>
  <read_first>config.json, bot_v2.py, tests/test_phase3_router.py</read_first>
  <behavior>
    - Test 1: `load_order_policy_config()` 读取顶层 `order_policy`，显式提供 YES/NO 的默认 `time_in_force`、`gtd_buffer_hours`、`replace_edge_buffer`、`price_improve_ticks`。
    - Test 2: `build_passive_order_intent()` 只会为 `route_decisions.status == accepted` 的 assessment 生成订单，且订单包含 `status=planned`、`limit_price`、`shares`、`time_in_force`、`expires_at`、`remaining_shares`、`history` 初始事件。
    - Test 3: `apply_order_transition()` 能把状态流转规范到 `planned`、`working`、`partial`、`filled`、`canceled`、`expired` 六类，并向 `history` 追加 reason/time/fill delta。
    - Test 4: `new_market()` / 向后兼容 loader 默认包含 `active_order=None` 和 `order_history=[]`，避免旧 JSON 因缺字段崩溃。
  </behavior>
  <action>新增 `tests/test_phase4_orders.py`，锁定最小订单合同。`config.json` 新增顶层 `order_policy` 配置块，至少包含这些具体键：`yes_time_in_force: "GTC"`、`no_time_in_force: "GTD"`、`gtd_buffer_hours: 6.0`、`price_improve_ticks: 1`、`replace_edge_buffer: 0.02`、`max_order_hours_open: 72.0`。测试里显式断言订单状态集合仅允许 `planned`、`working`、`partial`、`filled`、`canceled`、`expired`，并覆盖 `expires_at` 对 GTD 生效、`GTC` 不强制生成近端过期时间、以及 `history` 首条事件为 `planned`。不要在本任务里接 scan loop。</action>
  <acceptance_criteria>
    - `config.json` contains `"order_policy"`
    - `tests/test_phase4_orders.py` contains `planned`, `working`, `partial`, `filled`, `canceled`, `expired`
    - `uv run pytest tests/test_phase4_orders.py -q` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase4_orders.py -q</automated>
  </verify>
  <done>Phase 4 的订单输入/输出口径被测试锁定，执行者不需要在 wiring 阶段再猜状态字段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 bot_v2.py 落地订单配置、意图与状态机 helper</name>
  <files>bot_v2.py, config.json, tests/test_phase4_orders.py</files>
  <read_first>bot_v2.py, config.json, tests/test_phase4_orders.py, .planning/phases/03-资金路由与暴露控制/03-04-SUMMARY.md</read_first>
  <behavior>
    - Test 1: 被动订单 limit price 依据对应 side 的 quote `bid`/`ask` 与 `tick_size` 生成，不会 crossing spread；无 tick size 或 quote 缺失时给 deterministic reason。
    - Test 2: `build_passive_order_intent()` 输出的 shares/cost 使用已接受 route 的 `reserved_worst_loss` 和 quote 价格计算，不回退到旧的 `best_signal` 开仓结构。
    - Test 3: `apply_order_transition()` 保持 history append-only，并更新 `filled_shares`、`remaining_shares`、`status_reason`。
  </behavior>
  <action>在 `bot_v2.py` Phase 3 routing helper 后新增 Phase 4 纯函数：`load_order_policy_config(config_dict)`、`ORDER_POLICY = load_order_policy_config(_cfg)`、`find_assessment_for_reservation(market)`、`build_passive_order_intent(market, reservation, assessment, quote_snapshot, now_ts)`、`apply_order_transition(order, next_status, reason, ts, fill_shares=0.0, fill_price=None, patch=None)`、`is_order_terminal(order)`。实现约束：1) 继续维持 brownfield 单文件模式，不拆模块；2) `active_order` 只允许每个 market 一笔，和 Phase 3 单 market 单 reservation 模式对齐；3) order payload 必须带 `order_id`、`strategy_leg`、`token_side`、`market_id`、`range`、`limit_price`、`shares`、`filled_shares`、`remaining_shares`、`time_in_force`、`expires_at`、`status`、`status_reason`、`created_at`、`updated_at`、`history`；4) `new_market()` 和旧 market 默认兼容字段必须补齐 `active_order`、`order_history`。本计划实现的是订单合同，不要修改 `print_status()` / `print_report()`。</action>
  <acceptance_criteria>
    - `bot_v2.py` contains `ORDER_POLICY = load_order_policy_config(_cfg)`
    - `bot_v2.py` contains `def build_passive_order_intent(` and `def apply_order_transition(`
    - `bot_v2.py` contains `active_order` and `order_history` in market defaults
    - `uv run pytest tests/test_phase4_orders.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_phase4_orders.py -q</automated>
  </verify>
  <done>订单配置、intent schema 与状态流转 helper 已稳定，scan loop 可直接围绕这些合同接线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route_decisions + quote_snapshot → order intent | 已接受候选会升级为可执行订单真相 |
| config.json → ORDER_POLICY | 本地配置直接决定 tif、刷新边界与过期规则 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `order_policy` config | mitigate | `load_order_policy_config()` 显式解析必需字段并给 deterministic 默认值/错误 reason，避免 tif 或刷新阈值静默漂移。 |
| T-04-02 | R | `apply_order_transition()` history | mitigate | 所有状态流转都 append 到 `history`，保留 `reason`、`ts`、`fill_shares`，保证后续 restore/report 可追溯。 |
| T-04-03 | D | passive price helper | mitigate | quote/tick 缺失时不生成可执行订单，返回 deterministic reason，避免无效订单卡住后续 scan。 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase4_orders.py -q`
</verification>

<success_criteria>
- `order_policy`、被动限价意图和状态机 helper 已落地。
- market JSON 有稳定的 `active_order` / `order_history` 字段。
- 后续计划可直接基于这些 helper 做 scan wiring 和恢复，不需要再次发明订单 schema。
</success_criteria>

<output>
After completion, create `.planning/phases/04-被动挂单与订单恢复/04-01-SUMMARY.md`
</output>
