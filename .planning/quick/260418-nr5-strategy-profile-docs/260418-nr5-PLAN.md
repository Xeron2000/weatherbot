---
phase: quick-260418-nr5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/strategy-profile-playbook.md
autonomous: true
requirements:
  - QUICK-260418-NR5
must_haves:
  truths:
    - "用户能直接看到当前推荐 strategy profile 为 1000，以及推荐原因。"
    - "用户能在同一份文档里比较 100 / 1000 / 10000 三档真实参数差异。"
    - "用户能按文档指引判断何时从 100 升到 1000、何时从 1000 升到 10000，并知道如何切换与规避常见误用。"
  artifacts:
    - path: "docs/strategy-profile-playbook.md"
      provides: "strategy profile 实盘使用手册，覆盖推荐档位、三档差异、适用场景、升级时机、切换方式与注意事项"
      min_lines: 80
  key_links:
    - from: "docs/strategy-profile-playbook.md"
      to: "config.json"
      via: "引用 strategy_profile 与 strategy_profiles 的真实参数"
      pattern: "strategy_profile|strategy_profiles|100|1000|10000"
---

<objective>
产出一份只面向实盘使用判断的 strategy profile 手册，明确当前推荐档位是 `1000`，并把三档资金策略的真实差异、升级时机和切换注意事项整理进 `docs/`。

Purpose: 让使用者不必反复对照 `config.json` 推断三档差异，直接按文档完成选档、升级和切换。
Output: `docs/strategy-profile-playbook.md`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@config.json
@README.md
@.planning/quick/260418-n5y-100-1000-10000/260418-n5y-SUMMARY.md

<interfaces>
From config.json:
```json
{
  "strategy_profile": "1000",
  "strategy_profiles": {
    "100": { "balance": 100.0, "kelly_fraction": 0.35, "no_kelly_fraction": 1.8 },
    "1000": { "balance": 1000.0, "kelly_fraction": 0.22, "no_kelly_fraction": 1.5 },
    "10000": { "balance": 10000.0, "kelly_fraction": 0.12, "no_kelly_fraction": 1.2 }
  }
}
```

已有事实：当前最合理推荐档位为 `1000`；`docs/` 目录当前不存在；README 已有最小 profile 说明，因此本次不扩写 README，避免重复维护。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写出 strategy profile 实盘手册主体</name>
  <files>docs/strategy-profile-playbook.md</files>
  <action>创建 `docs/` 并新增 `docs/strategy-profile-playbook.md`。文档开头直接给出当前推荐档位 `1000`，说明其是当前阶段默认且最平衡的选择；随后基于 `config.json` 的真实数值整理三档差异，至少覆盖 `balance`、`kelly_fraction`、`no_kelly_fraction`、YES/NO 价格与概率阈值、`max_size`、`risk_router` 关键 cap、`order_policy`、`paper_execution`。文档必须包含：1) 当前推荐档位；2) 三档真实参数差异表；3) 每档适用场景；4) 何时从 100 升到 1000；5) 何时从 1000 升到 10000；6) 切换方式；7) 注意事项。不要发明代码里不存在的运行语义，不要把 README 改成重复手册。</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
path = Path('docs/strategy-profile-playbook.md')
text = path.read_text(encoding='utf-8')
required = [
    '推荐档位', '1000', '100', '10000', '参数差异', '适用场景',
    '从 100 升到 1000', '从 1000 升到 10000', '切换方式', '注意事项'
]
missing = [item for item in required if item not in text]
assert not missing, missing
print('ok')
PY</automated>
  </verify>
  <done>文档存在，且用真实配置值解释三档区别与推荐逻辑；读者无需打开 `config.json` 也能完成选档和切换判断。</done>
</task>

<task type="auto">
  <name>Task 2: 交叉校对文档与当前配置一致</name>
  <files>docs/strategy-profile-playbook.md</files>
  <action>逐项核对文档中的关键数字与 `config.json` 当前三档 preset 一致，尤其是 `1000` 默认档、`100` 更激进、`10000` 更保守这三层关系。把“何时升级”写成可观察条件：例如候选质量稳定、回撤承受能力、单市场容量限制开始成为瓶颈、执行耐心与成交质量要求提升等；避免写成模糊口号。最后补一节“切换后立即检查项”，至少包含 `strategy_profile` 字段、顶层 merge 后默认行为、不要手工只改顶层而忘记 profile 的提醒。</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
import json
from pathlib import Path
cfg = json.loads(Path('config.json').read_text(encoding='utf-8'))
doc = Path('docs/strategy-profile-playbook.md').read_text(encoding='utf-8')
checks = [
    str(cfg['strategy_profiles']['100']['yes_strategy']['max_price']),
    str(cfg['strategy_profiles']['1000']['no_strategy']['min_price']),
    str(cfg['strategy_profiles']['10000']['risk_router']['global_usage_cap_pct']),
    cfg['strategy_profile'],
]
missing = [item for item in checks if item not in doc]
assert not missing, missing
print('ok')
PY</automated>
  </verify>
  <done>文档里的关键阈值与默认推荐档完全对齐当前配置，且升级/切换建议具备可执行性，不依赖 README 补充解释。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator decision | 文档会影响操作者选档与切换，错误参数会直接误导实盘配置 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260418-nr5-01 | T | docs/strategy-profile-playbook.md | mitigate | 所有参数差异直接取自 `config.json` 当前 preset，不手写猜测值；用自动校验命令锁定关键数字出现在文档中 |
| T-quick-260418-nr5-02 | I | docs/strategy-profile-playbook.md | accept | 文档仅包含已提交策略阈值与使用说明，不新增 secret 或私有数据 |
| T-quick-260418-nr5-03 | E | docs/strategy-profile-playbook.md | mitigate | 在文档中明确 `1000` 是当前默认推荐，且说明升级前提与切换检查项，降低误把更激进/更保守档位当成通用默认的风险 |
</threat_model>

<verification>
- 运行文档完整性检查，确认必需章节全部存在。
- 运行配置交叉校验，确认文档包含三档关键真实参数与当前默认 `strategy_profile`。
</verification>

<success_criteria>
- `docs/strategy-profile-playbook.md` 创建完成。
- 文档明确推荐 `1000`，并解释为什么当前阶段优先该档。
- 文档完整覆盖三档真实参数差异、适用场景、升级时机、切换方式与注意事项。
- 不需要修改 README，或仅在执行时发现绝对必要才做最小补充。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-nr5-strategy-profile-docs/260418-nr5-SUMMARY.md`
</output>
