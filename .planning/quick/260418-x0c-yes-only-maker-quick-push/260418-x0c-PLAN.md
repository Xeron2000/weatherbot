---
phase: quick-260418-x0c-yes-only-maker-quick-push
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/strategy.py
  - weatherbot/paper_execution.py
  - tests/test_phase2_strategies.py
  - tests/test_strategy_paper_execution.py
  - tests/test_phase4_orders.py
  - .planning/quick/260418-uag-no-yes-yes/
autonomous: true
requirements:
  - QUICK-YES-MAKER-CONTRACT
must_haves:
  truths:
    - "YES 候选评估改按与挂单一致的 maker/target price 决策，不再被 live ask 单独提前打成 reprice。"
    - "YES 仍保持低价狙击策略：max_price、min_edge、min_probability 护栏继续生效，只是统一基于共享目标挂价合同。"
    - "YES accepted 候选进入 order intent 后，limit_price 与 assessment 暴露的 intent_limit_price（或等价字段）完全一致。"
    - "NO 逻辑与现有 yes_only_runtime 边界不受本次 quick 任务影响。"
    - "代码验证完成后，本次 YES maker 改动会被原子提交；废弃 quick 目录只做本地清理，不再污染工作区；随后 push 当前分支全部已提交内容到远端，而无关本地脏改动保持未提交。"
  artifacts:
    - path: weatherbot/strategy.py
      provides: "YES assessment 与 order-intent 共享定价合同"
    - path: weatherbot/paper_execution.py
      provides: "YES passive order intent 复用 assessment 定价或同一 helper"
    - path: tests/test_phase2_strategies.py
      provides: "YES 候选 maker 合同回归测试"
    - path: tests/test_strategy_paper_execution.py
      provides: "assessment ↔ order intent 共享价格合同回归"
    - path: tests/test_phase4_orders.py
      provides: "YES order intent limit_price 对齐回归"
    - path: .planning/quick/260418-uag-no-yes-yes/
      provides: "废弃 quick 目录已被移除，不再污染工作区"
  key_links:
    - from: weatherbot/strategy.py
      to: weatherbot/paper_execution.py
      via: "YES assessment 与 order intent 共用同一 target passive price"
      pattern: "intent_limit_price|compute_passive_limit_price|build_passive_order_intent"
    - from: tests/test_phase2_strategies.py
      to: weatherbot/strategy.py
      via: "高 ask / 低 maker target 的 YES 场景回归"
      pattern: "price_above_max|accepted|intent_limit_price"
    - from: tests/test_phase4_orders.py
      to: weatherbot/paper_execution.py
      via: "YES order limit_price 与 assessment 合同一致"
      pattern: "limit_price|intent_limit_price"
    - from: .planning/quick/260418-uag-no-yes-yes/
      to: git status
      via: "cleanup 后工作区不再保留该废弃目录"
      pattern: "260418-uag-no-yes-yes"
---

<objective>
把 YES-only 候选评估从 `ask` 口径切到与被动挂单一致的 maker/target 挂价口径，并显式落地 assessment → order intent 的共享价格合同。

Purpose: 修复 `ask` 很高但可被动挂单价格仍低的 YES 场景被过早 `reprice` 的问题，在不放松低价狙击护栏的前提下提升可成交性。
Output: 一个 YES-only 共享定价合同（`intent_limit_price` 或等价字段）、最小 wiring 改动、覆盖 assessment 与 order intent 的 targeted 回归测试；并在代码验证通过后清理废弃 quick 目录与 push 当前已提交内容。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@weatherbot/strategy.py
@weatherbot/paper_execution.py
@tests/test_phase2_strategies.py
@tests/test_strategy_paper_execution.py
@tests/test_phase4_orders.py

<interfaces>
From `weatherbot/strategy.py`:
```python
def evaluate_yes_candidate(bucket_probability, quote_snapshot, hours, market_context=None):
    ...

def build_candidate_assessments(bucket_probabilities, quote_snapshot, hours, market_context=None):
    ...
```

Current YES mismatch to remove:
```python
ask = quote.get("ask")
if ask is not None and ask > YES_STRATEGY.get("max_price", 1.0):
    reasons.append("price_above_max")
edge = round((fair_price or 0.0) - (ask or 0.0), 6) if ask is not None else None
```

From `weatherbot/paper_execution.py`:
```python
def compute_passive_limit_price(side_quote, policy):
    ...

def build_passive_order_intent(market, reservation, assessment, quote_snapshot, now_ts):
    ...
```

Existing YES order path already prices passively:
```python
limit_price, reason = compute_passive_limit_price(side_quote, ORDER_POLICY)
```

Task boundary:
- 只改 YES assessment 与 YES order intent 价格合同。
- NO helper / NO assessment / NO runtime 一律不动。
- 继续保留 `min_probability`、`min_edge`、`max_price`、yes_only_runtime、order schema 与 TIF 现有语义。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 YES maker 合同回归，覆盖 accepted 路径与 assessment→intent 对齐</name>
  <files>tests/test_phase2_strategies.py, tests/test_strategy_paper_execution.py, tests/test_phase4_orders.py</files>
  <behavior>
    - Test 1: 当 YES `ask` 高于 `max_price`，但 `compute_passive_limit_price()` 产出的 maker target 仍低于 `max_price` 且满足 `min_edge` 时，assessment 应 accepted/size_down，而不是仅因 ask 变成 reprice。
    - Test 2: assessment 暴露共享价格字段（推荐 `intent_limit_price`），其值等于 YES maker target，`edge` 也基于该价格而不是 ask。
    - Test 3: `build_passive_order_intent()` 对同一 YES assessment/quote 产出的 `limit_price` 必须与 assessment 共享价格字段一致。
    - Test 4: 缺 quote/tick 的 deterministic reason 以及 NO-only 边界保持原样。
  </behavior>
  <action>在三份测试文件中新增 YES-only 回归：构造 `ask` 显著高于 `bid`、但 maker target 仍处于低价狙击区间的盘口，明确断言 `price_above_max` 与 `edge/status` 改为基于共享 target price，而不是 live ask。把 assessment→order intent 的共享字段合同锁死；不要扩展到 NO 逻辑、风险路由、订单生命周期其他状态。</action>
  <verify>
    <automated>uv run pytest -q --no-cov tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py tests/test_phase4_orders.py</automated>
  </verify>
  <done>测试清楚表达 YES 共享 maker 合同，并在当前 ask-based 实现下暴露失败；NO 边界与既有 deterministic reason 断言继续成立。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 落地 YES 共享定价合同并让 order intent 直接复用</name>
  <files>weatherbot/strategy.py, weatherbot/paper_execution.py, tests/test_phase2_strategies.py, tests/test_strategy_paper_execution.py, tests/test_phase4_orders.py</files>
  <behavior>
    - Test 1: `evaluate_yes_candidate()` 生成共享 target price 字段，并用它决定 `price_above_max`、`edge`、`accepted/size_down/reprice/rejected`。
    - Test 2: `max_price`、`min_edge`、`min_probability` 继续生效，但都围绕 YES maker target，不再围绕 ask 单独早拒。
    - Test 3: `build_passive_order_intent()` 若 assessment 已提供共享 target price，则使用该价格；否则退回同一 helper 计算，确保合同不分叉。
    - Test 4: NO 分支与 yes_only_runtime、order schema、GTC/GTD 语义保持兼容。
  </behavior>
  <action>在 `weatherbot/strategy.py` 为 YES assessment 引入一个显式共享价格合同（优先 `intent_limit_price` 命名），其来源必须与 `compute_passive_limit_price()` 一致；随后把 YES 的 `price_above_max`、`edge`、status 决策统一切到该价格。再在 `weatherbot/paper_execution.py` 的 YES order intent 中优先消费 assessment 上的共享价格字段，避免 assessment 与挂单再次各算各的；若 assessment 字段缺失，仅允许回退到同一 helper，不能再回到 ask 驱动语义。不要改 NO helper、NO assessment、paper fill、risk router 或清理/push 流程。</action>
  <verify>
    <automated>uv run pytest -q --no-cov tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py tests/test_phase4_orders.py</automated>
  </verify>
  <done>YES assessment 与 YES order intent 使用同一 target passive price；宽 spread 高 ask 场景能进入 accepted → order intent 路径；低价狙击护栏与 NO 边界保持不变。</done>
</task>

<task type="auto">
  <name>Task 3: 清理废弃 quick 目录并 push 当前已提交内容</name>
  <files>.planning/quick/260418-uag-no-yes-yes/</files>
  <behavior>
    - Task 3a: 废弃目录 `.planning/quick/260418-uag-no-yes-yes/` 被移除，不再出现在 `git status --short` 的未跟踪项里。
    - Task 3b: 本次 YES maker 改动（代码+测试）形成原子 commit。
    - Task 3c: push 当前分支全部已提交内容；无关本地改动（如 `.planning/config.json` 与 phase 文档脏改动）保持未提交、未推送。
  </behavior>
  <action>在代码任务与 targeted 验证全部通过后，删除废弃未跟踪目录 `.planning/quick/260418-uag-no-yes-yes/`，仅作为本地工作区清理。然后检查 `git status --short --branch`，只暂存本 quick 相关文件：`weatherbot/strategy.py`、`weatherbot/paper_execution.py`、`tests/test_phase2_strategies.py`、`tests/test_strategy_paper_execution.py`、`tests/test_phase4_orders.py`。创建一个原子 commit 收口本次 YES maker 改动；无关本地脏改动必须保持 unstaged。最后执行 `git push` 推送当前分支所有已提交内容到远端。</action>
  <verify>
    <automated>test ! -e ".planning/quick/260418-uag-no-yes-yes" && test -z "$(git status --porcelain=v1 --branch | grep '^## .*ahead')" && test -n "$(git log --oneline -1)" && test -z "$(git status --porcelain=v1 | grep '260418-uag-no-yes-yes')" && test -z "$(git status --porcelain=v1 | grep 'weatherbot/strategy.py')" && test -z "$(git status --porcelain=v1 | grep 'weatherbot/paper_execution.py')" && test -z "$(git status --porcelain=v1 | grep 'tests/test_phase2_strategies.py')" && test -z "$(git status --porcelain=v1 | grep 'tests/test_strategy_paper_execution.py')" && test -z "$(git status --porcelain=v1 | grep 'tests/test_phase4_orders.py')"</automated>
  </verify>
  <done>废弃 quick 目录已在本地清理，本次 YES maker 改动已原子提交，且当前分支全部已提交内容已推送到远端；无关本地脏改动仍保留在本地。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quote snapshot → YES assessment | 外部盘口进入候选评估；若仍按 ask 决策，会把本可挂低价单的 YES 提前拦截。 |
| assessment → order intent | 若两层各自计算 limit price，会再次出现 accepted 候选与实际挂单价格分叉。 |
| local working tree → remote branch | 清理废弃 quick 目录与 push 时必须保证只影响已提交内容，不能把无关本地脏改动一并推送。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260418-x0c-01 | T | `weatherbot/strategy.py:evaluate_yes_candidate` | mitigate | 让 YES assessment 的 `price_above_max` / `edge` / `status` 全部基于共享 target price，而不是 ask。 |
| T-quick-260418-x0c-02 | T | `weatherbot/paper_execution.py:build_passive_order_intent` | mitigate | YES order intent 优先消费 assessment 上的共享价格字段；缺失时也只能回退到同一 helper。 |
| T-quick-260418-x0c-03 | D | `compute_passive_limit_price` inputs | mitigate | 对缺 tick / 缺 bid/ask 继续返回 deterministic reason，避免生成非法 YES order。 |
| T-quick-260418-x0c-04 | E | NO runtime / yes_only 边界 | accept | 本任务明确 YES-only；通过 targeted 回归证明 NO 逻辑与 yes_only_runtime 语义未扩大变更面。 |
| T-quick-260418-x0c-05 | T | local cleanup / push | mitigate | 删除 `.planning/quick/260418-uag-no-yes-yes/` 后检查 `git status`，只 push 已提交内容。 |
</threat_model>

<verification>
- 运行 `uv run pytest -q --no-cov tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py tests/test_phase4_orders.py`
- 重点确认：YES 高 ask / 低 maker target 场景通过；assessment 与 order intent 的价格字段一致；NO 边界断言仍通过。
- 代码验证通过后，删除 `.planning/quick/260418-uag-no-yes-yes/` 并执行 `git push`。
</verification>

<success_criteria>
- `weatherbot/strategy.py` 中 YES assessment 不再用 ask 单独决定 `price_above_max` 与 `edge`。
- assessment 暴露一个共享 target price 字段，且 `build_passive_order_intent()` 与之完全一致。
- YES 仍受 `max_price` / `min_edge` / `min_probability` 约束，但 accepted 候选可进入 `order_intent` 路径。
- NO 逻辑与 yes_only_runtime 边界未被扩大；废弃 quick 目录已在本地清理，且当前分支全部已提交内容已推送。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-x0c-yes-only-maker-quick-push/260418-x0c-SUMMARY.md`
</output>
