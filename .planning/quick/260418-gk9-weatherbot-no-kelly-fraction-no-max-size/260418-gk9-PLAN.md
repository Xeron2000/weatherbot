---
phase: quick-260418-gk9-weatherbot-no-kelly-fraction-no-max-size
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.json
  - weatherbot/strategy.py
  - tests/test_phase3_router.py
  - tests/test_strategy_paper_execution.py
autonomous: true
requirements:
  - QUICK-260418-GK9
must_haves:
  truths:
    - "NO 腿可以使用独立于全局/YES 的 sizing 缩放参数，并允许比 YES 更大的 max_size。"
    - "当 NO 候选通过路由后，reserved_worst_loss 与最终 passive order shares 会随 NO 专用 sizing 配置联动变大。"
    - "计划必须诚实保持现状：全局 `kelly_fraction` 仍不是主 sizing 链路，改动只把 NO 独立缩放接入现有 `candidate_worst_loss -> shares` 路径。"
  artifacts:
    - path: "config.json"
      provides: "NO 专用 `no_kelly_fraction` 与更大的 `no_strategy.max_size` 配置入口"
      contains: "no_kelly_fraction"
    - path: "weatherbot/strategy.py"
      provides: "NO 腿独立 sizing 缩放接线，继续复用现有 candidate_worst_loss 路径"
      contains: "candidate_worst_loss"
    - path: "tests/test_phase3_router.py"
      provides: "NO route/reservation sizing 回归测试"
      contains: "no_kelly_fraction"
    - path: "tests/test_strategy_paper_execution.py"
      provides: "reserved_worst_loss 到 passive order shares 的 NO 联动回归测试"
      contains: "build_passive_order_intent"
  key_links:
    - from: "config.json"
      to: "weatherbot/strategy.py"
      via: "NO 专用配置被 strategy 读取并进入 candidate_worst_loss"
      pattern: "no_kelly_fraction|max_size|candidate_worst_loss"
    - from: "weatherbot/strategy.py"
      to: "weatherbot/paper_execution.py"
      via: "reserved_worst_loss / limit_price -> shares"
      pattern: "reserved_worst_loss|shares ="
    - from: "tests/test_phase3_router.py"
      to: "tests/test_strategy_paper_execution.py"
      via: "先锁定 reservation sizing，再锁定 passive order shares 联动"
      pattern: "candidate_worst_loss|build_passive_order_intent"
---

<objective>
在不做全链路仓位重构的前提下，为 NO 腿引入独立的 sizing 缩放参数，并放大 NO 的 max_size，让高确定性 NO 候选在现有 reservation -> passive order 链路里拿到更大的模拟仓位。

Purpose: 当前全局 `kelly_fraction` 并未真正驱动 sizing；这次 quick task 要诚实地把工作收口为“NO 专用缩放 + NO max_size 提升 + shares 联动验证”，而不是假装修复整套 kelly 系统。
Output: 更新后的 `config.json`、`weatherbot/strategy.py`，以及锁定 NO reservation / shares 联动的回归测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@config.json
@weatherbot/strategy.py
@weatherbot/paper_execution.py
@tests/test_phase3_router.py
@tests/test_strategy_paper_execution.py
@README.md

<decisions>
- D-01: 这次只做“NO 专用 kelly fraction + 更大的 NO max_size”，不要扩散成全链路 sizing / bankroll 重构。
- D-02: 必须诚实反映现状：当前 `kelly_fraction` 仅存在于 `calc_kelly()`，并未驱动真实 sizing；计划要以现有 `NO_STRATEGY.max_size -> candidate_worst_loss() -> reserved_worst_loss -> build_passive_order_intent().shares` 链路为核心。
- D-03: 最小改动点锁定为 `config.json` + `weatherbot/strategy.py`，测试只补 router / paper execution 相关覆盖，不顺手改 README 以外的无关模块。
- D-04: 工作区已有无关脏文件 `.planning/config.json` 与 `.planning/phases/04-.../04-VERIFICATION.md`；执行时不要编辑、不要暂存、不要纳入验证范围。
</decisions>

<interfaces>
From `weatherbot/strategy.py`:
```python
def calc_kelly(p, price): ...
def strategy_for_leg(strategy_leg): ...
def candidate_worst_loss(assessment, bankroll): ...
def route_candidate_assessment(assessment, market, risk_state, router_cfg): ...
```

Current sizing path:
```python
strategy = strategy_for_leg(assessment.get("strategy_leg"))
max_size = float(strategy.get("max_size", 0.0) or 0.0)
size_multiplier = float(assessment.get("size_multiplier", 0.0) or 0.0)
worst_loss = max_size * size_multiplier
```

From `weatherbot/paper_execution.py`:
```python
def build_passive_order_intent(market, reservation, assessment, quote_snapshot, now_ts): ...
```

Current shares contract:
```python
reserved_worst_loss = float(reservation.get("reserved_worst_loss", 0.0) or 0.0)
shares = round(reserved_worst_loss / limit_price, 4)
```

Implication for this quick task:
```text
不要把 `calc_kelly()` 伪装成主链路修复；应把 NO 专用缩放接到 candidate_worst_loss，
再让 router reservation 与 paper order shares 自然放大。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在现有 sizing 链路里接入 NO 专用缩放与更大的 NO max_size</name>
  <files>config.json, weatherbot/strategy.py</files>
  <action>按 D-01/D-02 只做最小收口：在 `config.json` 中新增顶层 `no_kelly_fraction`（命名保持直白，不新增一整层配置结构），并把 `no_strategy.max_size` 提升到明显高于 YES 的数值；在 `weatherbot/strategy.py` 中把 NO 腿的 sizing 缩放接入 `candidate_worst_loss()` 或其最小必要 helper，使 `NO_CARRY` 的 reserved worst loss 由 `no_strategy.max_size * no_kelly_fraction * size_multiplier`（或等价、同语义的最小实现）驱动，而 YES 路径继续保持现状，不把未使用的全局 `calc_kelly()` 硬塞进真实下单链路。若需要兼容缺省配置，保持向后兼容默认值并明确只影响 NO 腿；不要修改 `paper_execution.py` 的 shares 公式，不要改 router 预算模型，不要碰无关脏文件。</action>
  <verify>
    <automated>MISSING — Task 2 先补 `tests/test_phase3_router.py` 与 `tests/test_strategy_paper_execution.py` 的 NO-sizing 回归后，再运行对应 pytest 命令验证</automated>
  </verify>
  <done>NO 腿拥有独立于 YES/全局的 sizing 缩放入口，且通过同一 `candidate_worst_loss -> reserved_worst_loss` 链路输出更大的 worst loss；YES sizing 语义保持不变。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 router 与 paper execution 回归锁住 NO reservation / shares 联动</name>
  <files>tests/test_phase3_router.py, tests/test_strategy_paper_execution.py</files>
  <behavior>
    - Test 1: 当 NO 腿配置了更大的 `max_size` 与 `no_kelly_fraction` 时，`candidate_worst_loss()` / route reservation 会比 YES 或旧 NO 配置输出更大的 `reserved_worst_loss`。
    - Test 2: `build_passive_order_intent()` 不改 shares 公式，但当 reservation 的 `reserved_worst_loss` 变大时，NO passive order 的 `shares` 会按 `reserved_worst_loss / limit_price` 同步放大。
    - Test 3: 回归明确说明全局 `kelly_fraction` 仍非真实 sizing 来源，避免测试把目标漂移成修复 `calc_kelly()`。
  </behavior>
  <action>在 `tests/test_phase3_router.py` 中补一组最小回归，直接 monkeypatch `NO_STRATEGY` / 新增的 `no_kelly_fraction` 相关运行时配置，断言 `candidate_worst_loss()` 与 `route_candidate_assessment()` 产出的 `reserved_worst_loss` 按 NO 独立缩放放大；在 `tests/test_strategy_paper_execution.py` 中补 NO order intent 用例，验证同一 shares 公式在更大的 reservation 下生成更大的 `shares`，且 `time_in_force` 等 NO 语义保持稳定。测试入口继续优先通过 `import weatherbot` / `import bot_v2` 的兼容面跑，不新增无关测试文件，不修改无关脏文件。</action>
  <verify>
    <automated>uv run pytest tests/test_phase3_router.py tests/test_strategy_paper_execution.py -q</automated>
  </verify>
  <done>测试稳定证明：NO 独立缩放确实驱动 reservation 与 passive order shares 放大，而全局 `kelly_fraction` 仍未被伪装成 sizing 主链路。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.json` → `weatherbot/strategy.py` | 本地配置直接决定 NO reservation sizing，错误接线会把 quick 改动误扩散到 YES 或全局 sizing |
| `weatherbot/strategy.py` → router reservation | `candidate_worst_loss()` 是 risk reservation 事实源，若语义漂移会让 budget / reservation 校验失真 |
| reservation → `weatherbot/paper_execution.py` | paper order shares 完全依赖 `reserved_worst_loss`，若 reservation 不稳定会导致 shares 假放大或假缩小 |
| 当前工作区脏文件 → 本次 quick 执行 | 无关 planning 文件若被顺手修改，会污染 quick task 范围与验证结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-gk9-01 | T | `weatherbot/strategy.py` | mitigate | 只在 `NO_CARRY` 分支接入 `no_kelly_fraction`，并保留 YES 原路径，防止 quick 改动误写到全局 sizing |
| T-quick-gk9-02 | D | `weatherbot/strategy.py` / router | mitigate | 用 `tests/test_phase3_router.py` 锁定 `reserved_worst_loss` 的数值变化，避免 reservation 因实现偏差失真 |
| T-quick-gk9-03 | R | `weatherbot/paper_execution.py` contract | mitigate | 不改 `shares = reserved_worst_loss / limit_price` 公式，只通过测试证明 NO reservation 放大后 shares 自然联动 |
| T-quick-gk9-04 | T | 工作区无关脏文件 | mitigate | 执行范围限制为 `config.json`、`weatherbot/strategy.py` 和两份测试文件，验证命令只跑相关 pytest 文件 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase3_router.py tests/test_strategy_paper_execution.py -q`
- spot check：`weatherbot/strategy.py` 中 NO 专用缩放只进入 `candidate_worst_loss()` 或其最小 helper，未把 `calc_kelly()` 强行接入真实 sizing 链路
- spot check：仅 `config.json`、`weatherbot/strategy.py`、`tests/test_phase3_router.py`、`tests/test_strategy_paper_execution.py` 发生改动，无关脏文件保持原样
</verification>

<success_criteria>
- `config.json` 提供 NO 专用缩放入口，且 NO `max_size` 大于 YES `max_size`。
- `candidate_worst_loss()` / route reservation 对 NO 腿输出更大的 `reserved_worst_loss`，YES 行为不变。
- `build_passive_order_intent()` 在不改 shares 公式的前提下，对更大的 NO reservation 生成更大的 `shares`。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-gk9-weatherbot-no-kelly-fraction-no-max-size/260418-gk9-SUMMARY.md`
</output>
