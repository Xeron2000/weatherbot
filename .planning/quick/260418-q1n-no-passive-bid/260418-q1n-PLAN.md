---
phase: quick-260418-q1n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/strategy.py
  - weatherbot/paper_execution.py
  - tests/test_strategy_paper_execution.py
  - tests/test_phase4_orders.py
autonomous: true
requirements:
  - QUICK-NO-PASSIVE-BID
must_haves:
  truths:
    - "NO 候选评估改按 target passive bid / maker 价格决策，而不是按 no.ask 的 taker 语义决策"
    - "YES 候选与 YES 下单路径完全不变"
    - "NO assessment 与 build_passive_order_intent 对同一 maker 价格语义保持一致"
  artifacts:
    - path: "weatherbot/strategy.py"
      provides: "NO 候选 maker 决策语义"
    - path: "weatherbot/paper_execution.py"
      provides: "NO 被动挂单目标价格合同"
    - path: "tests/test_strategy_paper_execution.py"
      provides: "NO assessment / paper execution 联动回归测试"
    - path: "tests/test_phase4_orders.py"
      provides: "NO passive order intent 稳定性回归"
  key_links:
    - from: "weatherbot/strategy.py"
      to: "weatherbot/paper_execution.py"
      via: "NO maker target price 计算语义"
      pattern: "compute_passive_limit_price"
    - from: "weatherbot/strategy.py"
      to: "tests/test_strategy_paper_execution.py"
      via: "NO accepted/reprice/rejected 决策断言"
      pattern: "evaluate_no_candidate|build_candidate_assessments"
    - from: "weatherbot/paper_execution.py"
      to: "tests/test_phase4_orders.py"
      via: "NO order limit price 与 GTD/GTC 回归"
      pattern: "build_passive_order_intent"
---

<objective>
把 NO 从 ask 驱动的 taker 决策改成 target passive bid 驱动的 maker 决策，只改 NO；YES 保持现状不动。

Purpose: 让 NO 候选筛选语义与真实被动挂单执行一致，避免评估层继续用 taker ask 决策、执行层却按 maker bid 挂单而产生错配。
Output: 一处 NO 评估修复、一个保持兼容的 maker 定价合同、以及覆盖 assessment→order intent 的回归测试。
</objective>

<execution_context>
@/home/xeron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/xeron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@weatherbot/strategy.py
@weatherbot/paper_execution.py
@tests/test_strategy_paper_execution.py
@tests/test_phase4_orders.py

<interfaces>
From weatherbot/strategy.py:
```python
def evaluate_no_candidate(bucket_probability, quote_snapshot, hours):
    ...
```

Current NO semantics to replace:
```python
ask = quote.get("ask")
if ask is None:
    reasons.append("missing_quote_price")
if ask is not None and ask < NO_STRATEGY.get("min_price", 0.0):
    reasons.append("price_below_min")
edge = round((fair_price or 0.0) - (ask or 0.0), 6) if ask is not None else None
```

From weatherbot/paper_execution.py:
```python
def compute_passive_limit_price(side_quote, policy):
    bid = side_quote.get("bid")
    ask = side_quote.get("ask")
    tick_size = side_quote.get("tick_size")
    ...
    candidate = bid + (tick_size * improve_ticks)
    candidate = max(candidate, bid)
    if ask > bid:
        candidate = min(candidate, ask - tick_size)
    return round(candidate, 4), None
```

Existing NO order path already uses maker pricing:
```python
limit_price, reason = compute_passive_limit_price(side_quote, ORDER_POLICY)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 NO maker 决策与 order intent 对齐的回归测试</name>
  <files>tests/test_strategy_paper_execution.py, tests/test_phase4_orders.py</files>
  <behavior>
    - Test 1: NO assessment 使用 target passive bid 价格；当 `bid=0.82 / ask=0.85 / tick=0.01` 且 improve=1 时，评估价应为 `0.83`，不是 `0.85`。
    - Test 2: 当 NO maker 目标价低于 `min_price` 时，assessment 返回 `reprice + price_below_min`；仅 ask 较高不能单独让其 accepted。
    - Test 3: `build_passive_order_intent()` 对同一 NO quote 仍产出同一个 maker `limit_price`，确保评估层与下单层语义一致。
    - Test 4: YES 相关断言保持原样，不新增 YES 语义变化。
  </behavior>
  <action>在 `tests/test_strategy_paper_execution.py` 增加 NO maker 价格回归：直接断言 `edge`、`status`、`reasons` 基于 target passive bid，而不是 `no.ask`。在 `tests/test_phase4_orders.py` 增加一条 NO order intent 对齐样例，验证 assessment 通过的同一 quote 会生成相同 maker `limit_price`。不要改 YES 测试语义，不要把范围扩展到 scan loop、风险路由或订单生命周期其他状态。</action>
  <verify>
    <automated>uv run pytest -q tests/test_strategy_paper_execution.py tests/test_phase4_orders.py</automated>
  </verify>
  <done>测试先把“NO 用 maker target 价决策”锁死，并明确覆盖 accepted / reprice 两类核心分支，且 YES 现状未被改写。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 最小改动实现 NO maker 决策并复用现有被动挂单价格合同</name>
  <files>weatherbot/strategy.py, weatherbot/paper_execution.py</files>
  <behavior>
    - Test 1: `evaluate_no_candidate()` 的 `missing_quote_price`、`price_below_min`、`edge`、`accepted/size_down/rejected/reprice` 全部改以 target passive bid 为基准。
    - Test 2: `ask_above_max` 保护仍保留，避免 NO 在极端 ask 条件下失去现有 ceiling 护栏。
    - Test 3: `build_passive_order_intent()` 的输出结构保持兼容；如需抽 helper，只能做最小共享，不改变已有 order schema。
  </behavior>
  <action>在 `weatherbot/strategy.py` 将 NO leg 评估价切到 maker target passive bid 语义，并与 `weatherbot/paper_execution.py` 的 `compute_passive_limit_price()` 保持同一公式。优先直接复用现有 helper；若必须抽共享函数，也只做最小搬运并保持 `build_passive_order_intent()` 返回结构、reason code、time_in_force 行为不变。NO 仍保留 `ask_above_max` ceiling 检查，但 `missing_quote_price` / `price_below_min` / `edge` 不再按 ask 判断。YES 路径、quote snapshot 结构、执行引擎其余部分一律不改。</action>
  <verify>
    <automated>uv run pytest -q tests/test_strategy_paper_execution.py tests/test_phase4_orders.py</automated>
  </verify>
  <done>NO assessment 与 NO passive order intent 对同一 quote 得出一致的 maker 目标价；既有 YES 语义和 order schema 完全兼容。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quote snapshot → strategy evaluator | 外部盘口数据进入候选评估，错误价格基准会直接污染 NO 决策 |
| strategy evaluator → passive order builder | assessment 与下单如果使用不同价格合同，会导致 accepted 候选无法按同语义挂单 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-q1n-01 | T | `weatherbot/strategy.py:evaluate_no_candidate` | mitigate | 改为使用与 `compute_passive_limit_price()` 一致的 maker target 价，并用回归测试锁定 `edge/status/reasons` |
| T-quick-q1n-02 | I | `weatherbot/paper_execution.py:build_passive_order_intent` | mitigate | 保持 order schema 不变，只共享价格合同；新增测试验证 NO assessment 与 intent 对齐 |
| T-quick-q1n-03 | D | YES / 其他执行路径 | mitigate | 任务明确限定只改 NO，验证命令只跑相关测试，避免对 scan/risk/order lifecycle 做无关改造 |
</threat_model>

<verification>
- `uv run pytest -q tests/test_strategy_paper_execution.py tests/test_phase4_orders.py`
</verification>

<success_criteria>
- NO 不再按 taker ask 做候选决策，而是按 target passive bid / maker 价格决策。
- NO assessment 与 `build_passive_order_intent()` 对同一盘口使用一致的 maker 目标价。
- YES 路径、order schema、既有被动挂单生命周期保持兼容。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-q1n-no-passive-bid/260418-q1n-SUMMARY.md`
</output>
