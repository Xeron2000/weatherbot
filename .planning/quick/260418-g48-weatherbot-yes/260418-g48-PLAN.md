---
phase: quick-260418-g48-weatherbot-yes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/strategy.py
  - tests/test_phase2_strategies.py
autonomous: true
requirements:
  - QUICK-260418-G48
must_haves:
  truths:
    - "同日市场在城市本地时间下午峰值窗口之后，若 METAR 已贴近或越过高温桶上沿，YES 候选会被降权或直接拒绝。"
    - "非同日市场或本地时间尚未进入峰值窗口时，不会误伤 YES 候选概率。"
    - "这次改动只收口 YES 峰值过滤，不改变 NO 候选 sizing / kelly 语义。"
  artifacts:
    - path: "weatherbot/strategy.py"
      provides: "YES 峰值窗口 penalty helper 与 evaluate_yes_candidate 接线"
      contains: "assess_yes_peak_window_penalty"
    - path: "tests/test_phase2_strategies.py"
      provides: "峰值窗口本地时区、same-day guard、YES-only 回归测试"
      contains: "test_yes_peak_window_penalty"
  key_links:
    - from: "weatherbot/strategy.py"
      to: "weatherbot/strategy.py"
      via: "evaluate_yes_candidate -> assess_yes_peak_window_penalty -> adjusted_probability/reasons"
      pattern: "evaluate_yes_candidate|assess_yes_peak_window_penalty|probability_penalty"
    - from: "tests/test_phase2_strategies.py"
      to: "weatherbot/strategy.py"
      via: "import bot_v2 后经兼容导出验证 YES penalty 行为"
      pattern: "evaluate_yes_candidate|probability_penalty_factor|yes_peak_window"
---

<objective>
基于已存在的峰值窗口实现，把 YES 高温桶过滤器收口到可交付状态，并用回归测试锁死本地时间/同日判断。

Purpose: 减少下午后高温桶 YES 假信号，同时避免把这次 quick task 扩散成无关策略改造。
Output: 收口后的 `weatherbot/strategy.py` 与通过的 `tests/test_phase2_strategies.py` 峰值窗口回归测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@weatherbot/strategy.py
@tests/test_phase2_strategies.py
@README.md

<decisions>
- D-01: 这次只做“YES 峰值窗口过滤器”，不要顺手改 NO kelly 或其它策略腿。
- D-02: `weatherbot/strategy.py` 已有 `YES_PEAK_WINDOW_*` 常量、`get_local_now()`、`assess_yes_peak_window_penalty()` 和 `evaluate_yes_candidate()` penalty 接线；计划应以补齐/收口/验证为主，不假装从零实现。
- D-03: 工作区已有与本任务无关的脏文件 `.planning/config.json` 与 `.planning/phases/04-.../04-VERIFICATION.md`；执行时不要编辑、不要暂存、不要作为验证对象。
</decisions>

<interfaces>
From `weatherbot/strategy.py`:
```python
def get_local_now(city_slug, now_ts=None): ...
def assess_yes_peak_window_penalty(bucket_probability, market_context=None): ...
def evaluate_yes_candidate(bucket_probability, quote_snapshot, hours, market_context=None): ...
```

Current YES candidate contract:
```python
{
  "adjusted_probability": float,
  "status": str,
  "reasons": list[str],
  "probability_penalty_factor": float,
  "probability_penalty_reason": str | None,
  "probability_local_time": str | None,
}
```

Current regression entrypoint:
```python
import bot_v2
```
测试继续通过兼容入口断言，不把 quick 修复伪装成深层 import 重写。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收口 YES 峰值窗口 penalty 逻辑，不扩散到 NO 策略</name>
  <files>weatherbot/strategy.py</files>
  <action>基于现有实现审计并补齐 `assess_yes_peak_window_penalty()` 与 `evaluate_yes_candidate()` 的行为，只保留本任务需要的 YES 峰值过滤：使用 `city_slug` 的本地时间判断 `market_date` 是否为同日、是否已过 `YES_PEAK_WINDOW_END_HOUR`，再按 METAR 相对 bucket ceiling 的位置给出 1.0 / 0.35 / 0.0 penalty，并把 reason/local_time 稳定写回 YES assessment。若发现当前实现已满足规则，只做最小修正与注释级收口，不新增新配置面，不改 `evaluate_no_candidate()`、NO sizing、kelly 或其它候选路由逻辑。严格只编辑 `weatherbot/strategy.py`，不要碰无关脏文件。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_strategies.py -q -k "peak_window"</automated>
  </verify>
  <done>YES assessment 在本地下午峰值窗口下会按 same-day + METAR 条件降权/拒绝，并持续暴露 penalty factor、reason、local time；NO 路径未被改动。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补齐并跑通峰值窗口回归测试，锁住 YES-only 语义</name>
  <files>tests/test_phase2_strategies.py</files>
  <behavior>
    - Test 1: 同一 UTC 时间映射到城市本地上午时，YES penalty 不触发；映射到本地下午且 METAR 贴近上沿时，penalty factor 变为 0.35 并使候选 rejected。
    - Test 2: 非同日 market 即使本地已过峰值窗口，也不会触发 penalty。
    - Test 3: 峰值窗口只影响 YES 候选，不引入 NO 调整测试目标漂移。
  </behavior>
  <action>整理当前已新增的峰值窗口测试，确保断言直接围绕 `evaluate_yes_candidate()` 返回的 `probability_penalty_factor`、`adjusted_probability`、`status`、`reasons` 与 same-day guard，而不是写成宽泛的 scan loop 回归。若需要补充一条保护性断言，优先在现有 `tests/test_phase2_strategies.py` 内完成，不新增无关测试文件。最终运行该文件的全量测试，确认这次 quick task 只覆盖 YES 峰值过滤；保持 `.planning/config.json` 与 `04-VERIFICATION.md` 完全不动。</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_strategies.py -q</automated>
  </verify>
  <done>峰值窗口相关回归测试稳定通过，并明确证明本地时区判断、same-day guard 与 YES-only 过滤语义都已锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `market_context` / METAR → `assess_yes_peak_window_penalty()` | 运行时外部观测与日期/时区信息会直接影响 YES 候选是否被拒绝 |
| `bot_v2` 兼容导出 → 测试 | quick 修复若只在深层模块生效、兼容入口失真，测试会给出假阳性 |
| 当前工作区脏文件 → 本次 quick 执行 | 无关脏文件若被顺手编辑或带入验证，会污染本次任务范围 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-g48-01 | T | `weatherbot/strategy.py` | mitigate | 用 same-day + local-hour + METAR ceiling 条件把 YES penalty 写死，避免把 UTC 或错误日期误当作峰值窗口 |
| T-quick-g48-02 | D | `weatherbot/strategy.py` | mitigate | 明确禁止触碰 `evaluate_no_candidate()` 与 NO sizing，防止 quick 修复扩散成其它腿的策略回归 |
| T-quick-g48-03 | R | `tests/test_phase2_strategies.py` | mitigate | 通过 `import bot_v2` 跑回归，锁定兼容入口下的真实行为，而不是只测私有 helper |
| T-quick-g48-04 | T | 工作区无关脏文件 | mitigate | 执行范围限定在 `weatherbot/strategy.py` 与 `tests/test_phase2_strategies.py`，验证命令也只针对相关测试文件 |
</threat_model>

<verification>
- `uv run pytest tests/test_phase2_strategies.py -q -k "peak_window"`
- `uv run pytest tests/test_phase2_strategies.py -q`
- spot check：`weatherbot/strategy.py` 仅有 YES peak-window 相关改动，没有触碰 NO candidate 逻辑或无关配置
</verification>

<success_criteria>
- 同日/本地下午/高 METAR 的高温桶 YES 候选会稳定触发 penalty 或拒绝。
- 非同日市场与本地上午场景不会被误罚。
- 全部改动限制在 `weatherbot/strategy.py` 与 `tests/test_phase2_strategies.py`，无关脏文件保持原样。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-g48-weatherbot-yes/260418-g48-SUMMARY.md`
</output>
