---
phase: quick-260418-g48-weatherbot-yes
plan: 01
subsystem: weatherbot-strategy
tags:
  - quick-task
  - yes-peak-window
  - regression-tests
requires:
  - QUICK-260418-G48
provides:
  - YES peak-window same-day guard hardening
  - YES-only peak-window regression coverage
affects:
  - weatherbot/strategy.py
  - tests/test_phase2_strategies.py
tech_stack:
  - Python
  - pytest
key_files:
  created: []
  modified:
    - weatherbot/strategy.py
    - tests/test_phase2_strategies.py
decisions:
  - Keep the quick fix scoped to YES peak-window filtering only.
  - Preserve bot_v2 compatibility-entrypoint regression coverage instead of rewriting imports.
metrics:
  completed_at: 2026-04-18T03:44:27Z
  task_commits: 2
---

# Phase quick-260418-g48 Plan 01: weatherbot yes peak-window Summary

本次 quick task 把 YES 峰值窗口过滤器收口为可交付状态：策略侧只做 same-day guard 的本地日期稳固化，测试侧补齐本地时区、same-day guard、YES-only 语义回归。

## Completed Tasks

| Task | Result | Commit |
|------|--------|--------|
| 1 | 规范 `assess_yes_peak_window_penalty()` 的 same-day 判定，允许 `market_date` 带 ISO 时间部分时仍按本地日期比较 | `c3c1c27` |
| 2 | 补齐 YES 峰值窗口回归测试，覆盖 local-time、non-same-day、YES-only 语义 | `0336807` |

## Decisions Made

- 继续只碰 `weatherbot/strategy.py` 与 `tests/test_phase2_strategies.py`，不扩散到 NO sizing / kelly / route 逻辑。
- 用 `str(market_date).split("T", 1)[0]` 做最小修复，避免把带时间的日期上下文误判成非同日市场。
- 通过 `import bot_v2` 的兼容入口锁测试，确保 quick 修复反映真实对外行为。

## Verification

- `uv run pytest tests/test_phase2_strategies.py -q -k "peak_window" --no-cov` ✅
- `uv run pytest tests/test_phase2_strategies.py -q --no-cov` ✅
- `uv run pytest tests/test_phase2_strategies.py -q -k "peak_window"` ⚠️ 目标断言通过，但被仓库现有全局 coverage fail-under=75 拦截
- `uv run pytest tests/test_phase2_strategies.py -q` ⚠️ 目标断言通过，但被仓库现有全局 coverage fail-under=75 拦截

## Deviations from Plan

None - 代码改动仍然只落在计划指定的两个文件内。

## Known Stubs

None.

## Threat Flags

None.

## Self-Check: PASSED

- Summary exists: `.planning/quick/260418-g48-weatherbot-yes/260418-g48-SUMMARY.md`
- Commit exists: `c3c1c27`
- Commit exists: `0336807`
