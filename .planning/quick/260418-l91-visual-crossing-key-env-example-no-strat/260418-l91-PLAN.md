---
phase: quick-260418-l91-visual-crossing-key-env-example-no-strat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/config.py
  - tests/test_modular_entrypoint.py
  - config.json
  - .env.example
  - README.md
  - .planning/quick/260418-l91-visual-crossing-key-env-example-no-strat/260418-l91-SUMMARY.md
autonomous: true
requirements:
  - QUICK-260418-L91
must_haves:
  truths:
    - "运行时 `vc_key` 默认走环境变量优先，不再要求把 Visual Crossing secret 提交进 `config.json`。"
    - "仓库内存在可提交的安全配置面：`config.json` 不含真实 key，且正式 `no_strategy` 组合固定为用户给定值。"
    - "使用者能按 `README.md` + `.env.example` 完成配置，且回归测试能证明 env 覆盖与默认配置仍然正常。"
  artifacts:
    - path: "weatherbot/config.py"
      provides: "JSON 配置加载 + Visual Crossing env 覆盖"
      contains: "load_config"
    - path: "tests/test_modular_entrypoint.py"
      provides: "env 优先级与默认配置回归"
      contains: "load_config"
    - path: "config.json"
      provides: "安全可提交的默认配置与正式 no_strategy 参数"
      contains: '"no_strategy"'
    - path: ".env.example"
      provides: "Visual Crossing 环境变量示例"
      contains: "VISUAL_CROSSING_KEY="
    - path: "README.md"
      provides: "env-first 配置说明"
      contains: "VISUAL_CROSSING_KEY"
  key_links:
    - from: ".env.example"
      to: "weatherbot/config.py"
      via: "VISUAL_CROSSING_KEY -> load_config() -> _cfg['vc_key']"
      pattern: "VISUAL_CROSSING_KEY|vc_key|load_config"
    - from: "weatherbot/config.py"
      to: "weatherbot/__init__.py"
      via: "load_config() 返回的 _cfg 驱动 VC_KEY 常量"
      pattern: "_cfg.get\('vc_key'"
    - from: "config.json"
      to: "weatherbot/__init__.py"
      via: "NO_STRATEGY 默认运行参数"
      pattern: "no_strategy|min_price|max_ask|min_probability|min_edge|max_size|min_size"
    - from: "README.md"
      to: ".env.example"
      via: "用户按示例创建本地 .env"
      pattern: "VISUAL_CROSSING_KEY|.env.example"
---

<objective>
把 Visual Crossing key 改成环境变量优先读取，补齐 `.env.example` 与 README 说明，并把仓库里的 `config.json` 收敛成可安全提交的正式 NO 策略组合。

Purpose: 既消除仓库里的 secret 风险，又把当前验证过的 NO 正式参数固化成可提交默认值，不改变现有 `weatherbot` 模块边界与运行方式。
Output: env-first 配置加载、可提交的安全 `config.json`、`.env.example`、README 更新、回归测试、执行总结。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@weatherbot/config.py
@weatherbot/__init__.py
@weatherbot/forecasts.py
@config.json
@README.md
@tests/test_modular_entrypoint.py
@.planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md
@.planning/quick/260418-kgq-weatherbot-no-max-ask-ask-0-99-edge-no/260418-kgq-SUMMARY.md

<interfaces>
From `weatherbot/config.py`:

```python
def load_config(config_path=CONFIG_FILE):
    with open(config_path, encoding="utf-8") as handle:
        return json.load(handle)
```

From `weatherbot/__init__.py`:

```python
_cfg = _config.load_config()
VC_KEY = _cfg.get("vc_key", "")
```

From `weatherbot/forecasts.py`:

```python
def get_actual_temp(city_slug, date_str):
    url = (
        "..."
        f"?unitGroup={vc_unit}&key={VC_KEY}&include=days&elements=tempmax"
    )
```

Known safe config target from quick history:

```json
"no_strategy": {
  "min_price": 0.80,
  "max_ask": 0.95,
  "min_probability": 0.90,
  "min_edge": 0.03,
  "max_size": 30.0,
  "min_size": 1.0
}
```

`.gitignore` already contains `.env`, so this quick task must NOT edit `.gitignore` unless new evidence appears.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 Visual Crossing key 加 env-first 配置加载并锁定回归</name>
  <files>weatherbot/config.py, tests/test_modular_entrypoint.py</files>
  <behavior>
    - Test 1: `load_config()` 读取临时 JSON 配置时，若设置 `VISUAL_CROSSING_KEY`，返回结果中的 `vc_key` 被环境变量覆盖。
    - Test 2: 未设置 `VISUAL_CROSSING_KEY` 时，`load_config()` 继续保持现有 JSON-only 回退行为。
    - Test 3: 默认仓库入口仍然能读到 `bot_v2._cfg`，不会因为 env 覆盖改坏模块化入口合同。
  </behavior>
  <action>在 `weatherbot/config.py` 内做最小改动：引入 `os.environ`，让 `load_config()` 先读 JSON，再以环境变量 `VISUAL_CROSSING_KEY` 覆盖 `vc_key`；不要新增 dotenv 依赖，不要改 `weatherbot/forecasts.py` 的消费方式，也不要把 env 解析扩散到其他配置字段。测试优先写在 `tests/test_modular_entrypoint.py`：使用临时配置文件 + `monkeypatch` 验证 env 覆盖与 JSON fallback，并保留现有 `load_config() == bot_v2._cfg` 入口约束。若需要 reload，请只对相关模块做最小 reload，不要扩大测试基建。</action>
  <verify>
    <automated>uv run pytest --no-cov tests/test_modular_entrypoint.py -x</automated>
  </verify>
  <done>`load_config()` 支持 `VISUAL_CROSSING_KEY` env-first 覆盖，且默认模块化入口与现有 JSON fallback 合同保持稳定。</done>
</task>

<task type="auto">
  <name>Task 2: 固化安全可提交配置面与使用示例</name>
  <files>config.json, .env.example, README.md</files>
  <action>把仓库根 `config.json` 改成安全可提交版本：移除真实 Visual Crossing secret，把 `vc_key` 设为安全占位值或空值（推荐空字符串，避免误用），并把 `no_strategy` 固化为用户确认的正式组合：`min_price=0.80`、`max_ask=0.95`、`min_probability=0.90`、`min_edge=0.03`、`max_size=30.0`、`min_size=1.0`，同时保留顶层 `no_kelly_fraction=1.5`。新增 `.env.example`，仅写 `VISUAL_CROSSING_KEY=` 示例，不写真实值。更新 `README.md` 的配置章节：明确 `vc_key` 现在由环境变量优先提供，`config.json` 中的 `vc_key` 仅作为回退/占位；说明复制 `.env.example` 到本地 `.env` 的步骤。基于现有证据，`.env` 已被 `.gitignore` 忽略，所以不要改 `.gitignore`。</action>
  <verify>
    <automated>uv run python -c "import json; from pathlib import Path; cfg=json.loads(Path('config.json').read_text(encoding='utf-8')); no=cfg['no_strategy']; assert cfg.get('vc_key','') in ('', 'YOUR_VISUAL_CROSSING_KEY'); assert cfg['no_kelly_fraction']==1.5; assert no['min_price']==0.80 and no['max_ask']==0.95 and no['min_probability']==0.90 and no['min_edge']==0.03 and no['max_size']==30.0 and no['min_size']==1.0; env_text=Path('.env.example').read_text(encoding='utf-8'); assert 'VISUAL_CROSSING_KEY=' in env_text; readme=Path('README.md').read_text(encoding='utf-8'); assert 'VISUAL_CROSSING_KEY' in readme and '.env.example' in readme"</automated>
  </verify>
  <done>仓库内的 `config.json` 已无真实 key 且 NO 正式参数已固定；`.env.example` 与 README 足以指导本地提供 Visual Crossing key。</done>
</task>

<task type="auto">
  <name>Task 3: 执行最终回归并产出无 secret 的 quick summary</name>
  <files>.planning/quick/260418-l91-visual-crossing-key-env-example-no-strat/260418-l91-SUMMARY.md</files>
  <action>运行本 quick 相关回归并写 summary：记录 env-first 方案、为什么不改 `.gitignore`（已有 `.env` ignore 证据）、正式 `no_strategy` 提交值、以及 secret 处理边界。summary 必须明确写出“仓库文件不含真实 key、真实 key 只通过本地环境变量提供”，但不得回显任何真实值。验证阶段增加 secret 泄漏检查：若执行环境里存在 `VISUAL_CROSSING_KEY`，确认它没有出现在 `config.json`、`.env.example`、`README.md`、测试文件和 summary 中。不要跑需要真实网络或真实 key 的扫描命令；这次 quick 的完成标准是配置/文档/测试闭环，而不是运行时行情验证。</action>
  <verify>
    <automated>uv run pytest -q && uv run python -c "import os; from pathlib import Path; files=['config.json','.env.example','README.md','tests/test_modular_entrypoint.py','.planning/quick/260418-l91-visual-crossing-key-env-example-no-strat/260418-l91-SUMMARY.md']; secret=os.getenv('VISUAL_CROSSING_KEY','').strip(); 
for file in files:
    text=Path(file).read_text(encoding='utf-8')
    assert 'F5X7LBGXYDYJHPV6X9SZJ4XGL' not in text
    if secret:
        assert secret not in text
summary=Path('.planning/quick/260418-l91-visual-crossing-key-env-example-no-strat/260418-l91-SUMMARY.md').read_text(encoding='utf-8'); assert 'VISUAL_CROSSING_KEY' in summary and 'no_strategy' in summary and '.gitignore' in summary"</automated>
  </verify>
  <done>全量测试通过，summary 已落盘，且仓库相关文件与 summary 都没有泄漏真实 Visual Crossing key。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local environment → `load_config()` | 本地 secret 从环境变量注入运行时配置 |
| committed `config.json` → runtime `_cfg` | 仓库默认配置进入策略与数据拉取路径 |
| docs/examples → user local setup | README 与 `.env.example` 指导用户创建本地 secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-l91-01 | I | `config.json` / docs / summary | mitigate | 移除仓库内真实 `vc_key`，使用 `VISUAL_CROSSING_KEY` env-first，并在验证中扫描关键文件防止 secret 泄漏。 |
| T-quick-l91-02 | T | `weatherbot/config.py` | mitigate | 仅覆盖 `vc_key` 一个字段，不扩散到其他配置；通过单元测试锁定 env override + JSON fallback 行为。 |
| T-quick-l91-03 | R | `README.md` / `.env.example` | mitigate | README 明确 env-first 与 fallback 语义，`.env.example` 提供唯一示例来源，避免用户根据旧 `vc_key` 说明误配。 |
| T-quick-l91-04 | D | repo hygiene | accept | `.env` 已在 `.gitignore` 中，无需新增 ignore 规则；保持改动范围只限配置、文档、测试，避免 quick task 扩散。 |
</threat_model>

<verification>
- `uv run pytest --no-cov tests/test_modular_entrypoint.py -x`
- `uv run pytest -q`
- 只读脚本确认 `config.json` 已是安全默认值、`.env.example`/README 已同步、且关键文件不包含真实 key
</verification>

<success_criteria>
- `weatherbot/config.py` 支持 `VISUAL_CROSSING_KEY` env-first 覆盖，现有入口行为不破
- 仓库里的 `config.json` 不再包含真实 Visual Crossing key，且正式 `no_strategy` 组合已固定为用户指定值
- `.env.example` 与 README 明确指导本地配置，不需要再把 secret 写进 git
- 回归测试与 summary 均证明没有 secret 泄漏，且 `.gitignore` 因已有 `.env` 保护而保持不变
</success_criteria>

<output>
After completion, create `.planning/quick/260418-l91-visual-crossing-key-env-example-no-strat/260418-l91-SUMMARY.md`
</output>
