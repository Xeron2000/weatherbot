---
phase: quick-260418-l91-visual-crossing-key-env-example-no-strat
plan: 01
subsystem: config
tags: [weatherbot, config, secrets, env-first, no-strategy]
requires: []
provides:
  - Visual Crossing env-first config loading
  - safe committed config.json defaults
  - local .env setup example and docs
affects: [weatherbot/config.py, tests/test_modular_entrypoint.py, config.json, .env.example, README.md]
tech-stack:
  added: []
  patterns: [env-first secret injection, committed-safe config defaults, regression-locked config loader]
key-files:
  created:
    - .env.example
    - .planning/quick/260418-l91-visual-crossing-key-env-example-no-strat/260418-l91-SUMMARY.md
  modified:
    - weatherbot/config.py
    - tests/test_modular_entrypoint.py
    - config.json
    - README.md
key-decisions:
  - "`load_config()` 只对 `vc_key` 应用 `VISUAL_CROSSING_KEY` 覆盖，不扩散到其他配置字段。"
  - "仓库提交面保留空 `vc_key`，真实 Visual Crossing key 只允许通过本地环境变量提供。"
  - "正式 `no_strategy` 提交值固定为 min_price=0.80、max_ask=0.95、min_probability=0.90、min_edge=0.03、max_size=30.0、min_size=1.0，并保留 no_kelly_fraction=1.5。"
requirements-completed: [QUICK-260418-L91]
duration: 10min
completed: 2026-04-18
---

# Quick Task 260418-l91 Summary

**把 Visual Crossing secret 改成 env-first 注入，并把仓库默认配置收敛成可安全提交的 NO 正式参数面。**

## Performance

- **Completed:** 2026-04-18T07:24:53Z
- **Tasks:** 3
- **Files modified:** 5 repo files + 1 summary

## Accomplishments

- 在 `weatherbot/config.py` 增加 `VISUAL_CROSSING_KEY` → `vc_key` 的 env-first 覆盖，并保留 JSON fallback。
- 在 `tests/test_modular_entrypoint.py` 锁定 env 覆盖、JSON fallback、以及 `load_config() == bot_v2._cfg` 的入口合同。
- 把 `config.json` 改成安全可提交版本，新增 `.env.example`，并在 `README.md` 写清本地 `.env` 配置步骤。

## Task Commits

1. **Task 1 RED: env override failing regression** - `46e7ce7` (`test`)
2. **Task 1 GREEN: env-first loader fix** - `9b2bf50` (`fix`)
3. **Task 2: safe committed config and docs** - `f49ddd1` (`chore`)

_Note: summary、PLAN、STATE 未提交；本 quick 没有改动 `.gitignore`。_

## Files Created/Modified

- `weatherbot/config.py` - 读取 JSON 后只对 `vc_key` 应用 `VISUAL_CROSSING_KEY` 覆盖。
- `tests/test_modular_entrypoint.py` - 新增 env override 与 JSON fallback 回归，保留默认入口合同校验。
- `config.json` - 清空已提交 `vc_key`，保留 `no_kelly_fraction=1.5` 与正式 `no_strategy` 组合。
- `.env.example` - 提供 `VISUAL_CROSSING_KEY=` 本地示例，不含真实值。
- `README.md` - 明确 env-first 语义、`.env.example` 复制步骤、以及 `config.json.vc_key` 仅作回退/占位。

## Secret Handling Boundary

- 仓库文件不含真实 Visual Crossing key。
- 真实 key 只通过本地环境变量 `VISUAL_CROSSING_KEY` 提供。
- `config.json` 提交值中的 `vc_key` 已收敛为空字符串，避免误把 secret 带入 git 历史。

## `.gitignore` Evidence

- 未修改 `.gitignore`。
- 证据：`.gitignore:137-139` 已包含 `.env` 与 `.envrc`，满足本 quick 的本地 secret 隔离要求。

## Decisions Made

- 不引入 dotenv 依赖；继续使用现有 JSON + `os.environ` 的最小方案。
- 不修改 `weatherbot/forecasts.py` 消费方式；继续让运行时读取 `VC_KEY = _cfg.get("vc_key", "")`。
- 不改变已验证的正式 NO 参数提交面。

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- 通过：`uv run pytest --no-cov tests/test_modular_entrypoint.py -x`
- 通过：`uv run python -c "import json; from pathlib import Path; cfg=json.loads(Path('config.json').read_text(encoding='utf-8')); no=cfg['no_strategy']; assert cfg.get('vc_key','') in ('', 'YOUR_VISUAL_CROSSING_KEY'); assert cfg['no_kelly_fraction']==1.5; assert no['min_price']==0.80 and no['max_ask']==0.95 and no['min_probability']==0.90 and no['min_edge']==0.03 and no['max_size']==30.0 and no['min_size']==1.0; env_text=Path('.env.example').read_text(encoding='utf-8'); assert 'VISUAL_CROSSING_KEY=' in env_text; readme=Path('README.md').read_text(encoding='utf-8'); assert 'VISUAL_CROSSING_KEY' in readme and '.env.example' in readme"`
- 通过：`uv run pytest -q`
- 通过：secret leak-check 脚本确认 `config.json`、`.env.example`、`README.md`、测试文件和本 summary 均未包含环境中的真实 VC key，且 summary 保留 `VISUAL_CROSSING_KEY` / `no_strategy` / `.gitignore` 说明。

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: `.planning/quick/260418-l91-visual-crossing-key-env-example-no-strat/260418-l91-SUMMARY.md`
- FOUND: `46e7ce7`
- FOUND: `9b2bf50`
- FOUND: `f49ddd1`
