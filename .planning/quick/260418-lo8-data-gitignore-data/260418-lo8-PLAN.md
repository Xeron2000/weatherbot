---
phase: quick-260418-lo8-data-gitignore-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/quick/260418-lo8-data-gitignore-data/260418-lo8-SUMMARY.md
autonomous: true
requirements:
  - QUICK-260418-LO8
must_haves:
  truths:
    - "运行时 `data/` 目录不再以未跟踪文件污染 git 状态。"
    - "本地已有 `data/` JSON 内容被完整保留，不发生删除或覆盖。"
    - "执行记录明确说明这是运行时产物忽略规则，而不是业务数据清理。"
  artifacts:
    - path: ".gitignore"
      provides: "runtime data ignore rule"
      contains: "data/"
    - path: ".planning/quick/260418-lo8-data-gitignore-data/260418-lo8-SUMMARY.md"
      provides: "变更结果与保留本地数据的说明"
      contains: "data/"
  key_links:
    - from: ".gitignore"
      to: "git status"
      via: "ignore rule converts runtime data files from ?? noise to ignored state"
      pattern: "(^|/)data/"
    - from: "data/state.json"
      to: ".planning/quick/260418-lo8-data-gitignore-data/260418-lo8-SUMMARY.md"
      via: "summary documents data preserved locally while git noise is removed"
      pattern: "保留|未删除|data/"
---

<objective>
把运行时 `data/` 目录加入 `.gitignore`，并确认当前本地 `data/` 未跟踪文件从 git 噪音变为 ignored，同时不删除任何本地运行数据。

Purpose: 让本地 JSON 状态继续服务 CLI/runtime，但不再污染仓库的日常 `git status`。
Output: `.gitignore` ignore 规则、git 状态验证、quick summary。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.gitignore
@data/state.json

<interfaces>
Current local runtime evidence:

```text
data/
├── markets/
└── state.json
```

Current git evidence before execution:

```text
?? data/markets/...
?? data/state.json
```

Constraint: this quick task must solve git noise by ignore rules only; it must NOT delete or rewrite local `data/` contents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为运行时 data 目录添加最小 ignore 规则</name>
  <files>.gitignore</files>
  <action>仅在 `.gitignore` 中追加一条针对仓库根 `data/` 的忽略规则，放在与本地运行时/缓存类规则相邻的位置，保持现有注释风格；不要顺手整理其他 ignore 项，不要新增更宽泛的 `*.json` 之类规则，也不要改动 `data/` 目录内容。该规则的目标是覆盖 `data/state.json` 与 `data/markets/*.json` 这类运行时持久化产物。</action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; text=Path('.gitignore').read_text(encoding='utf-8'); assert 'data/' in text.splitlines(); assert sum(1 for line in text.splitlines() if line.strip()=='data/')==1"</automated>
  </verify>
  <done>`.gitignore` 存在且仅存在一条 `data/` ignore 规则，运行时目录被明确标记为本地产物。</done>
</task>

<task type="auto">
  <name>Task 2: 验证未跟踪 data 已转为 ignored 且本地文件保留</name>
  <files>.planning/quick/260418-lo8-data-gitignore-data/260418-lo8-SUMMARY.md</files>
  <action>执行 git 与文件系统验证，确认当前未跟踪的 `data/` 内容在新增 ignore 后不再以 `??` 出现在 `git status` 中，而是变成 ignored；同时确认 `data/state.json` 与 `data/markets/` 仍然存在，未被删除。把结果写入 summary，明确记录：本 quick 只解决 git 状态污染，不清理、不迁移、不删除本地运行时数据。若验证时发现 `data/` 已有 tracked 文件，summary 里要写明这是超出本 quick 既有事实的异常，不得对本地文件做删除操作。</action>
  <verify>
    <automated>git status --short --ignored --untracked-files=all -- data .gitignore && uv run python -c "from pathlib import Path; import subprocess; assert Path('data/state.json').exists(); assert Path('data/markets').is_dir(); out=subprocess.check_output(['git','status','--short','--ignored','--untracked-files=all','--','data'], text=True); assert '?? data/' not in out and '?? data/state.json' not in out; assert '!! data/' in out or '!! data/state.json' in out"</automated>
  </verify>
  <done>`git status` 不再把 `data/` 报成未跟踪噪音，本地运行时数据仍保留，summary 已说明边界与结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime process → local filesystem | 运行时会持续写入 `data/*.json` |
| local filesystem → git working tree | 本地运行时文件会影响开发者看到的仓库状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-lo8-01 | T | `.gitignore` | mitigate | 只新增精确的 `data/` 规则，不扩大到通配 JSON，避免误隐藏真实源码/配置文件。 |
| T-quick-lo8-02 | D | `git status` hygiene | mitigate | 用 ignore 规则把 runtime JSON 转为 ignored，消除日常状态噪音。 |
| T-quick-lo8-03 | I | local runtime data | mitigate | 验证 `data/state.json` 与 `data/markets/` 仍存在，明确禁止删除本地数据。 |
| T-quick-lo8-04 | R | quick summary | mitigate | 在 summary 写清“仅忽略、不删除”的执行边界，保留可审计记录。 |
</threat_model>

<verification>
- `uv run python -c "...assert 'data/' rule exists once..."`
- `git status --short --ignored --untracked-files=all -- data .gitignore`
- `uv run python -c "...assert data files still exist and status no longer shows ?? ..."`
</verification>

<success_criteria>
- `.gitignore` 新增最小范围的 `data/` 忽略规则
- 当前本地 `data/` 文件从未跟踪噪音变为 ignored，可继续本地使用
- 没有删除、覆盖或迁移任何 `data/` 内容
- quick summary 记录了验证结果与边界
</success_criteria>

<output>
After completion, create `.planning/quick/260418-lo8-data-gitignore-data/260418-lo8-SUMMARY.md`
</output>
