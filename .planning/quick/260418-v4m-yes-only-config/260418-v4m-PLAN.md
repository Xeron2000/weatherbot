---
phase: quick-260418-v4m-yes-only-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.json
  - weatherbot/config.py
  - weatherbot/__init__.py
  - tests/test_modular_entrypoint.py
  - tests/test_phase3_router.py
autonomous: true
requirements:
  - QUICK-260418-V4M
must_haves:
  truths:
    - "`load_config()` 完成 profile merge 后，活跃配置面只保留 YES 与共享执行参数，不再把 `no_strategy` 等 NO 字段带进运行时 merged config。"
    - "`VISUAL_CROSSING_KEY` 继续 env-first：环境变量存在时覆盖 JSON，环境变量不存在时仍回退到 `vc_key`。"
    - "`weatherbot` 运行时只对外暴露 YES / shared config 常量；导出的 `RISK_ROUTER` / `ORDER_POLICY` 也不再含 `no_budget_pct`、`no_leg_cap_pct`、`no_time_in_force` 这类 NO 配置键。"
  artifacts:
    - path: "config.json"
      provides: "YES-only 提交态 schema 与 profile 默认值"
      contains: '"yes_strategy"'
    - path: "weatherbot/config.py"
      provides: "profile merge + env-first VC + 兼容性配置加载"
      contains: "load_config"
    - path: "weatherbot/__init__.py"
      provides: "YES/shared runtime config exports（含收口后的 RISK_ROUTER / ORDER_POLICY）"
      contains: "YES_STRATEGY"
    - path: "tests/test_modular_entrypoint.py"
      provides: "merged config 与 runtime export 回归"
      contains: "load_config"
    - path: "tests/test_phase3_router.py"
      provides: "提交态 config schema 断言回归"
      contains: "config.json"
  key_links:
    - from: "config.json"
      to: "weatherbot/config.py"
      via: "strategy_profile -> load_config() deep merge"
      pattern: "strategy_profile|strategy_profiles|load_config"
    - from: "weatherbot/config.py"
      to: "weatherbot/__init__.py"
      via: "_cfg 驱动 YES/shared runtime constants，同时保留 env-first vc_key"
      pattern: "_cfg = _config.load_config\(|vc_key|YES_STRATEGY"
    - from: "weatherbot/__init__.py"
      to: "tests/test_modular_entrypoint.py"
      via: "reload runtime modules 后验证 NO config export 与 NO router/order policy keys 已移除"
      pattern: "NO_STRATEGY|YES_STRATEGY|VC_KEY|RISK_ROUTER|ORDER_POLICY"
---

<objective>
把 YES-only 拆分任务的第一步收口到配置 schema / profile merge / runtime config 导出层：删除提交态与 merged config 中的 NO 活跃配置面，同时保留 env-first `VISUAL_CROSSING_KEY` 行为。

Purpose: 先把“NO 还是活跃配置选项”这层语义从仓库默认配置和 runtime 导出面拿掉，为后续策略执行层与报告层删除 NO 逻辑留出干净边界。
Output: YES-only 的 `config.json` / profile merge、兼容的 `weatherbot/config.py`、收口后的 `weatherbot/__init__.py` 导出、对应回归测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260418-uag-no-yes-yes/260418-uag-PLAN.md
@config.json
@weatherbot/config.py
@weatherbot/__init__.py
@tests/test_modular_entrypoint.py
@tests/test_phase3_router.py

<interfaces>
From `weatherbot/config.py`:

```python
def load_config(config_path=None):
    ...
    profile_name = loaded.get("strategy_profile")
    ...
    env_vc_key = os.environ.get("VISUAL_CROSSING_KEY")
    if env_vc_key is not None:
        loaded["vc_key"] = env_vc_key
    return loaded
```

From `weatherbot/__init__.py`:

```python
_cfg = _config.load_config()
VC_KEY = _cfg.get("vc_key", "")
YES_STRATEGY = _cfg.get("yes_strategy", {...})
NO_STRATEGY = _cfg.get("no_strategy", {...})
RISK_ROUTER = _config.load_risk_router_config(_cfg)
ORDER_POLICY = _config.load_order_policy_config(_cfg)
```

Current constraint for this quick:
- 只能收口配置与导出层；不要改 `weatherbot/strategy.py`、`weatherbot/paper_execution.py`、`weatherbot/reporting.py`。
- 因后续 runtime 仍可能有内部 NO 逻辑，本 quick 允许 loader helper 为兼容保留私有/default fallback，但这些 fallback 不得继续来自提交态 schema、profile 或公共 runtime config export。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收口提交态 YES-only schema 与 profile merge 回归</name>
  <files>config.json, weatherbot/config.py, tests/test_modular_entrypoint.py, tests/test_phase3_router.py</files>
  <behavior>
    - Test 1: `config.json` 顶层与每个 `strategy_profiles[*]` 里都不再提交 `no_strategy`、`no_kelly_fraction`、`risk_router.no_budget_pct`、`risk_router.no_leg_cap_pct`、`order_policy.no_time_in_force`。
    - Test 2: `load_config()` merge 选中 profile 后，返回结果仍保留 `balance`、`yes_strategy`、共享 `risk_router` / `order_policy` / `paper_execution`，且 `VISUAL_CROSSING_KEY` 继续覆盖 `vc_key`。
    - Test 3: legacy JSON-only case 仍可工作；如果当前 runtime helper 还需要 NO 默认值，必须由 `weatherbot/config.py` 私有 fallback 提供，而不是由提交态 schema/profile 提供。
  </behavior>
  <action>先改测试，再改实现。把 `config.json` 的顶层默认档与全部 `strategy_profiles` 收口成 YES-only 配置面：删除 `no_strategy`、`no_kelly_fraction`、`risk_router.no_budget_pct`、`risk_router.no_leg_cap_pct`、`order_policy.no_time_in_force`，只保留 YES 参数和共享执行参数。同步更新 `tests/test_modular_entrypoint.py` 与 `tests/test_phase3_router.py`：把断言改成“提交态 schema 不再含 NO 活跃字段，merge 结果仍保留 env-first VC 与 YES/shared 参数”；不要把范围扩散到策略执行逻辑。随后最小修改 `weatherbot/config.py`：`load_config()` 继续做 deep merge + env-first `VISUAL_CROSSING_KEY`，并在必要时仅于 loader/helper 内部补兼容默认值，避免后续未改的 runtime 因缺 NO key 直接崩掉；但 `load_config()` 返回的 merged config 不得重新注入被删除的 NO 活跃字段。不要新增 dotenv/新依赖，不要修改任何策略、订单或报告模块。</action>
  <verify>
    <automated>uv run pytest --no-cov tests/test_modular_entrypoint.py tests/test_phase3_router.py -x</automated>
  </verify>
  <done>仓库提交态与 profile merge 已变成 YES-only 配置面；env-first `VISUAL_CROSSING_KEY` 行为保持不变；相关测试已锁定新合同。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 删除公共 runtime NO 配置导出面但不碰内部策略实现</name>
  <files>weatherbot/__init__.py, tests/test_modular_entrypoint.py, tests/test_phase3_router.py</files>
  <behavior>
    - Test 1: reload `weatherbot` / `bot_v2` 后，公共 config export 仍包含 `BALANCE`、`VC_KEY`、`YES_STRATEGY`、`RISK_ROUTER`、`ORDER_POLICY`、`PAPER_EXECUTION`。
    - Test 2: reload 后不再把 `NO_STRATEGY` 作为公共 runtime export surface 暴露给调用方。
    - Test 3: reload 后导出的 `RISK_ROUTER` / `ORDER_POLICY` 不再包含 `no_budget_pct`、`no_leg_cap_pct`、`no_time_in_force`。
    - Test 4: 本 quick 不删除 `evaluate_no_candidate` 等执行层函数合同；测试只约束配置常量导出面，避免越界到后续 quick 的代码删除工作。
  </behavior>
  <action>在 `weatherbot/__init__.py` 做外科式收口：删除 `NO_STRATEGY` 这一类 config-derived 公共常量的导出面，不再从 `_cfg` 把 NO 当活跃 runtime config 公开出去；同步确保导出的 `RISK_ROUTER` / `ORDER_POLICY` 已经通过 `weatherbot/config.py` 的 loader 合同收口，不再包含 `no_budget_pct`、`no_leg_cap_pct`、`no_time_in_force`。同步清理 `__all__` 与相关 reload 断言。保持 `YES_STRATEGY`、`RISK_ROUTER`、`ORDER_POLICY`、`PAPER_EXECUTION`、`VC_KEY` 的现有入口合同稳定。不要顺手删除策略/订单/报告函数包装；如果当前模块同步机制还需要私有兼容值来避免未改 runtime 直接 import 失败，可保留为私有实现细节，但不能继续作为公共配置面或测试合同出现。同步更新 `tests/test_modular_entrypoint.py` 与 `tests/test_phase3_router.py`：只验证 YES/shared exports 与 VC env-first 行为，不再锁定 `bot_v2.NO_STRATEGY` 或 `no_*` 导出。</action>
  <verify>
    <automated>uv run pytest --no-cov tests/test_modular_entrypoint.py tests/test_phase3_router.py -x</automated>
  </verify>
  <done>`weatherbot` runtime 对外只暴露 YES/shared config 常量；`NO_STRATEGY` 不再是公共导出面；未改的内部执行模块仍能继续被后续 quick 接手。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `config.json` → runtime merge | 提交态 schema 会直接决定 `_cfg` 是否继续把 NO 当活跃配置读入 |
| environment → `load_config()` | `VISUAL_CROSSING_KEY` 从本地环境进入 runtime，需要保持 env-first 且不回退成提交 secret |
| `weatherbot/__init__.py` → callers/tests | 公共导出面会决定调用方是否继续把 NO 当官方支持配置面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-v4m-01 | T | `config.json` / profiles | mitigate | 删除提交态与 profile 中的 NO 活跃字段，并用测试锁定 YES-only schema。 |
| T-quick-v4m-02 | I | `weatherbot/config.py` | mitigate | 保留 env-first `VISUAL_CROSSING_KEY`，禁止把真实 secret 或新的 env 解析扩散到其他字段。 |
| T-quick-v4m-03 | T | `weatherbot/__init__.py` | mitigate | 删除 `NO_STRATEGY` 公共导出，避免 bot 调用方继续把 NO 当公开配置合同。 |
| T-quick-v4m-04 | D | current runtime compatibility | accept | 允许 helper 内部做最小兼容 fallback，避免在未改策略层前因缺 NO key 直接 import 崩溃，但这些 fallback 不得回流到提交态 schema 或公共导出。 |
</threat_model>

<verification>
- `uv run pytest --no-cov tests/test_modular_entrypoint.py tests/test_phase3_router.py -x`
- 重点确认 `config.json` / `strategy_profiles[*]` 已无 NO 活跃字段，且 runtime reload 仍能读到 env-first VC 与 YES/shared exports。
</verification>

<success_criteria>
- 提交态 `config.json` 与全部 profile 不再把 NO 作为活跃配置面提交。
- `load_config()` merge 结果保留 YES/shared 参数与 env-first `VISUAL_CROSSING_KEY`，不重新带回 NO 活跃字段。
- `weatherbot` 公共 runtime config export 不再暴露 `NO_STRATEGY`，但本 quick 不越界修改策略/挂单/报告层实现。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-v4m-yes-only-config/260418-v4m-SUMMARY.md`
</output>
