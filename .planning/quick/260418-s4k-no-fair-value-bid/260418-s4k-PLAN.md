---
phase: quick-260418-s4k-no-fair-value-bid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/paper_execution.py
  - weatherbot/strategy.py
  - tests/test_strategy_paper_execution.py
  - tests/test_phase4_orders.py
autonomous: true
requirements:
  - QUICK-NO-FAIR-VALUE-BID
must_haves:
  truths:
    - "NO assessment 在 fair_no 很高且盘口极宽时，target price 不再跟随超低 bid，而是围绕 fair value 生成高位被动买价。"
    - "NO order intent 与 NO assessment 使用同一套 anchored passive bid 定价合同，不再出现评估价与挂单价分叉。"
    - "YES maker 定价路径保持当前 bid-improve 语义不变。"
    - "NO anchored price 仍遵守现有安全护栏：不穿 ask、按 tick size 对齐、缺 quote/tick 时返回稳定 reason。"
  artifacts:
    - path: weatherbot/paper_execution.py
      provides: "NO fair-value anchored passive bid helper 与 order intent wiring"
    - path: weatherbot/strategy.py
      provides: "NO assessment 改用 anchored passive bid 计算 edge/status/reasons"
    - path: tests/test_strategy_paper_execution.py
      provides: "NO assessment anchored pricing 回归测试"
    - path: tests/test_phase4_orders.py
      provides: "NO order intent anchored pricing 合同回归测试"
  key_links:
    - from: weatherbot/strategy.py
      to: weatherbot/paper_execution.py
      via: "NO assessment 调用与 order intent 相同的定价 helper"
      pattern: "evaluate_no_candidate.*fair.*anchor|build_passive_order_intent.*fair"
    - from: tests/test_strategy_paper_execution.py
      to: weatherbot/strategy.py
      via: "wide-spread NO quote + high fair_no 回归"
      pattern: "fair_value|anchored|wide"
    - from: tests/test_phase4_orders.py
      to: weatherbot/paper_execution.py
      via: "NO order intent limit_price 与 assessment 合同一致"
      pattern: "limit_price"
---

<objective>
把 NO 被动挂单 target 从“跟着当前 bid improve”改成“围绕 fair_no 的 anchored passive bid”，并让 assessment 与 order intent 共用同一合同；YES 保持现状。

Purpose: 修正 `bid≈0.001, ask≈0.999, fair_no≈0.95` 这类宽价差市场里 NO 挂单过低、无法体现真实优势的问题。
Output: 一个 NO-only 定价合同、最小 wiring 改动、覆盖 assessment 与 order intent 的回归测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260418-q1n-no-passive-bid/260418-q1n-SUMMARY.md
@weatherbot/strategy.py
@weatherbot/paper_execution.py
@tests/test_strategy_paper_execution.py
@tests/test_phase4_orders.py

<interfaces>
From `weatherbot/paper_execution.py`:
```python
def compute_passive_limit_price(side_quote, policy):
    ...

def build_passive_order_intent(market, reservation, assessment, quote_snapshot, now_ts):
    ...
```

From `weatherbot/strategy.py`:
```python
def evaluate_no_candidate(bucket_probability, quote_snapshot, hours):
    ...

def build_candidate_assessments(bucket_probabilities, quote_snapshot, hours, market_context=None):
    ...
```

已有约束：
- 继续保留 `ask_above_max` 作为 NO ceiling 护栏。
- YES 仍走现有 `compute_passive_limit_price()`，本次不要改 YES 任何 status / edge / limit_price 语义。
- 仅改 NO maker target 生成与对应测试，不重构订单引擎、risk router、paper fill 逻辑。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 NO fair-value anchored passive bid 的回归合同</name>
  <files>tests/test_strategy_paper_execution.py, tests/test_phase4_orders.py</files>
  <behavior>
    - Test 1: NO assessment 在 `fair_no=0.95, bid≈0.001, ask≈0.999, tick=0.01` 时，不再产出接近 0 的 maker target；应落在按 fair value 折价后的 0.8x 档位，并保持 `status="accepted"` 或按 `min_price` 正确 `reprice`。
    - Test 2: NO order intent 使用与 assessment 同一 anchored price 合同，`limit_price` 与 assessment 计算一致。
    - Test 3: YES 现有 deterministic order / assessment 断言保持不变，证明本次改动不波及 YES。
  </behavior>
  <action>在两份测试文件中新增/改写 NO-only 场景：显式覆盖极宽 spread (`bid=0.001, ask=0.999`) 与高 `fair_no`；把预期锚定为“按 `fair_no - 0.10` 先折价，再按 tick 向下对齐，并在 `ask - tick_size` 之下截断”的 anchored passive bid 合同。先只改测试，不改生产代码；让失败直接暴露当前 NO 仍跟随 bid improve 的缺口。</action>
  <verify>
    <automated>uv run pytest -q --no-cov tests/test_strategy_paper_execution.py tests/test_phase4_orders.py</automated>
  </verify>
  <done>测试明确表达 NO anchored 定价合同，且在当前实现下因 NO 仍走 bid-improve 而失败；YES 相关既有断言保持通过。</done>
</task>

<task type="auto">
  <name>Task 2: 在 paper_execution 落地 NO-only anchored pricing helper 并接入 order intent</name>
  <files>weatherbot/paper_execution.py, tests/test_phase4_orders.py</files>
  <action>在 `weatherbot/paper_execution.py` 增加 NO 专用 helper，避免改坏 YES 共享路径：输入 `side_quote`, `fair_no`, `policy`，按 `anchored_target = fair_no - 0.10` 生成基准价，再向下按 `tick_size` 对齐；如果 `ask > bid`，则最终价格必须 `<= ask - tick_size`，绝不穿 ask；若缺 `tick_size`、quote 或 `fair_no`，继续返回稳定 reason。随后把 `build_passive_order_intent()` 的 NO 分支切到该 helper，YES 仍保留现有 `compute_passive_limit_price()` 调用与 TIF 语义。</action>
  <verify>
    <automated>uv run pytest -q --no-cov tests/test_phase4_orders.py</automated>
  </verify>
  <done>NO order intent 的 `limit_price` 不再由当前 bid improve 决定，而是由 fair-value anchored helper 决定；YES order intent 行为与现有测试完全一致。</done>
</task>

<task type="auto">
  <name>Task 3: 把 NO assessment 切到同一 anchored 合同并保持现有护栏语义</name>
  <files>weatherbot/strategy.py, weatherbot/paper_execution.py, tests/test_strategy_paper_execution.py</files>
  <action>在 `evaluate_no_candidate()` 中改用 Task 2 的 NO anchored helper，令 `missing_quote_price`、`price_below_min`、`edge` 与 maker target 全部基于同一 anchored 价格；继续只让 `ask_above_max` 读取 live ask ceiling，`probability_below_min` 继续看 `fair_no`，`status` 仍沿用 accepted / size_down / reprice / rejected 语义。不要调整 YES assessment、route schema、paper execution lifecycle 或任何 unrelated wiring。</action>
  <verify>
    <automated>uv run pytest -q --no-cov tests/test_strategy_paper_execution.py tests/test_phase4_orders.py</automated>
  </verify>
  <done>NO assessment 与 NO order intent 使用同一 anchored price 合同；宽 spread + 高 fair_no 场景会生成高位被动买价；YES 全路径断言保持原样通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assessment → order intent | 同一市场的 NO 定价事实从候选评估流入挂单生成，若合同分叉会导致错误下单。 |
| quote snapshot → pricing helper | 外部盘口数据可能缺 tick / 缺 bid/ask / spread 极宽，定价 helper 需要稳定降级。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260418-s4k-01 | T | `weatherbot/strategy.py` + `weatherbot/paper_execution.py` | mitigate | 让 NO assessment 与 order intent 共用同一 helper，消除价格语义漂移。 |
| T-quick-260418-s4k-02 | D | `weatherbot/paper_execution.py` | mitigate | 对缺 `tick_size` / quote / `fair_no` 返回 deterministic reason，而不是生成无效价格。 |
| T-quick-260418-s4k-03 | T | NO anchored pricing | mitigate | 保留 `ask - tick_size` ceiling 与 tick 对齐，避免 anchored bid 穿 ask 或产生非法精度。 |
| T-quick-260418-s4k-04 | E | YES pricing path | accept | 本任务明确 NO-only；通过现有 YES 回归断言证明未扩大变更面。 |
</threat_model>

<verification>
- 运行 `uv run pytest -q --no-cov tests/test_strategy_paper_execution.py tests/test_phase4_orders.py`
- 重点确认：NO wide-spread anchored case 通过；YES deterministic order / assessment case 仍通过。
</verification>

<success_criteria>
- `weatherbot/paper_execution.py` 存在 NO-only anchored pricing helper，YES 仍走旧 helper。
- `weatherbot/strategy.py` 的 NO assessment 不再把超低 bid 直接变成超低 target price。
- 两份测试文件都覆盖 NO anchored 合同，且 full targeted pytest 通过。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-s4k-no-fair-value-bid/260418-s4k-SUMMARY.md`
</output>
