---
phase: quick-260418-he3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/strategy.py
  - tests/test_phase2_strategies.py
  - tests/test_strategy_paper_execution.py
autonomous: true
requirements:
  - QUICK-NO-PRICE-SOURCE
must_haves:
  truths:
    - "NO 候选不再因为 no.bid 极低而被误判为 price_below_min"
    - "NO 候选的 fair/edge 计算与实际可成交入场价语义一致"
    - "回归测试和一轮真实扫描都能证明扫描结果已从误判中恢复"
  artifacts:
    - path: "weatherbot/strategy.py"
      provides: "NO 候选取价与 edge 计算修复"
    - path: "tests/test_phase2_strategies.py"
      provides: "NO 候选取价回归测试"
    - path: "tests/test_strategy_paper_execution.py"
      provides: "候选到 paper execution 的语义护栏"
  key_links:
    - from: "weatherbot/strategy.py"
      to: "quote_snapshot[].no.ask"
      via: "evaluate_no_candidate"
      pattern: "quote\.get\(\"ask\"\)"
    - from: "tests/test_phase2_strategies.py"
      to: "weatherbot/strategy.py"
      via: "低 no.bid / 高 no.ask 回归样例"
      pattern: "price_below_min"
    - from: "实际扫描命令"
      to: "data/markets/*.json 或 status 输出"
      via: "scan_and_update 后复查 candidate_assessments"
      pattern: "NO_CARRY"
---

<objective>
修复 weatherbot 的 NO 候选取价逻辑，只改候选评估这一层，避免 `no.bid` 极低时把本应可做的 NO 候选错误打成 `price_below_min`。

Purpose: 恢复 NO 策略筛选的真实语义，让扫描结果能反映可执行的 NO 机会，而不是被订单簿 bid 噪音系统性压扁。
Output: 一处策略修复、对应回归测试、以及一轮真实扫描后的状态复查记录。
</objective>

<execution_context>
@/home/xeron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/xeron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@README.md
@weatherbot/strategy.py
@weatherbot/polymarket.py
@tests/test_phase2_strategies.py
@tests/test_strategy_paper_execution.py

<interfaces>
From weatherbot/strategy.py:
```python
def evaluate_no_candidate(bucket_probability, quote_snapshot, hours):
    ...
```

Current behavior to replace:
```python
bid = quote.get("bid")
if bid is not None and bid < NO_STRATEGY.get("min_price", 0.0):
    reasons.append("price_below_min")
edge = round((bid or 0.0) - (fair_price or 0.0), 6) if bid is not None else None
```

Available quote shape from weatherbot/polymarket.py:
```python
{
    "no": {
        "bid": float | None,
        "ask": float | None,
        "spread": float | None,
        "tick_size": float | None,
        "min_order_size": float | None,
        "book_ok": bool,
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补 NO 取价回归测试，锁定 ask 语义</name>
  <files>tests/test_phase2_strategies.py, tests/test_strategy_paper_execution.py</files>
  <behavior>
    - Test 1: 当 `fair_no` 很高、`no.bid` 极低、`no.ask` 满足 `min_price` 时，NO 候选不应再出现 `price_below_min`。
    - Test 2: 当 `no.ask` 低于 `min_price` 时，仍应保持 `reprice` / `price_below_min` 语义。
    - Test 3: `build_candidate_assessments()` 产出的 NO assessment 在低 bid / 高 ask 场景下仍可被后续执行层消费，不因旧语义回退。
  </behavior>
  <action>在 `tests/test_phase2_strategies.py` 增加最小回归样例：显式构造 `no.bid=0.01`、`no.ask=0.99`、`fair_no≈0.95-1.0` 的 quote，先让现状失败，再锁定 NO leg 必须基于可成交入场价判断 `min_price` 与 edge。必要时在 `tests/test_strategy_paper_execution.py` 增加一条跨层护栏，确认候选评估修复后不会破坏既有 `NO_CARRY` assessment / paper execution 语义。不要改动 YES 逻辑，不要扩展到路由、订单生命周期或无关配置。</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py</automated>
  </verify>
  <done>测试中明确覆盖“低 no.bid / 高 no.ask”误判场景，并且至少一条断言能在修复前失败、修复后通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 只修 NO 候选评估层的取价与 edge 逻辑</name>
  <files>weatherbot/strategy.py</files>
  <behavior>
    - Test 1: `evaluate_no_candidate()` 使用 NO 侧可执行入场价，而不是 `no.bid`，来判断 `missing_quote_price`、`price_below_min`、`edge`。
    - Test 2: `quote_context` 仍保留原始 bid/ask，便于报告和下游执行读取，不破坏现有快照结构。
  </behavior>
  <action>在 `weatherbot/strategy.py` 中最小修改 `evaluate_no_candidate()`：将 NO leg 的价格闸门与 edge 计算改为使用 `quote_context["ask"]` 这一可成交买入价，与 YES leg 按 ask 买入的语义保持一致；`fair_no` 继续来自 `bucket_probability`，edge 应表达为 `fair_no - no.ask`，而不是 `no.bid - fair_no`。保留 `quote_context` 原样，不新增新的配置项、不重写 `quote_snapshot` 结构、不改 `build_quote_snapshot()`、不改 routing / paper execution 逻辑。</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py</automated>
  </verify>
  <done>`evaluate_no_candidate()` 不再因极低 `no.bid` 触发系统性误判；NO leg 仍只在 ask 真低于 `min_price` 或其他既有拒绝条件命中时才被 `reprice`/`rejected`。</done>
</task>

<task type="auto">
  <name>Task 3: 复跑一轮真实扫描并复查 NO 候选状态</name>
  <files>weatherbot/strategy.py</files>
  <action>在代码修复和回归测试通过后，执行一轮真实扫描（不要启动无限循环），建议使用 `uv run python -c "import bot_v2; bot_v2.scan_and_update()"`；随后用 `uv run python bot_v2.py status` 和/或直接检查新写入的 `data/markets/*.json`，确认至少一个 `candidate_assessments` 中的 `NO_CARRY` 结果不再因为 `no.bid` 极低而批量落成 `price_below_min`。如果扫描产生的新事实仍显示普遍 `price_below_min`，必须记录是 ask 本身过低还是其他 stop reason，而不是旧 bug 复发。严禁编辑、暂存或回滚 `.planning/config.json` 与 `.planning/phases/04-执行paper订单生命周期与状态恢复/04-VERIFICATION.md` 这两个无关脏文件。</action>
  <verify>
    <automated>uv run python -c "import bot_v2; bot_v2.scan_and_update()" && uv run python bot_v2.py status</automated>
  </verify>
  <done>完成一轮真实扫描复查，并能用 status 输出或 market JSON 证明：NO 候选状态变化来自 ask / 概率 / execution stop reasons 的真实约束，而不是 `no.bid` 误判。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Polymarket CLOB → strategy evaluator | 外部 order book 数据进入候选评估，错误字段选择会直接污染交易决策 |
| strategy evaluator → persisted market JSON | 评估结果会写入 `candidate_assessments` 并被 status / execution 继续消费 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-he3-01 | T | `weatherbot/strategy.py:evaluate_no_candidate` | mitigate | 固定使用 `quote_context.no.ask` 作为 NO 入场价格来源，并用回归测试锁定低 bid / 高 ask 场景 |
| T-quick-he3-02 | I | `data/markets/*.json` candidate assessments | mitigate | 通过真实扫描后复查 persisted `NO_CARRY` assessment，确认落盘原因码来自 ask/probability/stop reasons 而非旧 bug |
| T-quick-he3-03 | D | 实盘式长循环误启动 | mitigate | 验证阶段只运行单次 `scan_and_update()`，避免 `python bot_v2.py` 进入无限 loop 干扰状态 |
</threat_model>

<verification>
- `uv run pytest -q tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py`
- `uv run python -c "import bot_v2; bot_v2.scan_and_update()"`
- `uv run python bot_v2.py status`
</verification>

<success_criteria>
- NO 候选不再以 `no.bid` 作为 `min_price` / edge 判断依据。
- 目标回归测试覆盖低 bid / 高 ask 场景并稳定通过。
- 一轮真实扫描后，状态复查能看到 `NO_CARRY` 不再被旧 bug 批量压成 `price_below_min`。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-he3-weatherbot-no-no-bid-price-below-min/260418-he3-SUMMARY.md`
</output>
