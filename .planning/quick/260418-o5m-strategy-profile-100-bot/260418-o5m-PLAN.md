---
phase: quick-260418-o5m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.json
  - docs/strategy-profile-playbook.md
autonomous: true
requirements:
  - QUICK-260418-O5M
must_haves:
  truths:
    - "默认 strategy_profile 已切到 100，运行时 merge 后会按 100 档阈值与风控执行。"
    - "操作者能用一份简明检查清单观察切换后 bot 是否出现预期的小资金行为变化。"
    - "切换后至少有一次自动化 smoke 校验，证明配置文件与 merge 结果没有停留在旧的 1000 档。"
  artifacts:
    - path: "config.json"
      provides: "默认 strategy_profile 切换到 100 档"
      min_lines: 60
    - path: "docs/strategy-profile-playbook.md"
      provides: "与 100 档默认选择一致的最小说明和观察清单"
      min_lines: 110
  key_links:
    - from: "config.json"
      to: "weatherbot/config.py"
      via: "load_config() 按 strategy_profile 深度 merge preset"
      pattern: "strategy_profile|strategy_profiles|_deep_merge_dicts|load_config"
    - from: "docs/strategy-profile-playbook.md"
      to: "config.json"
      via: "引用 100 档关键阈值与观察点"
      pattern: "100|max_price|min_price|min_probability|max_order_hours_open|global_usage_cap_pct"
---

<objective>
把默认 `strategy_profile` 从 `1000` 切到 `100`，并补一份只面向切档后的简明观察清单。

Purpose: 用户已明确只有 100 刀资金，本次要让默认配置与资金规模一致，并让后续观察 bot 行为时有一套最小但可执行的检查标准。
Output: 更新后的 `config.json` 与最小改动版 `docs/strategy-profile-playbook.md`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@config.json
@docs/strategy-profile-playbook.md
@weatherbot/config.py

<interfaces>
From weatherbot/config.py:
```python
def load_config(config_path=None):
    with open(config_path, encoding="utf-8") as handle:
        loaded = json.load(handle)
    profile_name = loaded.get("strategy_profile")
    if profile_name is not None:
        profiles = loaded.get("strategy_profiles")
        loaded = _deep_merge_dicts(loaded, profiles[profile_name])
    return loaded
```

当前已知事实：
- 用户明确是小资金，目标是切到 `100` 档。
- `config.json` 已支持 `100` / `1000` / `10000` 三档。
- 现有手册已解释三档差异；本次不要重写整份文档，只把默认选择与切换后观察清单改到位。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 切换默认 strategy profile 到 100</name>
  <files>config.json</files>
  <action>仅修改 `config.json` 顶层默认选择，把 `strategy_profile` 从 `1000` 改成 `100`，其余 preset 内容保持不动。不要顺手重写 `strategy_profiles` 三档参数，不要手工把顶层 `yes_strategy`、`no_strategy`、`risk_router`、`order_policy`、`paper_execution` 改成另一套散装值；运行时应继续依赖 `weatherbot/config.py` 的深度 merge 语义来生效。此次切换的核心是“默认选择变更”，不是重新设计 profile。</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
import json
from pathlib import Path
cfg = json.loads(Path('config.json').read_text(encoding='utf-8'))
assert cfg['strategy_profile'] == '100'
assert set(cfg['strategy_profiles']) == {'100', '1000', '10000'}
print('ok')
PY</automated>
  </verify>
  <done>`config.json` 默认档位已改为 `100`，且三档 preset 本体未被意外改坏。</done>
</task>

<task type="auto">
  <name>Task 2: 最小更新手册中的默认说明与观察清单</name>
  <files>docs/strategy-profile-playbook.md</files>
  <action>对 `docs/strategy-profile-playbook.md` 做最小必要修改：把“当前推荐/默认”叙述从 `1000` 调整为本次用户明确选择的 `100`，并新增或改写一段“切到 100 后观察 bot 行为的检查清单”。清单必须简明、面向观察，不要重写整份三档说明；至少覆盖：1) merge 后关键代表值是否落到 `100` 档；2) YES 候选是否比 `1000` 更宽；3) NO 候选是否更容易进入候选池；4) 单市场/单事件占用是否更快打到小资金上限；5) 订单等待与取消节奏是否更短更快；6) 如果行为异常，先排查 profile 没切对而不是直接归因策略失效。避免把这次文档更新扩写成新的完整使用手册。</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path('docs/strategy-profile-playbook.md').read_text(encoding='utf-8')
required = [
    '100',
    '检查清单',
    'YES',
    'NO',
    'global_usage_cap_pct',
    'max_order_hours_open',
]
missing = [item for item in required if item not in text]
assert not missing, missing
print('ok')
PY</automated>
  </verify>
  <done>手册已与新的默认档位一致，且包含一份可直接拿来观察切档后 bot 行为的简明检查清单。</done>
</task>

<task type="auto">
  <name>Task 3: 做一次 merge 后 smoke 校验并固化观察基准</name>
  <files>config.json, docs/strategy-profile-playbook.md</files>
  <action>使用 `weatherbot.config.load_config()` 对当前 `config.json` 做一次 merge 后 smoke 校验，确认运行时看到的是 `100` 档关键值，而不是仍停留在旧的 `1000` 顶层默认。校验至少断言：`balance = 100.0`、YES `max_price = 0.08`、NO `min_price = 0.72`、`risk_router.global_usage_cap_pct = 0.92`、`order_policy.max_order_hours_open = 36.0`。如果手册中的观察清单引用这些代表值，也要与 smoke 校验保持一致。不要引入新的测试文件；本次只做轻量 smoke 验证。</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from weatherbot.config import load_config
cfg = load_config('config.json')
assert cfg['balance'] == 100.0
assert cfg['yes_strategy']['max_price'] == 0.08
assert cfg['no_strategy']['min_price'] == 0.72
assert cfg['risk_router']['global_usage_cap_pct'] == 0.92
assert cfg['order_policy']['max_order_hours_open'] == 36.0
print('ok')
PY</automated>
  </verify>
  <done>已证明运行时 merge 结果确实切到 `100` 档，且文档中给用户的观察基准与实际加载值一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → runtime merge | 默认档位切换会直接改变 bot 的候选、风控与挂单行为 |
| docs → operator interpretation | 文档检查清单会影响操作者对切档后行为是否正常的判断 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260418-o5m-01 | T | config.json | mitigate | 只改顶层 `strategy_profile`，并用 `load_config('config.json')` smoke 校验 merge 后关键值已经落到 `100` 档 |
| T-quick-260418-o5m-02 | T | docs/strategy-profile-playbook.md | mitigate | 文档只引用 `config.json` 当前 `100` 档真实代表值与观察项，不凭记忆重写三档参数 |
| T-quick-260418-o5m-03 | I | docs/strategy-profile-playbook.md | accept | 文档仅描述已提交策略阈值与观察建议，不新增 secret 或外部账户信息 |
| T-quick-260418-o5m-04 | E | docs/strategy-profile-playbook.md | mitigate | 明确提示异常时先排查 profile 是否切对与 merge 是否生效，避免把配置错误误判成策略问题 |
</threat_model>

<verification>
- 顶层 `strategy_profile` 已切到 `100`。
- 文档包含“切到 100 后观察 bot 行为的检查清单”。
- `weatherbot.config.load_config()` smoke 校验返回 `100` 档关键代表值。
</verification>

<success_criteria>
- 默认配置切到 `100`，满足用户 100 刀小资金使用场景。
- 文档只做最小必要更新，没有重写整份手册。
- 用户拿到一份简明检查清单，可直接观察切档后候选、风控和挂单节奏是否符合预期。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-o5m-strategy-profile-100-bot/260418-o5m-SUMMARY.md`
</output>
