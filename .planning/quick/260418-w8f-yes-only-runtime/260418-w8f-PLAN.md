---
phase: quick-260418-w8f-yes-only-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/strategy.py
  - weatherbot/paper_execution.py
  - weatherbot/reporting.py
  - tests/test_phase2_strategies.py
  - tests/test_strategy_paper_execution.py
  - tests/test_phase4_orders.py
  - tests/test_phase2_reporting.py
  - tests/test_phase3_reporting.py
  - tests/test_phase3_scan_loop.py
  - tests/test_phase4_reporting.py
  - tests/test_phase4_scan_loop.py
autonomous: true
requirements:
  - QUICK-260418-W8F
must_haves:
  truths:
    - "运行时扫描结果只生成 YES 候选、YES 路由和 YES 保留暴露；不再产生 NO_CARRY 活跃路径。"
    - "被动挂单与 paper execution 只接受 YES 订单意图，但保留 YES 订单生命周期、取消、过期、恢复与持仓落地。"
    - "status/report/replay 输出不再汇报 NO 活跃路径，同时继续展示 YES 扫描、订单和持仓事实。"
  artifacts:
    - path: weatherbot/strategy.py
      provides: YES-only candidate assessment, routing, scan-loop runtime wiring
    - path: weatherbot/paper_execution.py
      provides: YES-only passive order intent and shared order lifecycle wiring
    - path: weatherbot/reporting.py
      provides: YES-only runtime summaries and terminal order output
    - path: tests/test_phase2_strategies.py
      provides: YES-only strategy and scan-loop regression coverage
    - path: tests/test_strategy_paper_execution.py
      provides: YES-only strategy/paper execution contract coverage
    - path: tests/test_phase4_orders.py
      provides: YES-only order-intent regression coverage
    - path: tests/test_phase2_reporting.py
      provides: YES-only candidate reporting coverage
    - path: tests/test_phase3_reporting.py
      provides: YES-only route/risk reporting coverage
    - path: tests/test_phase3_scan_loop.py
      provides: YES-only routing and reservation coverage
    - path: tests/test_phase4_reporting.py
      provides: YES-only order lifecycle reporting coverage
    - path: tests/test_phase4_scan_loop.py
      provides: YES-only active-order lifecycle coverage
  key_links:
    - from: weatherbot/strategy.py
      to: weatherbot/paper_execution.py
      via: scan_and_update -> sync_market_order / reconcile_market_reservation
      pattern: "candidate_assessments|route_decisions|reserved_exposure"
    - from: weatherbot/paper_execution.py
      to: weatherbot/reporting.py
      via: active_order/order_history/paper_execution_state facts
      pattern: "active_order|order_history|execution_events"
    - from: weatherbot/reporting.py
      to: bot_v2 import surface
      via: print_status / print_report exercised through bot_v2 alias
      pattern: "print_status|print_report"
---

<objective>
删除 YES-only 第二步里残留的 NO 活跃运行时路径，只保留 YES 扫描、YES 挂单、YES 汇报链路。

Purpose: 把上一轮配置面收口延伸到真实运行时，避免系统继续生成/接受/展示 NO 活跃路径，同时不破坏 YES 峰值窗口过滤、订单生命周期和共享状态基础设施。
Output: 一套 YES-only runtime 实现与回归测试，覆盖 strategy / paper execution / reporting 三层。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/quick/260418-v4m-yes-only-config/260418-v4m-SUMMARY.md
@weatherbot/strategy.py
@weatherbot/paper_execution.py
@weatherbot/reporting.py
@bot_v2.py
@tests/test_phase2_strategies.py
@tests/test_strategy_paper_execution.py
@tests/test_phase4_orders.py
@tests/test_phase2_reporting.py
@tests/test_phase3_reporting.py
@tests/test_phase3_scan_loop.py
@tests/test_phase4_reporting.py
@tests/test_phase4_scan_loop.py

<interfaces>
From weatherbot/strategy.py:
```python
def build_candidate_assessments(bucket_probabilities, quote_snapshot, hours, market_context=None):
def reconcile_market_reservation(market, risk_state, router_cfg, reserved_at=None):
def scan_and_update():
def monitor_active_orders():
```

From weatherbot/paper_execution.py:
```python
def build_passive_order_intent(market, reservation, assessment, quote_snapshot, now_ts):
def sync_market_order(market, risk_state, forecast_snap, market_ready=True):
def restore_order_state_from_markets(markets):
```

From weatherbot/reporting.py:
```python
def print_status():
def print_report():
def print_order_summary(state, markets):
def print_candidate_assessments(markets):
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收口 strategy 层到 YES-only 候选与路由事实</name>
  <files>weatherbot/strategy.py, tests/test_phase2_strategies.py, tests/test_strategy_paper_execution.py, tests/test_phase3_scan_loop.py</files>
  <behavior>
    - Test 1: `build_candidate_assessments()` 只返回 YES 候选，且保留 YES 峰值窗口过滤、报价拒绝原因与 size_down/accepted 语义。
    - Test 2: `reconcile_market_reservation()` / `scan_and_update()` 只接受 YES_SNIPER 保留暴露，不再生成 NO_CARRY route decision / reservation。
    - Test 3: 已存在的 YES risk_state / route_decisions / reserved_exposure JSON 结构继续可恢复，不新增新的共享状态模型。
  </behavior>
  <action>在 `weatherbot/strategy.py` 删除 NO 活跃运行时分支：去掉 `evaluate_no_candidate()` 参与的候选生成、NO leg routing、NO 预算/暴露统计写入，以及任何会让 scan loop 持久化 NO candidate_assessments / route_decisions / reserved_exposure 的路径。保留 YES 的 `evaluate_yes_candidate()`、峰值窗口惩罚、已有 shared risk ledger、order lifecycle 调用点和 market JSON 事实源。同步改写 `tests/test_phase2_strategies.py`、`tests/test_strategy_paper_execution.py`、`tests/test_phase3_scan_loop.py`，把断言改成 YES-only runtime 合同；不要把 YES 逻辑简化成占位实现，也不要动 config/schema/docs。</action>
  <verify>
    <automated>uv run pytest --no-cov tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py tests/test_phase3_scan_loop.py -x</automated>
  </verify>
  <done>扫描与路由层不再生成 NO 活跃候选/保留暴露；YES 候选评估、峰值窗口过滤、scan persistence 与 shared risk_state 继续通过回归测试。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收口 paper execution 到 YES-only 挂单入口但保留共享生命周期</name>
  <files>weatherbot/paper_execution.py, tests/test_phase4_orders.py, tests/test_phase4_scan_loop.py</files>
  <behavior>
    - Test 1: `build_passive_order_intent()` 仅对 YES 接受 route/reservation/quote 构建订单，NO 输入会被拒绝且不会生成活跃订单。
    - Test 2: `sync_market_order()` / `monitor_active_orders()` 继续支持 YES 订单提交、部分成交、取消、改价、过期、恢复与持仓落地。
    - Test 3: `active_order`、`order_history`、`paper_execution_state` 的共享状态结构保持兼容，不因删除 NO 路径而破坏恢复逻辑。
  </behavior>
  <action>在 `weatherbot/paper_execution.py` 移除 NO 专用下单定价与 runtime 接受路径，让被动挂单入口只消费 YES reservation/assessment/token_side；保留通用订单状态机、paper execution 事件、archive/restore、取消与过期逻辑，因为这些仍是 YES 运行时必须的共享基础设施。同步更新 `tests/test_phase4_orders.py` 与 `tests/test_phase4_scan_loop.py`，删掉 NO active-order 正向路径断言，改为 YES-only 生命周期回归和 NO 被拒绝/不生成订单的断言。</action>
  <verify>
    <automated>uv run pytest --no-cov tests/test_phase4_orders.py tests/test_phase4_scan_loop.py -x</automated>
  </verify>
  <done>paper execution 不再接受或生成 NO 活跃订单；YES passive order 提交到成交/取消/过期/恢复链路完整通过回归测试。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 收口 reporting 输出到 YES-only runtime 事实</name>
  <files>weatherbot/reporting.py, tests/test_phase2_reporting.py, tests/test_phase3_reporting.py, tests/test_phase4_reporting.py</files>
  <behavior>
    - Test 1: `print_status()` / `print_report()` 不再输出 NO_CARRY 候选、NO 风险腿或 NO 活跃订单路径。
    - Test 2: YES 扫描摘要、YES route/risk summary、YES active/terminal order 事实仍完整可见。
    - Test 3: 报告层继续通过 `bot_v2` 别名入口工作，不需要新增用户可见配置或文档改动。
  </behavior>
  <action>在 `weatherbot/reporting.py` 移除 NO 活跃路径的展示与计数：候选摘要、risk usage、route decision summary、order lifecycle summary 只反映 YES-only runtime 事实；保留 scan accepted/skipped、YES order history、recent terminal orders/reasons、trade stats 分离规则。同步更新 phase2/3/4 reporting 测试，确保输出从 dual-leg 改为 YES-only，但仍覆盖共享报告结构。除非验证失败，不要修改 `bot_v2.py`；它应继续仅作为 `weatherbot` 别名入口。</action>
  <verify>
    <automated>uv run pytest --no-cov tests/test_phase2_reporting.py tests/test_phase3_reporting.py tests/test_phase4_reporting.py -x</automated>
  </verify>
  <done>CLI 报告层不再展示 NO 活跃运行时路径，YES-only 扫描/风控/订单事实仍完整可读并通过回归测试。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forecast/market data → strategy runtime | 外部天气与盘口输入进入候选评估与路由，必须避免因删 NO 分支造成错误接受/拒绝 YES 候选。 |
| persisted market JSON → execution/reporting | 本地持久化事实驱动订单恢复与报告，必须避免 NO 历史字段删除导致恢复或输出崩溃。 |
| strategy → paper execution | 候选/保留暴露到订单意图的连接必须只接受 YES 合同，不能让 NO 通过旧分支偷偷下单。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260418-w8f-01 | T | `weatherbot/strategy.py` candidate routing | mitigate | 仅删除 NO 活跃路径，保留 YES `evaluate_yes_candidate()` 与 shared risk ledger；用 scan/routing 回归测试锁定 YES persistence。 |
| T-quick-260418-w8f-02 | E | `weatherbot/paper_execution.py` order intent | mitigate | 让 `build_passive_order_intent()` 明确拒绝 NO token_side/strategy_leg，并用 phase4 order/scan-loop 测试验证不会生成 NO active_order。 |
| T-quick-260418-w8f-03 | D | `weatherbot/reporting.py` persisted facts rendering | mitigate | 报告层继续读取 `active_order` / `order_history` / `route_decisions` 既有事实结构，只收窄到 YES-only 输出；用 reporting 回归测试防止 crash 或计数漂移。 |
</threat_model>

<verification>
按任务分三组运行 pytest；完成后再执行一次总回归：`uv run pytest --no-cov tests/test_phase2_strategies.py tests/test_strategy_paper_execution.py tests/test_phase4_orders.py tests/test_phase2_reporting.py tests/test_phase3_reporting.py tests/test_phase3_scan_loop.py tests/test_phase4_reporting.py tests/test_phase4_scan_loop.py -x`。
</verification>

<success_criteria>
- `weatherbot/strategy.py` 不再持久化或路由 NO 活跃候选，但 YES 峰值窗口过滤与 shared state 语义保持不变。
- `weatherbot/paper_execution.py` 不再构建或恢复 NO 活跃订单，但 YES 订单生命周期与持仓落地保持可用。
- `weatherbot/reporting.py` 不再输出 NO 活跃路径，且所有指定 runtime 回归测试通过。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-w8f-yes-only-runtime/260418-w8f-SUMMARY.md`
</output>
