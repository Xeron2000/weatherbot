---
phase: quick-260418-j1b-no-strategy-min-probability-0-95-0-90-no
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.json
  - .planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md
autonomous: true
requirements:
  - QUICK-260418-J1B
must_haves:
  truths:
    - "本地 `no_strategy.min_probability` 已从 0.95 下调到 0.90，用于本次 NO 参数实验。"
    - "执行了一次单次扫描，并能基于 persisted `candidate_assessments` 对比 NO 的 accepted/rejected 变化。"
    - "`config.json` 中的 secret 保持本地使用，不进入 git，也不波及 `.planning/config.json`、`04-VERIFICATION.md` 或手工编辑 `data/`。"
  artifacts:
    - path: "config.json"
      provides: "本地 NO 策略实验阈值"
      contains: '"min_probability": 0.90'
    - path: ".planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md"
      provides: "本次调参与扫描前后 NO 候选变化结论"
      contains: "baseline vs post-scan NO comparison"
  key_links:
    - from: "config.json"
      to: "weatherbot/strategy.py"
      via: "load_config() -> NO_STRATEGY.min_probability"
      pattern: "no_strategy|min_probability|load_config"
    - from: "weatherbot/strategy.py"
      to: "data/markets/*.json"
      via: "scan_and_update() -> build_candidate_assessments()"
      pattern: "evaluate_no_candidate|candidate_assessments|probability_below_min"
    - from: ".planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md"
      to: "data/markets/*.json"
      via: "记录扫描前后 NO accepted/rejected/reasons 变化"
      pattern: "NO_CARRY|accepted|rejected"
---

<objective>
执行一次最小本地参数实验：把 `no_strategy.min_probability` 从 `0.95` 下调到 `0.90`，复跑单次扫描，并对比 NO 候选通过/拒绝变化。

Purpose: 用最小扰动验证上个 quick 的结论——NO 常见 `probability_below_min` 是否会因阈值放宽而出现可观候选释放，而不是继续停留在纯判断层讨论。
Output: 一份 local-only 实验 summary + 本地 `config.json` 阈值更新。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@config.json
@weatherbot/strategy.py
@.planning/quick/260418-ilo-weatherbot-no-probability-below-min-visu/260418-ilo-SUMMARY.md

<interfaces>
From `config.json`:

```json
"no_strategy": {
  "min_price": 0.80,
  "min_probability": 0.95,
  "min_edge": 0.03
}
```

From `weatherbot/strategy.py`:

```python
def evaluate_no_candidate(bucket_probability, quote_snapshot, hours):
    ...
    if bucket_probability.get("fair_no", 0.0) < NO_STRATEGY.get("min_probability", 0.0):
        reasons.append("probability_below_min")
```

Prior evidence from `260418-ilo-SUMMARY.md`:

```text
NO_CARRY 的 fair_no 样本上界约 0.9453，均值约 0.7609；
`probability_below_min` 主因是阈值 0.95 高于当前 fair_no 分布。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化 0.95 基线 NO 候选分布并建立对比口径</name>
  <files>.planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md</files>
  <action>先不要改配置，基于当前 `data/markets/*.json` 的 persisted `candidate_assessments` 提取 NO 基线：仅统计 `strategy_leg == "NO_CARRY"` 的记录，至少按 `status`（accepted / size_down / reprice / rejected）和关键 reason（尤其 `probability_below_min`、`price_below_min`）出一组 baseline 计数，并挑出 2-5 个代表 bucket 样本写入 summary。summary 中要明确这组 baseline 对应的是当前 `config.json` 的 `no_strategy.min_probability = 0.95`，并引用上个 quick 的结论：主因是 fair_no 分布低于 0.95，而不是旧 no.bid bug。不要手工编辑 `data/`，只读取现有 persisted 数据。</action>
  <verify>
    <automated>uv run python -c "import json; from pathlib import Path; vals=[]; statuses={}; reasons={};
for p in Path('data/markets').glob('*.json'):
    data=json.loads(p.read_text(encoding='utf-8'))
    for item in data.get('candidate_assessments', []) or []:
        if item.get('strategy_leg')!='NO_CARRY':
            continue
        statuses[item.get('status','unknown')]=statuses.get(item.get('status','unknown'),0)+1
        for r in item.get('reasons',[]) or []:
            reasons[r]=reasons.get(r,0)+1
        vals.append(float(item.get('fair_no') or 0.0))
assert vals and min(vals) < 0.95 and max(vals) <= 1.0
assert reasons.get('probability_below_min',0) > 0" && uv run python -c "from pathlib import Path; text=Path('.planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md').read_text(encoding='utf-8'); assert '0.95' in text and 'probability_below_min' in text and 'baseline' in text.lower()"</automated>
  </verify>
  <done>summary 已固化一份可复用的 0.95 基线，能作为本次 0.90 实验后的对比参照。</done>
</task>

<task type="auto">
  <name>Task 2: 仅做本地配置实验，把 NO 最小概率阈值下调到 0.90</name>
  <files>config.json</files>
  <action>把 `config.json` 中 `no_strategy.min_probability` 从 `0.95` 改为 `0.90`，除此之外不要改其他策略参数，也不要改代码逻辑。`vc_key` 与其他本地 secret 必须原样保留且不得出现在 summary、git diff 说明或提交中。把这次操作明确视为 local-only config experiment：不得 `git add config.json`，不得为了可提交去覆盖/清空 secret；同时不要编辑或暂存 `.planning/config.json`、`.planning/phases/04-执行paper订单生命周期与状态恢复/04-VERIFICATION.md`，也不要手工改 `data/`。</action>
  <verify>
    <automated>uv run python -c "import json; from pathlib import Path; cfg=json.loads(Path('config.json').read_text(encoding='utf-8')); assert cfg['no_strategy']['min_probability']==0.90; assert cfg.get('vc_key') and cfg['vc_key']!='YOUR_KEY_HERE'" && uv run python -c "from weatherbot.config import load_config; cfg=load_config(); assert cfg['no_strategy']['min_probability']==0.90 and cfg.get('vc_key')" && test -z "$(git diff --cached --name-only -- config.json)" && test -z "$(git diff --cached --name-only -- .planning/config.json)" && test -z "$(git diff --cached --name-only -- '.planning/phases/04-执行paper订单生命周期与状态恢复/04-VERIFICATION.md')" && test -z "$(git diff --name-only -- .planning/config.json)" && test -z "$(git diff --name-only -- '.planning/phases/04-执行paper订单生命周期与状态恢复/04-VERIFICATION.md')"</automated>
  </verify>
  <done>本地配置已切到 0.90，secret 仍只留在本地，且没有把 `config.json` 或无关脏文件放进暂存区。</done>
</task>

<task type="auto">
  <name>Task 3: 复跑单次扫描并对比 0.90 下 NO accepted/rejected 变化</name>
  <files>.planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md, config.json</files>
  <action>执行一次且仅一次扫描，使用 `uv run python -c "import bot_v2; bot_v2.scan_and_update()"`，不要启动无限循环。扫描后重新统计 `data/markets/*.json` 中 `NO_CARRY` 的 `status` 与关键 `reasons`，并与 Task 1 的 0.95 baseline 做对比：至少写清 accepted/size_down/reprice/rejected 的前后计数变化、`probability_below_min` 是否下降、是否出现新释放的代表 bucket。若 accepted 仍几乎没有增长，也要如实记录“0.90 仍不足以显著放行”并指出实际主导原因；不要把结果归咎回旧 no.bid bug。summary 中同时写明：本次扫描对 `data/` 的写入仅是运行时副作用，不属于手工编辑。</action>
  <verify>
    <automated>uv run python -c "import bot_v2; bot_v2.scan_and_update()" && uv run python -c "import json; from pathlib import Path; statuses={}; reasons={}; no_items=[]
for p in Path('data/markets').glob('*.json'):
    data=json.loads(p.read_text(encoding='utf-8'))
    for item in data.get('candidate_assessments', []) or []:
        if item.get('strategy_leg')!='NO_CARRY':
            continue
        no_items.append(item)
        statuses[item.get('status','unknown')]=statuses.get(item.get('status','unknown'),0)+1
        for r in item.get('reasons',[]) or []:
            reasons[r]=reasons.get(r,0)+1
assert no_items and sum(statuses.values())==len(no_items)
assert reasons.get('probability_below_min',0) >= 0" && uv run python bot_v2.py status && uv run python -c "from pathlib import Path; text=Path('.planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md').read_text(encoding='utf-8'); assert '0.90' in text and 'accepted' in text and 'rejected' in text and 'probability_below_min' in text and 'local-only' in text"</automated>
  </verify>
  <done>已完成单次扫描并在 summary 中写出 0.95→0.90 后 NO 候选的前后变化，而不是只停留在“改了参数”。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `config.json` → git history | `vc_key` 等 secret 一旦被提交会直接泄露 |
| local config threshold → live scan output | 本地参数实验会改变候选评估与 `data/markets/*.json` 落盘事实 |
| persisted market JSON → experiment summary | 若前后口径不一致，会把实验结果写成错误结论 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260418-j1b-01 | I | `config.json` secret | mitigate | `config.json` 只做 local-only 调整，不暂存、不提交，summary 不回显明文 secret。 |
| T-quick-260418-j1b-02 | T | `config.json` / strategy params | mitigate | 只改 `no_strategy.min_probability`，验证其他策略参数与代码逻辑未被顺手修改。 |
| T-quick-260418-j1b-03 | T | `data/markets/*.json` | mitigate | 禁止手工编辑 `data/`；只允许单次 `scan_and_update()` 作为运行时副作用写回。 |
| T-quick-260418-j1b-04 | R | experiment summary | mitigate | summary 中显式区分 baseline(0.95) 与 post-scan(0.90) 口径，并记录 accepted/rejected/reasons 的前后计数。 |
</threat_model>

<verification>
- `config.json` 的 `no_strategy.min_probability` 已为 `0.90`，且 secret 仅本地存在。
- 执行一次 `uv run python -c "import bot_v2; bot_v2.scan_and_update()"`，随后用 `uv run python bot_v2.py status` 复查。
- `260418-j1b-SUMMARY.md` 写清 baseline vs post-scan 的 NO accepted/rejected/reasons 变化。
</verification>

<success_criteria>
- 这次 quick 交付的是一次可验证的本地参数实验，而不是代码重构。
- 能明确回答：`0.95 -> 0.90` 后，NO 候选通过率和 `probability_below_min` 拒绝情况发生了什么变化。
- `config.json` 中的 secret 没有进入 git，且无关脏文件保持未触碰状态。
</success_criteria>

<output>
After completion, create `.planning/quick/260418-j1b-no-strategy-min-probability-0-95-0-90-no/260418-j1b-SUMMARY.md`
</output>
